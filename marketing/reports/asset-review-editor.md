# 渠道落地包物料 · 增量人工复审报告（Editor / PRO-18）

> 交付方：Editor（PM Toolkit）
> 审查对象：PRO-16 复审判定处置后的渠道执行包物料（分支 `PRO-1-pro-1`）
> **复审基线：commit `28c1309`**（= PRO-16 处置提交；其父 `7127d64` = PRO-14 增量复审报告；`7127d64` 的父 `0830dd7` = PRO-15 上游条款同步；再上为 `725f5b9` = PRO-13 收尾）
> 审查相位：**`pre_t0`**（T0 = MVP 可灰度日 = PRO-7 交付；复核时点 PRO-7 看板状态仍为 `blocked`）
> 本报告为**就地覆盖版本**（不留 V1/V2/V3）：本文件是唯一现行版本，修订即覆盖。
> 性质声明：本报告是**增量人工通读判定**，不重写文案、不改 code、不改 `docs/05`/`docs/06`/`docs/07`、不重跑物料生成脚本、不新增任何数字或承诺等级。判定为「需再改」的条目一律以「原文 + 改法」退回 Marketing Strategist 处置。

### 基线取值说明

本单标题与 `docs/08` §10.2 均指定**复审基线 = `28c1309`**。本报告的全部判定只引用该 commit 的文件内容；`28c1309` 之后分支上的任何提交**不在**本报告取值范围内（见下节）。

### 复审期间的并发提交（已记录，不影响本单取值）

本轮复审进行中，同一分支 `PRO-1-pro-1` 收到 **PRO-17（Marketing Strategist）** 的两个提交：`1273783`（「记录收口与口径对齐」：`docs/08` §4.4 #5 / §9.5 #3 标注已闭合、§4.3 影响行两条→三条候选、§9.4② 补机制措辞出处、四处 `e3414ef` 指针改写、新增 §11）与 `36d398e`（§11 补「已知残留」行）。两提交**只改 `docs/08`**，未触及 `marketing/` 任何物料、词表、判据或 `scan_manifest.json`。

- 该并发提交**未被本报告审阅**（超出本单基线），也**不影响**本报告任何判定；但它是**必须由下一轮复审或 board 知悉的分支状态变化**（本报告 §3 的「需再改」中，有 2 项与该提交的改动面重叠，已逐处标注）。
- **取值防护（本轮新增做法）**：因 `docs/08` 在同一工作区被并发改写，本报告的**全部逐字核对与机器复跑**均在 `28c1309` 的**独立只读副本**（`git archive 28c1309` 解到工作区之外）中进行，避免误取被并发改写后的 `docs/08`。原始回执见 §5。

---

## 0. 结论摘要

### 0.1 逐条判定（R-1 ~ R-7 + PRO-16 补充三项）

| # | PRO-14 判定 | 本轮判定 | 判定依据（要点） |
|---|---|---|---|
| R-1 | **高**：未兑现暗示（曝光「可回读」现在时断言） | **部分闭合** | PRO-14/§10 列出的 **6 处落点全部真改到位**（逐字核对 ✓）；**但同文件仍留 2 处同类现在时能力断言未加条件式**，且 2 处模板预设未实现位 → 见 §3 N-1 |
| R-2 | 中：脚本 C 个人事实写死 3 处 | **已闭合** | 3 处（`三条` / `三条 24h` / `×3`）全部改为 `〈真实条数〉`，与同脚本 L84 占位自洽 ✓ |
| R-3 | 中：§0 第 2 条「不可删」第③类无对应正文句 | **已闭合** | `share_scripts.md` §2 正文首行已补 `我是创作者，不是官方号。〈不可删〉`，§0 三类与正文实例一一对应 ✓ |
| R-4 | 中：「**所有**用户可见文案取自 `ui.*`」与实现不符 | **部分闭合** | `docs/08` §1.3/§9.3 D-4/§7 + `scan_manifest.json` 四处口径已收窄，行号引用与 `app.py` 现状**逐处相符** ✓；**但 `marketing/README.md` §3 尾句仍保留未收窄的同一断言** → 见 §3 N-2 |
| R-5 | 低：§9.4 复核口径枚举不完整 | **已闭合** | 独立复跑同一 `grep`：命中与 §9.4 末枚举的**四处语境完全一致**，且**无一处是投放文案** ✓ |
| R-6 | 中：门禁能力表述过强 | **已闭合** | 三处限定齐备（§0.1 ② / §8 #9 / 模板 `notes`），且与 `t0_evidence_gate()` 的真实校验范围一致 ✓ |
| R-7 | 低：`docs/08` 三行失准 | **部分闭合** | 7b（上游同步三处 + §6.2 #5）**已闭合且未重开上游修改** ✓；**7a 因本轮就地覆盖而再度失准**（§7 + `README` 仍写「现行版本 = PRO-14 / 基线 `725f5b9`」），另有 4 处 `e3414ef` 缺「分支实际提交 `0830dd7`」指针 → 见 §3 N-3 |
| 附① O-5 采纳 | 低 | **已闭合** | §6.1 新增「作品页『曝光回读位』+『推荐理由回读位』」一行、§6.3 移交行同步 ✓（**但该登记恰好使 N-1 成立**，见 §2.1） |
| 附② §9.2 L-5 / L-7 自评行同步 | 低 | **部分闭合** | L-7 已按 PRO-14 改判结论改写 ✓；**L-5 的收口句「同类现在时断言已按 R-1 一并清理」过宽**，与 N-1 的事实不符 → 见 §3 N-1 |
| 附③ `README` 与 §7 报告版本行同步 | 低 | **两者互相一致 ✓，但均已再度失准** | `docs/08` §7 与 `README` §1 目录树都写「现行版本 = PRO-14 增量复审，基线 `725f5b9`」；本轮覆盖后应改为 PRO-18 / `28c1309` → 见 §3 N-3 |

**合计（10 行判定）：已闭合 5 项（R-2/R-3/R-5/R-6 + 附①）· 部分闭合 4 项（R-1/R-4/R-7 + 附②）· 未闭合 0 项 · 另 1 项（附③）为「同步已达成、但随本轮覆盖一并失准」· 归并后需再改 3 项：N-1（投放安全相关）· N-2、N-3（文档准确性）。**

### 0.2 投放放行结论

**不通过（需再改后复验）。**

- **0 项阻断级**：机器自检独立复跑为「42 项单测 OK + 合规器 阻断级 0 / 告警级 0」（§5）；本轮未新增任何数字、未新增任何承诺等级。
- **但 N-1 是投放安全相关**：`community_scripts.md`（扫描目标 T-02，`pre_t0` 相位）中仍有**现在时**能力断言未被条件式限定，其所指兑现位按本包**自己的登记**（`docs/08` §6.1，PRO-16 已补登）为「❌ 未实现」。按本包**自定红线**（`docs/08` §9.2 L-5「未实现，**不得对外暗示已可用**」），该项在清除前不得进入对外投放。
- N-2、N-3 属**文档口径准确性**，不是投放违规，不单独构成阻断；但按本包「记录失真即缺陷」的既有纪律（PRO-14 R-5/R-7 同款），应与 N-1 一并就地修订。
- **范围**：本轮偏离面很窄（`community_scripts.md` 3 处 + `marketing/README.md` 1 行 + `docs/08` 2 行 + 4 处指针）。修完只需**按新 commit 重跑一次增量复审**（`docs/08` §9.5 #1 现行口径）。

### 0.3 机器自检与人工通读的差距（本单存在的理由，实测）

| 维度 | 机器自检（本轮独立复跑，基线 `28c1309`） | 人工通读（本报告）补充 |
|---|---|---|
| 禁用表述 | `hard_ban=18 / t0_gated=6 / 9 目标`，**阻断级 0 / 告警级 0**（exit 0） | 「是否构成未兑现暗示」不在词表覆盖内 → 本轮仍发现 **2 处同类现在时断言 + 2 处未实现位预设**（N-1） |
| 文案 ↔ 实现对照 | 只校验「文案是否含禁用词」 | 「`ui.*` 是否真的覆盖全部对外文案」「`app.py` 是否真的不再保留对外字面量」需逐字符串核对 → 发现 `README` 尾句仍未收窄（N-2） |
| 处置记录自身的准确性 | 不检查 | `docs/08` §7/§9.2 L-5 与 `README` 的自评与实际不符 → N-1 / N-3 |

**机器自检不等于合规**：本轮 42 项单测全绿、合规器 0/0，但上表三项**没有一项**是机器可以发现的。人工通读不可省略。

---

## 1. 方法与判据（沿用 PRO-12 / PRO-14，不放宽、不收紧）

1. 判据来源同 PRO-12 §1.1：`docs/05` §0.3/§3.5/§4.1/§5.4、`lexicon.json`（`hard_ban` 18 + `t0_gated` 6 + `required_tokens` 4）、`docs/08` §4.3 收紧裁定（T0 前禁一切数字**与一切结果承诺（含无数字）**）。
2. 本单**新增判据（来自 PRO-14 §7.6 与 `docs/08` §9.5 #1 的现行口径）**：物料一经修订，人工通读必须**按新 commit 重跑**；复审出具「通过」前不得投放。
3. 三条外部边界：不做站内关系链表述、不比算法精度、不作对手不透明指控、不变现承诺。
4. 逐字通读范围（**全部以 `28c1309` 内容为准**）：`marketing/data/*.md`（5 份）、`marketing/landing/copy_pack.json`、`marketing/landing/app.py`、`marketing/landing/compliance.py`、`marketing/scripts/compliance_lint.py`、`marketing/compliance/*.json`、`marketing/data/t0_evidence.template.json`、`marketing/README.md`、`docs/08` 全文、`docs/04`（范围）、`server/recommend.py`（`_reason_for` 实际能力）。
5. 对 PRO-16 的机器回执**独立复跑而非引用**（§5）；并在 `28c1309` 的**独立只读副本**上复跑（原因见「复审期间的并发提交」）。
6. 本单不做：商标/域名初筛；改 `docs/05`/`docs/06`/`docs/07`；对外部署；重跑物料生成脚本；重写任何文案；改 `docs/08` 正文（判定一律以「原文 + 改法」提出）。

---

## 2. 逐条复核（不接受笼统结论）

### 2.1 R-1（高）未兑现暗示：现在时能力断言是否真的清零

**PRO-14/§10 列出的 6 处落点 —— 逐字核对，全部真改到位（✓）：**

| # | 落点（`28c1309`） | 当前原文 | 判定 |
|---|---|---|---|
| 1 | `community_scripts.md` 脚本 A ③（L46） | `③ 曝光数字可以在自己的作品页回读（回读位随功能开放）。` | ✓ 已加条件式 |
| 2 | `community_scripts.md` 脚本 B 正文（L68） | `我怎么拿到这些数字：来自作品页的回读位（随功能开放，开放后不用问后台）。` | ✓ 已加条件式 |
| 3 | `community_scripts.md` §3 异议表（L102、L103） | 「…你可以自己核对（曝光回读位随功能开放）。」／「…理由在作品页（展示位随功能开放）。」 | ✓ 2 行都已限定 |
| 4 | `share_scripts.md` §2（L34）、§3（L44） | 「…理由的展示位随功能开放，开放后这一点我能自己核对。」／「那边会显示每条的推荐理由（展示位随功能开放），」 | ✓ 2 行都已限定 |
| 5 | `creator_outreach.md` §4（L59） | 「…而且你会看到每条内容的推荐理由（展示位随功能开放）。」 | ✓ 已限定 |
| 6 | `copy_pack.json` 招募 V1/V2 `pre_t0` bullets（L39、L68） | 「每条内容配一份可回读的推荐理由（展示位随功能开放）」／「每条内容的推荐理由可回读（展示位随功能开放）」 | ✓ 已限定（分享 V2 `pre_t0` 维持 PRO-13 的降级措辞，未回退） |

**跨文件强度差是否消除**：PRO-14 §0.2 记录的「同一能力出现条件式 vs 无条件式两种强度」在**上述 6 处**已消除 ✓ —— 同一能力（推荐理由／曝光回读）在 5 份物料里的表述强度一致。

**同一能力是否已**全部**限定 —— 否（本轮的实质发现，N-1）**：`community_scripts.md`（T-02，`pre_t0`）内仍有 2 处**现在时**能力断言未加任何条件式：

- **L44（脚本 A ①）**：`① 每条内容都写了一行推荐理由，我能看到它为什么被推；`
- **L87（脚本 C 正文）**：`而且每条都会给一行推荐理由——至少我们知道东西发出去之后发生了什么。`

并且同文件 2 处**模板预设了未实现的兑现位**：

- **L30（占位符替换规则）**：`② 有推荐理由原文（从作品页复制，不得改写）`
- **L53（脚本 A 评论区模板）**：`【推荐理由】⟨从作品页原文复制，不得改写⟩`

**为何这在 `28c1309` 上确属未闭合（三条本包自证，非外部标准）**：

1. `docs/08` §6.1（**PRO-16 本轮采纳 PRO-14 O-5 新增**）登记：`作品页「曝光回读位」+「推荐理由回读位」 … ❌ 未实现`，并明确其定性为「**分享话术/社群脚本所引「回读」的兑现位**；亦为承诺卡 §1 第 4 条的兑现位」。即：本包**自己**把社群脚本引用的「回读」位置登记为未实现。
2. `docs/08` §9.2 L-5 的**自定红线**保留未动：`（未实现，不得对外暗示已可用）`。L44/L87 以现在时断言「**我能看到它**」／「**我们知道…发生了什么**」→ 与该红线冲突。
3. `docs/08` §9.2 L-5 的收口句已声称为「**同类现在时断言已按 R-1 一并清理**」（并逐一列名）。**该收口句过宽**：它列出的名字里不含 L44/L87，而这两行是同一能力、同一文件、同一相位的同类断言 → 记录与事实不符（附② 判为部分闭合）。

**存在一处反向依据，必须如实写出（本报告的判定边界）**：`docs/08` §2.3 第 2 条使用约束（L181）**把 L44 这一句当作「可写」的正面例子**：`脚本里只能写实现里真实存在的机制（例：可写「我能看到它为什么被推」；不得写「能看到它被推给谁」—— server/recommend.py 的 _reason_for() 不含受众信息）`；且 `docs/04` FR-2.6.a 登记 `reason` 覆盖率 100%（`_reason_for` 已实现），PRO-14 §2.1 #8 / §2.2 L-3 也据此判该项「已闭合」。

→ 因此本轮的问题**不是**「L44 凭空越界」，而是 **`docs/08` §2.3 的「可写」示例与 `docs/08` §6.1 + §9.2 L-5 在同一次修订里互相矛盾**：前者以「`reason` 字段已实现」为准，后者以「作品页回读位未实现」为准，两者指向同一句文案。**矛盾必须由处置方显式消解**（两种改法见 §3 N-1），不能同时保留。

### 2.2 R-2（中）个人事实占位是否全部闭合 —— **已闭合**

| 位置（`community_scripts.md` 脚本 C） | 当前原文 | 判定 |
|---|---|---|
| 正文 L84 | `帮一家开了〈真实店龄〉年的〈店名 / 品类〉拍了〈真实条数〉条。` | ✓ |
| 正文 L89（PRO-14 原文「我把**三条**的…」） | `我把〈真实条数〉条的真实曝光数和推荐理由贴在评论区了。` | ✓ 已改 |
| 评论区 L93（原「【实证】**三条** 24h 曝光」） | `【实证】〈真实条数〉条 24h 曝光：⟨〈真实条数〉条 24h 真实曝光数，未拿到数据前不得发布⟩` | ✓ 已改 |
| 评论区 L94（原「【推荐理由】⟨原文 **×3**⟩」） | `【推荐理由】⟨原文 ×〈真实条数〉⟩` | ✓ 已改 |

同脚本内 `〈真实条数〉` 用法**自洽**（4 处同形）；脚本 B L75 的 `N=⟨真实条数，未拿到数据前不得发布⟩` 亦为占位形式，未写死。§2 使用约束（L34）保留 ✓。**未见新的写死事实。**

### 2.3 R-3（中）「不可删」第③类与正文是否一致 —— **已闭合**

- `share_scripts.md` §0 第 2 条（L15）仍列三类，第③类 = `社群版的身份披露句——「我是创作者，不是官方号」。`
- §2 社群版正文**首行（L32）**现为：`我是创作者，不是官方号。〈不可删〉` → 规则与正文**一一对应** ✓（PRO-14 的 R-3 原文「§2 正文中不存在该句」已不成立）。
- 交叉核对：三处 `〈不可删〉` 标记（§1 L22/L24/L26、§2 L32/L35、§3 L45）均落在 §0 列明的三类句上，无「标了标记但不在三类里」或「属于三类但无标记」的情况 ✓。
- 与 `community_scripts.md` §1 硬门 3（身份披露）的要求现已一致 ✓。

### 2.4 R-4（中）口径收窄与登记理由 —— **部分闭合**（`docs/08` + `scan_manifest` 已闭合；`README` 未同步）

**已闭合部分（逐字核对，与 `app.py` 现状相符 ✓）**：

| 落点 | 当前原文（`28c1309`） | 与实现是否相符 |
|---|---|---|
| `docs/08` §1.3 界面文案行（L130） | 「**对外文案**（页面正文与内联脚本提示语）已抽到 … `ui.*`；**仍留在 `app.py` 的不属投放文案**：页脚版本/变体/候选标签（L720-721、L749，运维核对用）、渲染兜底默认值（L712/716/732/758）与 `/api/*` 错误文案（PRO-14 R-4 口径收窄）」 | ✓ **行号逐处命中**：L712 `联系方式` 兜底、L716 `提交申请` 兜底、L720-721 页脚 `页面版本/变体/候选`、L732 `内测名额申请` 兜底、L749 分享页页脚、L758 `分享` 兜底；`/api/*` 中文错误文案 L884–1012（`未知变体`、`缺少 asset`、`服务内部错误…` 等）确实仍在文件内 |
| `docs/08` §9.3 D-4（L492） | 「**已闭合（机制）+ 范围限定**…」 | ✓ 与 `ui.*` 12 键受同一扫描、必需键缺失 exit 4 的机制相符 |
| `docs/08` §7 文件清单 `app.py` 行（L407） | 「**对外文案**已抽到文案包，页脚标签/兜底值与 `/api/*` 错误文案仍在内、不属投放文案」 | ✓ |
| `scan_manifest.json` `device_files` 的 `app.py.reason` | 「自 PRO-13 起其**对外文案**…已抽到 `ui.*`（由 T-01 扫描）；仍留在本文件的是页脚版本/变体/候选标签（运维核对用）、渲染兜底默认值与 `/api/*` 错误文案——均为站内运维/异常路径文本，不属对外投放文案…（PRO-14 R-4：原登记理由…不实，已改写为现状…）」 | ✓ 登记理由与 `app.py` 现状**相符**，且**自我标注了改写原因** |

**未闭合部分（N-2）**：`marketing/README.md` §3 尾句（L77）仍为**未收窄的同一断言**：

```
因此「T0 前误投量化承诺」与「T0 前误用 t0 臂文案」在装置层都不可能发生；
页面与脚本里用户可见的每一句话都来自受扫描的文案包，不再是代码里的硬编码。
```

该句与 PRO-14 R-4 判为「不成立」的「**所有**用户可见文案」「代码内不再保留对外文案字面量」是同一命题，且 `README.md` 属本包**物料索引与运行说明**（下游运维/上线整合的第一入口）。PRO-14 §2.5.2 曾把 `README` 判为「未失准」，但当时只核对了数字与命令行，**漏了本句**；PRO-16 本轮改了 `README` 的**报告版本行**（L25），**未改本句** → 同一文件里出现「口径已收窄（`docs/08`/`scan_manifest`）」与「口径未收窄（`README`）」的并存。

### 2.5 R-5（低）§9.4 复核口径枚举 —— **已闭合**

独立复跑（原始回执见 §5）：`grep -rn "曝光下限\|产品里的一条规则\|写进产品的曝光规则" marketing/ docs/08`

实际命中与 §9.4 末（L515）枚举**逐类一致**：

| §9.4 枚举 | 实际命中 | 判定 |
|---|---|---|
| ① `lexicon.json` 的 HB-18 规则定义与 TG-02 `reason` | HB-18（L37 `pattern` 含「曝光下限」）、TG-02（L41 `reason` 含「曝光下限承诺」） | ✓ |
| ② `asset-review-editor.md`（审阅报告引用行）与 `asset-compliance-report.md`（原始命中明细行） | `asset-review-editor.md` L76、L188；`asset-compliance-report.md` L33 | ✓ |
| ③ `tests/test_compliance.py` L47/L186 | L47（注释）、L186（单测样例） | ✓ **行号精确一致** |
| ④ 本文件处置记录行 + `__pycache__` 编译产物 | `docs/08` L265/L457/L512/L515；`marketing/tests/__pycache__/…pyc`（binary match） | ✓ |

**结论采纳**：`无一处是投放文案` ✓ —— 独立复核 `marketing/data/`（5 份物料）与 `copy_pack.json` **零命中**，故「无投放文案命中」成立，「枚举不完整」这一 R-5 原判已消除。

### 2.6 R-6（中）门禁能力表述是否已补限定 —— **已闭合**

| 落点 | 当前原文（`28c1309`） | 判定 |
|---|---|---|
| `docs/08` §0.1 ②（L24） | 「**门禁的能力边界（PRO-14 R-6，必须一并读）**：它校验的是「**是否提交了结构完整的 T0 证据**」（两个 true + 八项条目 + 采集日非空），**不校验八项证据内容的真伪**；证据本身的真实性由证据留档 + 人工复核负责。」 | ✓ |
| `docs/08` §8 第 9 条（L438） | 「…证明的是「在**结构完整的 T0 证据已提交**的前提下，装置不会在 T0 前进入 `t0` 相位」…**限定（PRO-14 R-6）**：T0 证据门禁只校验证据**信封**…**不校验八项证据内容的真伪**；证据真实性由留档 + 人工复核负责，门禁不能替代人工判断。」 | ✓ |
| `t0_evidence.template.json` `notes` | 「门禁能力边界（PRO-14 R-6）：启动校验只检查证据信封的结构完整性——`pro7_delivered` / `preconditions_all_green` 均为 true、`preconditions` 条目数 ≥ 8、`evidence_date` 非空；它不校验这八项证据内容的真伪。…门禁不能替代人工判断。」 | ✓ |

与 `app.py::t0_evidence_gate()` 的真实校验范围逐条比对**一致**（只查两个 `true` + `preconditions` 长度 ≥ 8 + `evidence_date` 非空）✓ —— 表述不再强于实现。

### 2.7 R-7（低）三行失准 —— **部分闭合**（7b 已闭合；7a 再度失准 + 指针缺限定）

**7b（上游条款同步）—— 已闭合 ✓，且未重开上游修改 ✓**

| 落点 | 当前原文（`28c1309`） | 判定 |
|---|---|---|
| §4.3「上游条款同步」行（L292） | 「…**已由 PRO-15 于 `e3414ef` 完成同步**（本单不重开上游修改）」 | ✓ |
| §4.4 #5（L303） | 「**已闭合**：已由 PRO-15 于 `e3414ef` 上游同步（本单不重开）；本包物料不受影响（本包内已全部替换）。见 §9.5 #3」 | ✓ |
| §6.2 #5（L387） | 「~~是否授权修改…~~ **已闭合（无需裁定）**：PRO-15 已于 `e3414ef` 完成上述同步（本单不重开上游修改）。」（删除线保留历史记录） | ✓ |
| §9.5 #3（L521） | 「~~上游条款未同步…（超出本单授权）~~ **已闭合**：…已由 PRO-15 于 `e3414ef` 完成上游同步…」（保留原授权边界表述，未改写历史） | ✓ |

**未重开上游修改 —— 独立核验 ✓**：`git diff --stat 7127d64..28c1309`（PRO-16 本单改动）只触及 9 个文件，`docs/` 下**仅** `docs/08-gtm-channel-execution-pack.md`；`docs/05` / `docs/06` / `docs/07` **零改动** ✓。

**7a（本报告的覆盖动作引起）—— 再度失准（N-3）**

- 原文（`docs/08` §7 文件清单，L423）：`…（人工复审报告的**现行版本 = PRO-14 增量复审**（asset-review-editor.md，复审基线 725f5b9），由 Editor 维护；其判定依据正文见 §10）`
- 原文（`marketing/README.md` §1 目录树，L25）：`asset-review-editor.md（人工复审报告现行版本 = PRO-14 增量复审，基线 725f5b9，由 Editor 维护）`
- 问题：本报告已**就地覆盖同一文件**（唯一现行版本 = PRO-18 增量复审，基线 `28c1309`）→ 两行**同时再度失准**。这与 PRO-14 的 7a 是**同一个结构性缺陷**：报告版本行由「处置方」填写，而下一步的覆盖动作必然使其失效。两处附带的指针亦有偏差：§7 的「其判定依据正文见 §10」把 **PRO-16 的处置记录**称作报告的「判定依据正文」（§10 是对 PRO-14 判定的处置，判定正文在本报告自身）。
- **本轮如实记录**：该缺陷在当前体例下**无法由处置方一次性闭合**，只能「每轮复审随附更新」；建议同时把指针改为**指向报告文件本身 + 该轮的 issue/评论 id**，不再指向某一节。

**附：`e3414ef` 缺「分支实际提交」限定（N-3 的第 2 部分）**

- 4 处（L292 / L303 / L387 / L521）均写作「PRO-15 于 `e3414ef`」。独立核验：`e3414ef` 在分支 `PRO-1-pro-1` 上**不存在于 `git log`**（是 amend/重排后的**悬挂对象**，`git cat-file -t e3414ef` = commit、`git log` 不可达）；PRO-15 在分支上的**实际提交 = `0830dd7`**。
- 该映射**只被披露一次**：§10 的「哈希口径」注（L533）。下游按 `e3414ef` 检索会**找不到该提交**。
- 判定：不影响任何判据与物料，属**可追溯性**问题；建议 4 处补「（分支实际提交 `0830dd7`）」或不带哈希只写「PRO-15 上游条款同步」。

### 2.8 PRO-16 主动补充的三项（非 R 项）

| 项 | 判定 | 依据 |
|---|---|---|
| ① §6.1/§6.3 采纳 PRO-14 **O-5** 补登「作品页『曝光回读位』+『推荐理由回读位』」 | **已闭合** | §6.1（L375）新增独立一行并说明「与上一条是不同的兑现位，此前只登记了分享页渲染位」；§6.3（L395）PRO-7 移交行同步 ✓。**副作用**：该登记使 §2.1 的 N-1 成立（同一能力的两处表述在同一修订里互斥）。 |
| ② §9.2 L-5 / L-7 自评行按 PRO-14 改判结论更新 | **部分闭合** | L-7（L482）已由「判定不变（合规）」改写为「文档层已登记 + 物料层已降级」✓，与 §6.1 登记一致；**L-5（L480）** 的收口句「同类现在时断言已按 R-1 一并清理（脚本 A ③ / 脚本 B 正文 / §3 异议表 / `share_scripts.md` §2-§3 / `creator_outreach.md` §4）」**过宽**（未含 L44/L87，见 N-1）。 |
| ③ `marketing/README.md` 与 `docs/08` §7 的报告版本行同步 | **互相一致 ✓ / 均已失准** | 两行措辞一致（都写 PRO-14 / `725f5b9`）→「同步」达成；但本报告覆盖后两行均需改为 PRO-18 / `28c1309`（N-3）。 |

---

## 3. 需再改清单（原文 + 改法，退回 Marketing Strategist 处置）

> 全部为**就地修订**，不另建 V2 文件。N-1 涉及投放安全，优先级最高。
> 编号接续 PRO-14 的 R-1 ~ R-7，从 **N-1** 起，避免与历史编号混淆。

### N-1（高 · 投放安全）R-1 同类残余：`community_scripts.md` 2 处现在时断言未限定 + 2 处模板预设未实现位

- **原文**（`marketing/data/community_scripts.md`，基线 `28c1309`）
  - 脚本 A ①（L44）：`① 每条内容都写了一行推荐理由，我能看到它为什么被推；`
  - 脚本 C 正文（L87）：`而且每条都会给一行推荐理由——至少我们知道东西发出去之后发生了什么。`
  - 配套（同一未实现位被预设为取数来源）：
    - 占位符替换规则（L30）：`② 有推荐理由原文（从作品页复制，不得改写）`
    - 脚本 A 评论区模板（L53）：`【推荐理由】⟨从作品页原文复制，不得改写⟩`
- **问题**：`docs/08` §6.1（本轮由 PRO-16 新增）已把「**作品页**『推荐理由回读位』」登记为 **❌ 未实现**，并定性为「**社群脚本所引『回读』的兑现位**」；§9.2 L-5 的自定红线为「未实现，**不得对外暗示已可用**」。上列 4 处均以**现在时**断言该位可用或以其为取数来源，位于 **T-02（`pre_t0` 相位、`strict` 模式）扫描目标**内，且社群脚本以**创作者第一人称**发布，是**被照抄概率最高**的物料。
- **本包内部的反向依据（必须先消解，二选一，否则同一修订内继续自相矛盾）**：`docs/08` §2.3 第 2 条（L181）把 L44 列为「**可写**」的正面示例（依据：`server/recommend.py::_reason_for()` 已实现 `reason` 字段）。
- **改法（二选一；两处正文句与两处模板同批处理）**
  - **方案 A（把「可看/可读」一并降级为条件式 —— 与已通过的 6 处落点同款，推荐）**
    - L44 → `① 每条内容都写了一行推荐理由，我能看到它为什么被推（理由的展示位随功能开放）；`
    - L87 → `而且每条都会给一行推荐理由——至少我们知道东西发出去之后发生了什么（推荐理由回读位随功能开放）。`
    - L30 → `② 有推荐理由原文（从作品页复制，不得改写；该回读位开放后）`
    - L53 → `【推荐理由】⟨从作品页原文复制，不得改写（回读位开放后）⟩`
    - 同时把 `docs/08` §2.3 第 2 条（L181）的示例句改为**不依赖未实现位**的说法（例：`可写「每条内容都带一行推荐理由（理由的展示位随功能开放）」；不得写「能看到它被推给谁」…`），使 §2.3 与 §6.1 + §9.2 L-5 同口径。
  - **方案 B（取消条件式，改以「登记 + 截断」消解矛盾）**
    - 承认「`reason` 字段已实现」这一定性，把 §6.1 的该行**限定为「作品页回读位未实现」**（即：字段存在、位置不存在），并据此删去 L44 的「我能看到它为什么被推」改为 `① 每条内容都带一行推荐理由；`、L87 删去后半句 `——至少我们知道东西发出去之后发生了什么`；L30/L53 的取数来源由「作品页」改为「该内容当前的推荐理由原文」。
    - 注意：方案 B 会**弱化**已通过的 6 处落点（它们已按「展示位随功能开放」限定），故若选 B 需同时说明为何两处强度不同 —— **本报告建议方案 A**（改动最小、与现有口径一致、不撤销已通过项）。
- **不改则后果**：按 §9.2 L-5 红线，本包物料不得进入对外投放；且 §9.2 L-5 的收口句（「同类现在时断言已按 R-1 一并清理」）与 `docs/08` §6.1 的登记将继续**自相矛盾**。

### N-2（中 · 文档准确性）`marketing/README.md` 的「每一句话都来自文案包」未收窄

- **原文**（`marketing/README.md` §3 末，L76–L77）
  ```
  因此「T0 前误投量化承诺」与「T0 前误用 t0 臂文案」在装置层都不可能发生；
  页面与脚本里用户可见的每一句话都来自受扫描的文案包，不再是代码里的硬编码。
  ```
- **问题**：与 PRO-14 R-4 判为不成立的「**所有**用户可见文案」「代码内不再保留对外文案字面量」是同一命题；`app.py` 现状（PRO-14 R-4 已列明、本轮复核一致）仍保留：页脚 `页面版本/变体/候选`（L720-721、L749）、渲染兜底默认值 `联系方式` / `提交申请` / `内测名额申请` / `分享`（L712/716/732/758）、`/api/*` 中文错误文案（L884–1012）。`README` 是物料索引与上线整合的第一入口，下游据此会误以为「`app.py` 已抽空」。
- **改法（沿用 R-4 的收窄口径，仅改这一句）**
  ```
  因此「T0 前误投量化承诺」与「T0 前误用 t0 臂文案」在装置层都不可能发生；
  页面与脚本里的**对外投放文案**已全部来自受扫描的文案包，不再硬编码在代码里；
  仍留在 `app.py` 的页脚版本/变体/候选标签（运维核对用）、渲染兜底默认值与 `/api/*` 错误文案不属投放文案（见 `docs/08` §1.3 / §9.3 D-4 的口径收窄）。
  ```
- **说明**：本项**不是投放违规**（残留字符串无禁用表述），属**文档口径与登记理由的准确性**问题。

### N-3（低 · 记录可追溯性）报告版本行与 `e3414ef` 指针

- **原文 → 改法**
  - `docs/08` §7 文件清单（L423）：`…现行版本 = PRO-14 增量复审（asset-review-editor.md，复审基线 725f5b9），由 Editor 维护；其判定依据正文见 §10`
    → `…现行版本 = **PRO-18 增量复审**（`asset-review-editor.md`，**复审基线 `28c1309`**），由 Editor 维护；本文件是唯一现行版本，每轮修订即覆盖（本轮判定正文见该报告自身；本轮 issue 评论为结论摘要）`
  - `marketing/README.md` §1 目录树（L25）：`asset-review-editor.md（人工复审报告现行版本 = PRO-14 增量复审，基线 725f5b9，由 Editor 维护）`
    → `asset-review-editor.md（人工复审报告现行版本 = PRO-18 增量复审，基线 28c1309，由 Editor 维护）`
  - `docs/08` §10 首行（L531）：「判定依据：`marketing/reports/asset-review-editor.md`（**PRO-14 增量复审**，…复审基线 `725f5b9`）」→ 该行是 PRO-16 轮的历史记录，**可保留**；但若下游按「现行版本」检索会读到已失效的快照，建议在该行末补 `（现行版本已由 PRO-18 按 `28c1309` 覆盖）`。
  - 4 处 `e3414ef`（`docs/08` L292 / L303 / L387 / L521）→ 补 `（分支实际提交 `0830dd7`）`，或不带哈希只写「PRO-15 上游条款同步（commit `0830dd7`）」，与 §10 的哈希口径注（L533）一致。
- **结构性建议（本项的重点）**：报告版本行与 §10 首行的「现行版本」指针**每轮必然失效**（PRO-14 的 7a 与本轮 N-3 是同一缺陷的两次复现）。建议改为**不绑定轮次的表述**：`asset-review-editor.md（Editor 维护；就地覆盖，唯一现行版本；版本与基线以文件头「复审基线」行为准）`，并把「判定依据」的指向改为**本文件 + 该轮 issue 评论 id**，而不是文档内的某一节。
- **关联（本轮并发提交，不在本单取值范围）**：`PRO-17` 的 `1273783` / `36d398e` 已把 4 处 `e3414ef` 指针统一改写并把 §4.4 #5 / §9.5 #3 收口 —— 方向与本项一致；但其提交**基于 `28c1309` 之后**，且**未审阅**，故本项仍以「按新 commit 复验」收口（`docs/08` §9.5 #1 现行口径）。

---

## 4. 观察项（不阻断，不计入需再改；供后续修订采用）

- **O-1（新）`docs/08` §0.1 相位表仍把「推荐理由可回读」列为 `pre_t0` 允许表述**：§0.1（L18）`pre_t0` 行「允许对外说」列含 `机制类表述：规则先公开、推荐理由可回读、邀请制内测`，未附条件式，与 §6.1 的「回读位 ❌ 未实现」登记 + §9.2 L-5 红线口径不一致。建议把该项写作 `推荐理由**回读位**（随功能开放后）` 或在同行加注「（见 §6.1 兑现位登记）」。**与 N-1 同源**，一并处置最省事。
- **O-2（承接 PRO-14 O-1）** 第二批命名测试的归因边界（名称与 Slogan 同批变化，无法分离）—— `docs/08` §10 已如实登记「未在本单处理」，本轮**仍未处理**，移交下一轮。
- **O-3（承接 PRO-14 O-3）** `HB-17` 的 `pattern` 不含 V3 改后的新措辞「一个入口管理多平台发布」—— 该句合规性依赖「内测目标 + 支持清单以开放时公开说明为准」的意向式框架，而非词表保护。建议在 `lexicon.json` `notes` 或 §9.3 加一行说明。本轮**未处理**。
- **O-4（承接 PRO-14 O-4）** `t0_evidence.template.json` 的 `#1`/`#8` `name` 仍是「见 `docs/05` §0.3 #1/#8」占位（`#2`–`#7` 已写实）→ 建议八项全部写实，降低填写人二次查阅与漏填风险。本轮**未处理**。
- **O-5（新）`docs/08` 文档头「修订记录」（L8）只记到 PRO-13**：`修订记录：PRO-10 首版（commit 9b032e5）→ PRO-13 终审返修（…；处置明细见 §9）`，未记 PRO-15（`0830dd7`）/ PRO-14（`7127d64`）/ PRO-16（`28c1309`）。建议补一行链式修订记录（与本包「文档清单与版本必须自洽」的纪律一致）。
- **O-6（新）`community_scripts.md` §1 硬门 1 与占位规则的取数来源依赖未实现位**：已在 N-1 列出，此处另记其**判据层**影响——「每个话题第 7 天必须含一次『真实曝光数 + 推荐理由』的展示」（`docs/08` §2.2 排期纪律 ③）在「曝光回读位/理由回读位」实现前**无法执行**。建议在 §2.2 或 §6.1 明确「该纪律的启动前置 = §6.1 两个回读位实现」，避免排期先行。

---

## 5. 独立复跑输出（原文，可复核）

工作目录：`28c1309` 的**独立只读副本**（`git archive 28c1309` 解至工作区外；分支 `PRO-1-pro-1`）。使用独立副本的原因见「复审期间的并发提交」。

**① 单测**

```
$ python3 -m unittest discover -s marketing/tests -t .
Ran 42 tests in 19.063s

OK
[参考比对] dim=49(version=8) ref_mask=[2] auto_mask=6 一致=False | 样本=https://lcnn.example.com/?utm_source=douyin&utm_…
[参考比对] dim=69(version=13) ref_mask=[2] auto_mask=2 一致=True | 样本=https://lcnn.example.com/?utm_source=wechat&utm_…
exit=0
```

> 版本 8 的掩码不一致为**编码器自选项**（ISO/IEC 18004 允许自选合法掩码），已在 `docs/08` §1.1/§8 #6 如实限定；该用例不 fail，属预期。

**② 合规审查器**

```
$ python3 marketing/scripts/compliance_lint.py
[compliance-lint] hard_ban=18 t0_gated=6 扫描目标=9
[compliance-lint] 阻断级违规：0，告警级：0
[compliance-lint] 报告：marketing/reports/asset-compliance-report.md
exit=0
```

> 复跑会就地重写 `asset-compliance-report.md`（仅时间戳变化）。为把改动范围收敛到 1 个文件，该文件已还原为 `HEAD` 版本；上表即复跑的实际输出（在独立副本中产生）。

**③ T0 门禁实测**

```
$ python3 marketing/landing/app.py --phase t0 --port 0
[compliance] phase=t0 缺少有效的 T0 前置证据，拒绝启动（exit 3）：
  - 未提供 --t0-evidence（T0 证据文件）
  说明：T0 = MVP 可灰度日（PRO-7 交付 + §0.3 八项前置全绿）。
  模板：marketing/data/t0_evidence.template.json
exit=3

$ python3 marketing/landing/app.py --phase t0 --port 0 --t0-evidence marketing/data/t0_evidence.template.json
[compliance] phase=t0 缺少有效的 T0 前置证据，拒绝启动（exit 3）：
  - 证据字段 pro7_delivered 必须为 true（当前：False）
  - 证据字段 preconditions_all_green 必须为 true（当前：False）
  - 证据字段 evidence_date 缺失（须写明证据采集日）
exit=3
```

> 与 PRO-16 §10.1 的回执**一致**，且独立复现；门禁能力边界见 §2.6（R-6）。

**④ R-5 复核口径复跑**

```
$ grep -rn "曝光下限\|产品里的一条规则\|写进产品的曝光规则" marketing/ docs/08-gtm-channel-execution-pack.md
marketing/reports/asset-review-editor.md:76,188      # 审阅报告引用行
marketing/reports/asset-compliance-report.md:33      # 原始命中明细行
marketing/tests/test_compliance.py:47,186            # 单测注释/样例
marketing/tests/__pycache__/test_compliance…pyc      # binary file matches
marketing/compliance/lexicon.json:37,41              # HB-18 pattern / TG-02 reason
docs/08-gtm-channel-execution-pack.md:265,457,512,515 # 处置记录行
$ grep -rn "…" marketing/data/ marketing/landing/copy_pack.json
（无输出 —— 投放文案零命中）
```

**⑤ 本单改动范围与基线核验**

```
$ git diff --stat 7127d64..28c1309        # PRO-16 本单改动（9 个文件）
 docs/08-gtm-channel-execution-pack.md        | 85 +++++--
 marketing/README.md                          |  2 +-
 marketing/compliance/scan_manifest.json      |  2 +-
 marketing/data/community_scripts.md          | 25 +--
 marketing/data/creator_outreach.md           |  6 +-
 marketing/data/share_scripts.md              |  9 +-
 marketing/data/t0_evidence.template.json     |  2 +-
 marketing/landing/copy_pack.json             |  4 +-
 marketing/reports/asset-compliance-report.md |  2 +-

$ git diff --name-only 7127d64..28c1309 | grep '^docs/'
docs/08-gtm-channel-execution-pack.md      # docs/05、docs/06、docs/07 零改动 ✓

$ git cat-file -t e3414ef        → commit（悬挂对象，不在分支 log 上）
$ git log --oneline -1 0830dd7   → fix(PRO-15): 上游条款同步收紧裁定口径（就地修订，不另建版本）
```

> 本报告全部判定的取值依据是**物料文件在 `28c1309` 时的内容**；本单未修改任何物料（唯一改动 = 本报告文件 `marketing/reports/asset-review-editor.md`，就地覆盖）。

---

## 6. 移交

| 接收方 | 内容 |
|---|---|
| **PRO-10 / Marketing Strategist** | §3 的 3 项「需再改」（原文 + 改法已给出）；**N-1 优先**（投放安全，且需在方案 A / B 之间**显式消解** §2.3 与 §6.1+§9.2 L-5 的口径矛盾）。N-2/N-3 为文档准确性，可与 N-1 同批。全部就地修订，不另建 V2 文件。修订完成后**须再按新 commit 重跑一次增量复审**（`docs/08` §9.5 #1 现行口径）。 |
| **PRO-7（Prototype Engineer）** | 兑现位：作品页**曝光回读位** + **推荐理由回读位**（`docs/08` §6.1 已由 PRO-16 补登）—— 它们是 `community_scripts.md` 脚本 A/B/C 所引「回读」与承诺卡 §1 第 4 条的兑现位，仍是全部对外物料解锁 T0 的前置。 |
| **Director of Go-to-Market** | 上线时**不得以 `--phase t0` 启动**（会被 T0 证据门禁拦住）；且注意 R-6：门禁只验证据**信封**，证据真实性由留档与人工负责。 |
| **board / 下一轮复审** | `28c1309` 之后分支新增的 **PRO-17 提交 `1273783` / `36d398e`**（仅 `docs/08` 记录行，未触及物料）**未被本报告审阅**；请按「物料/文档一经修订即按新 commit 重跑」的现行口径另行纳入。 |

---

## 7. 诚实限定（不得省略）

1. 本报告是**人工通读判定**，未重写任何文案、未改 code、未改 `docs/05`/`docs/06`/`docs/07`、未改 `docs/08` 正文、未重跑物料生成脚本、未新增任何数字或承诺等级。所有「需再改」一律以「原文 + 改法」提出，由 Marketing Strategist 处置。
2. 本轮判定为 **不通过**，但**含 0 项阻断级**（机器 0/0；无数字、无承诺等级）。挡在投放前面的**只有 N-1 一项**（同类现在时断言 + 未实现位预设），N-2/N-3 属文档准确性。
3. **N-1 存在一处反向依据**，本报告已如实写出（`docs/08` §2.3 把 L44 列为「可写」示例）。因此 N-1 的正确定性是「**同一修订内两处口径互斥，必须显式消解**」，而不是「文案凭空越界」。若处置方选择以「`reason` 字段已实现」为准，则须同时修订 `docs/08` §6.1 与 §9.2 L-5 的措辞（方案 B），并说明为何与已按条件式限定 6 处落点的强度不同。
4. **机器自检不等于合规**：本轮独立复跑得到「42 项单测 OK + 阻断级 0 / 告警级 0」，而本报告发现的 3 项问题**全部**不在词表与装置的可判定范围内。
5. 本报告的复审基线为 commit **`28c1309`**；若物料或处置记录在此之后再次修订（含已发生的 PRO-17 `1273783`/`36d398e`），须按**新 commit**重跑本复审（本报告就地覆盖，不留历史版本）。
6. **取值防护**：因 `docs/08` 在同一工作区被并发改写（PRO-17），本报告的全部逐字核对与机器复跑均在 `28c1309` 的**独立只读副本**中进行；副本中未做任何写入型探针（本轮**未**改写任何物料文件做反证，故不涉及还原问题）。
7. 本次复审的**全部文件改动 = `marketing/reports/asset-review-editor.md`（本文件，就地覆盖）**；`compliance_lint.py` 复跑产生的 `asset-compliance-report.md` 时间戳变化已还原，以把改动范围收敛到 1 个文件。
8. 本报告未覆盖的对象：`docs/05`/`docs/06`/`docs/07` 上游条款（超出授权，已由 PRO-15 / `0830dd7` 完成）、`marketing/out/` 生成物、商标与域名初筛、对外部署、`28c1309` 之后的分支提交。

---

*报告结束 · Editor（PM Toolkit）· PRO-18 增量人工复审（唯一现行版本，复审基线 commit `28c1309`）*
