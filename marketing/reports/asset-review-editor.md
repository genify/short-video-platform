# 渠道落地包物料 · 增量人工复审报告（Editor / PRO-14）

> 交付方：Editor（PM Toolkit）
> 审查对象：PRO-13 返修后的渠道执行包物料（分支 `PRO-1-pro-1`）
> **复审基线：commit `725f5b9`**（含 `5e34952`）；旧基线 `1fead09`（PRO-12 报告）/ `9b032e5`（PRO-10 首版）
> 审查相位：**`pre_t0`**（T0 = MVP 可灰度日 = PRO-7 交付；复核时点 PRO-7 看板状态仍为 `blocked`）
> 本报告为**就地覆盖版本**（不留 V1/V2/V3）：本文件是唯一现行版本，修订即覆盖。
> 性质声明：本报告是**增量人工通读判定**，不重写文案、不改 code、不改 `docs/05`/`docs/06`/`docs/07`、不重跑物料生成脚本。判定为「需再改」的条目一律以「原文 + 改法」退回 Marketing Strategist 处置。

### 基线说明（为何是 `725f5b9` 而不是 `5e34952`）

本单标题基线的 `5e34952` 之后，PRO-13 的受派人又提交了 `725f5b9`（补完 §4-② 机制措辞 3 处残留 + 新增 §9.4 口径对照表 + 就地重跑合规报告）。该请求已在本单评论中提出，本报告**采纳**：取值基线取 `725f5b9`，同时把 `5e34952..725f5b9` 的 5 个文件改动纳入复审。该区间仅 5 个文件、共 `+29 / -6` 行，且全部为就地修订；已逐行核对（见 §2.5）。

**复审期间的并发提交（已记录，不影响本单取值）**：本单复审进行中，同一分支又收到 **`e3414ef`（PRO-15「上游条款同步」）**，仅改 `docs/05-gtm-plan.md`（+52/−10 行）与 `docs/06-user-needs-map-and-assumption-plan.md`（+13/−10 行）——**均在本单「明确不做」的范围内，未被本报告审阅**。核对其措辞与 `copy_pack.json` `rules[0]`、`docs/08` §4.3 同口径，未见新的机制措辞分叉；但它使 `docs/08` 中三条「上游条款未同步」的表述失准（见 R-7）。

---

## 0. 结论摘要

### 0.1 复审判定的总量

| 复审块 | 条目数 | 已闭合 | 部分闭合（需再改） | 未闭合 | 需再改合计 |
|---|---:|---:|---:|---:|---:|
| A. `docs/08` §9.1 的 18 处「需改」（PRO-12 §2） | 18 | 16 | 2（#2、#9） | 0 | **2** |
| B. `docs/08` §9.2 的 8 项机器漏检（PRO-12 §3） | 8 | 5 | 2（L-5、L-6） | 1（L-7） | **3** |
| C. `docs/08` §9.3 的 4 项装置盲区（PRO-12 §5） | 4 | 3 | 1（D-4） | 0 | **1** |
| D. 新增可读物（PRO-12 未审） | 3 | 2 | 1（§9.4 复核口径） | 0 | **1** |

**合并后共 7 项「需再改」**（其中 2 项为同一条未兑现暗示的两处实例，故去重后为 6 个独立问题）：
R-1 未兑现暗示（曝光「可回读」，2 处实例）· R-2 个人事实占位未闭合 · R-3「不可删」第③类无对应句 · R-4 D-4 口径「所有」过头 · R-5 §9.4 复核枚举不完整 · R-6 门禁能力表述过强 · R-7 `docs/08` 三行失准（本报告覆盖 1 行 + PRO-15 完成后 3 处上游同步表述）。

**投放放行结论：不通过（需再改后复验）。** 0 项**阻断级**（无数字承诺、无禁止表述）；但存在**未兑现暗示**（R-1）——按本包自己的判据（`docs/08` §9.2 L-5 括注「未实现，不得对外暗示已可用」），该项在清除前不得进入对外投放。

### 0.2 判据 ② 的直接回答（返修是否引入新的隐性承诺 / 未兑现暗示 / 跨文件分叉）

| 问题 | 回答 | 证据 |
|---|---|---|
| 是否引入**新的隐性承诺**？ | **否（0 项）** | 逐字通读 `5e34952..725f5b9` 的 5 个文件；新增文字全部为处置记录、纪律句、口径对照表与降级措辞，无一句新增承诺 |
| 是否引入**新的未兑现暗示**？ | **否（0 项新增）**；但**同类遗留未被清理** | R-1 的两处实例（`community_scripts.md` 脚本 A ③ / 脚本 B 正文）是 PRO-10 首版即存在、PRO-12 与 PRO-13 **双双漏检**的「曝光可回读」现在时断言（`git log -L` 证实两行自 `9b032e5` 起未改动） |
| 是否引入**新的跨文件口径分叉**？ | **是，1 处（物料层）+ 1 处（文档层）** | ① 物料层：返修把 `copy_pack.json` share V2 `pre_t0` 的「现在就能看到它」降级为「理由的展示位**随功能开放**」，但**未同步** `creator_outreach.md` §4「而且**你会看到**每条内容的推荐理由」与 `community_scripts.md` §3 异议表「理由在作品页」→ 同一能力在三份物料里出现**条件式 vs 无条件式**两种强度（返修前两者都是无条件式，故这是**本轮新产生的强度差**）；② 文档层：`docs/08` §1.3/§9.3 的「**所有**用户可见文案统一取自 `ui.*`」与实现不符（R-4） |
| 机制措辞统一（`docs/08` §9.4 ②） | **已达成**，无分叉 | `grep -rn "产品里的一条硬约束"` → 恰好命中 4 份投放物料（`share_scripts.md` §1 / `creator_outreach.md` §2、§4 / `community_scripts.md` 脚本 C）+ `copy_pack.json` V1 `pre_t0` `sub`，**逐字一致** |
| 「T0 前禁什么」口径统一（`docs/08` §9.4 ①） | **已达成** | 5 份文件逐条比对；仅 `copy_pack.json` `rules[0]` 被 §9.4 摘引时省略了尾段「、也无无数字的结果暗示」与框架句（见 §2.6 注） |

### 0.3 机器自检与人工通读的差距（本单存在的理由，实测）

| 维度 | 机器自检（本轮独立复跑） | 人工通读（本报告）补充 |
|---|---|---|
| 禁用表述 | `hard_ban=18 / t0_gated=6 / 9 目标`，**阻断级 0 / 告警级 0**（exit 0） | 语义级「是否构成未兑现暗示」仍不在词表覆盖内 → 本单仍发现 **2 处**（R-1） |
| 装置是否兑现文案 | 只校验「文案是否含禁用词」 | 文案 ↔ 实现逐条对照 → 发现 **1 类未兑现暗示（曝光可回读）** 与 **1 项口径过头（D-4）** |
| 处置记录自身的准确性 | 不检查 | `docs/08` §9.3/§9.4/§8.9 的 3 处自评与实际不符 → R-4 / R-5 / R-6 |

**机器自检不等于合规**：本轮 42 项单测全绿、合规器 0/0，但上述 6 个独立问题**没有一项**是机器可以发现的。人工通读不可省略。

---

## 1. 方法与判据（沿用 PRO-12，不放宽、不收紧）

1. 判据来源同 PRO-12 §1.1：`docs/05` §0.3/§3.5/§4.1/§5.4、`lexicon.json`（`hard_ban` 18 + `t0_gated` 6 + `required_tokens` 4）、`docs/08` §4.3 收紧裁定（T0 前禁一切数字**与一切结果承诺（含无数字）**，裁定确认前按更严一版执行）。
2. 三条外部边界：不做站内关系链表述、不比算法精度、不作对手不透明指控、不变现承诺。
3. 逐字通读范围：`marketing/data/*.md`（5 份）、`marketing/landing/copy_pack.json`、`marketing/landing/app.py`、`marketing/landing/compliance.py`、`marketing/scripts/compliance_lint.py`、`marketing/compliance/*.json`、`marketing/data/t0_evidence.template.json`、`marketing/README.md`、`docs/08` 全文、`docs/05` §0.3/§4.1/§4.3/§5.4、`docs/04`（范围）、`docs/07` §5.2/§6.2。
4. **新增动作（本单判据 ③ 要求）**：对 4 项装置盲区**独立复跑**，并另做**反证式探针**（注入/抽出后看是否命中），不复用 PRO-13 的自测结论。
5. 本单不做：商标/域名初筛；改 `docs/05`/`docs/06`/`docs/07` 上游条款；对外部署；重跑物料生成脚本；重写任何文案。

---

## 2. 逐条核对（不接受笼统结论）

### 2.1 A 块 —— `docs/08` §9.1 的 18 处「需改」逐条结论

| # | PRO-12 出处 | 复审结论 | 实测证据（当前文本） |
|---|---|---|---|
| 1 | §2.1-A（L-2）竞品对照句 | **已闭合** | `share_scripts.md` §2 已是自我陈述「这一点我能自己核对」；`lexicon.json` HB-16 pattern 含「比我用过的」，能命中原句 |
| 2 | §2.1-B「哪些句子不可删」 | **部分闭合 → 需再改（R-3）** | §0 第 2 条已列三类 + 正文 `〈不可删〉` 标记（§1 三处、§2 一处、§3 一处）✓；**但第③类「社群版的身份披露句『我是创作者，不是官方号』」在 §2 正文中不存在**（§2 仅 3 行 + 链接，全文无该句、也无对应标记）→ 规则指向一句不存在的话 |
| 3 | §2.1-C 承诺等级与收紧裁定不一致 | **已闭合** | 文件头已改为「不得出现任何数字与任何结果承诺（含无数字表述，如「有人看」「被看见」）；词例仅作提示，不作为判断边界」，与 `creator_outreach.md` §3 同款 |
| 4 | §2.1-D「我直接发你视频」 | **已闭合** | §3 末句已改为「我把视频发在这里的评论区」 |
| 5 | §2.2 建议 1「曝光下限」 | **已闭合** | §4 已改为「把首发的曝光规则写成产品里的一条硬约束（具体数值规则开放时一并公开）」；HB-18 已登记且能命中旧说法 |
| 6 | §2.2 建议 2 内部判据 | **已闭合** | §5 表头「本表为**内部记录口径，不得对外引用**」+ 行内「（内部判据，不得对外引用）」 |
| 7 | §2.3-A 占位符写法不一致 | **已闭合** | 三处数字占位统一为「…，未拿到数据前不得发布」完整形式（脚本 A 1 处、B 3 处、C 1 处）；§1 已补「占位符替换规则（缺一不发）」三者齐备条款 |
| 8 | §2.3-B（L-3）能力句超出实现 | **已闭合** | 脚本 A ① 已改为「每条内容都写了一行推荐理由，我能看到它为什么被推」；「被推给谁」已删；HB-17 pattern 含「被推给谁\|推给谁」 |
| 9 | §2.3-C（L-6）创作者个人事实写死 | **部分闭合 → 需再改（R-2）** | 主示例值已改占位（`〈真实年限〉`/`〈真实店龄〉`/`〈真实条数〉`）+ §2 使用约束 ✓；**但脚本 C 仍写死 3 处**：「我把**三条**的真实曝光数…」「【实证】**三条** 24h 曝光」「⟨原文 **×3**⟩」→ 与同脚本的 `〈真实条数〉` 占位自相矛盾 |
| 10 | §2.3-E 内部 KPI 出现在对外脚本 | **已闭合** | §4 表头「**内部口径，不得对外引用**」 |
| 11 | §2.3-F「邀请码」「按顺序给」 | **已闭合** | 答句改「我按留资顺序把邀请名额发给你」；§1 补「名额发放口径」；`creator_outreach.md` §5 同步「名额发放顺序口径」，并写明不使用「邀请码」 |
| 12 | §2.4-A 检查项漏「无数字结果承诺」 | **已闭合** | §4 检查 1 =「不得含任何数字，也不得含任何结果承诺（含无数字表述）——覆盖第一批卡片、第二批候选名与 Slogan」 |
| 13 | §2.4-B（L-8）候选无渲染路径 | **已闭合（装置侧，已独立复跑）** | `app.py` 有 `Application.naming_ids()` / `naming_candidate()`（按 `self.phase` 取臂）+ `render_recruit(..., naming=…)` 渲染「候选名 + 当前臂 Slogan」；`naming` 写入三处：`page_view`（L918）、`cta_click`（内联脚本 L595）、`/api/lead`（L604） |
| 14 | §2.4-C 子批 1 排期冲突 | **已闭合** | `naming_test_brief.md` §2「子批 1 开跑前 A 方向须完成商标与域名初筛并留档；未完成则顺延至子批 3」；§4 检查 6 同步；`copy_pack.json` 方向 A `risk` 同步 |
| 15 | §2.5-A（L-1）命名候选 slogan | **已闭合** | 9 个候选全部按臂存放；A 方向 `pre_t0` 为机制类替代句、结果承诺型句移入 `t0` 臂；B2 `路线都写在明处`；B3 `pre_t0` slogan = `null`（仅展示名称） |
| 16 | §2.5-B（L-4）V3 宣传未上线能力 | **已闭合** | V3 两臂 h1 均改为「同一份素材，一个入口管理多平台发布（内测逐步开放）」；bullets 改为「内测目标…」+「支持清单以开放时的公开说明为准」；`footer_note` 明确「不构成对已可用范围的承诺」 |
| 17 | §2.5-C（L-5）分享 V2「现在就能看到它」 | **已闭合（本项）/ 同类未清（见 B 块 L-5）** | `copy_pack.json` share V2 `pre_t0` `sub` 已降级为「理由的展示位随功能开放，开放后可在此页面回读」，`bullets` 加「（随功能开放）」；`known_gap` 如实登记；`docs/08` §6.1 新增「分享页『推荐理由』渲染位 ❌ 未实现」一行 |
| 18 | §2.5-D / §2.6-A/C/D / §2.7-A（5 个子项） | **已闭合（5/5）** | ① `rules[2]` 已重写为「跨臂 vs 跨变体」两轴；② §1.3 表已更正 V3 行并注明取值节点 `assets.recruit.variants.<V>.<arm>.h1`（V1/V2/V3 六格取值与 `copy_pack.json` 逐一相符）；③ §4.2 HB-08 行括注已更正；④ §4.3 影响已改清单式（含上游 05/06 与 A1–A3、B2/B3）；⑤ 承诺卡结语已改为「这三条数字，以及第 4、5 条两项非数字承诺，是我们对外承诺的全部内容」 |

**A 块小计：已闭合 16 / 部分闭合 2 / 未闭合 0。**

### 2.2 B 块 —— `docs/08` §9.2 的 8 项机器漏检逐条结论

| # | 项 | 复审结论 | 实测证据 |
|---|---|---|---|
| L-1 | 命名候选结果承诺 slogan | **已闭合** | 反证探针：把「发出来，就被看见」注入某候选 `pre_t0.slogan` → **命中 TG-06**，`json_path = naming_candidates.directions.A.candidates[0].pre_t0.slogan`；把同一句放进该候选 `t0.slogan` → `pre_t0` 相位**不命中**（分臂隔离正确）；放进 `ui.share.gate_text` 也命中（UI 受检） |
| L-2 | 竞品对照句 | **已闭合** | 物料侧已改自我陈述；装置侧 HB-16 已登记（pattern 含「比我用过的\|比别的平台\|比其他平台…」） |
| L-3 | 能力句超出实现 | **已闭合** | 脚本 A ① 已收回到 `_reason_for()` 可实现范围；HB-17 覆盖「被推给谁」 |
| L-4 | 多平台分发类未上线能力 | **已闭合** | V3 两臂改意向式；HB-17 覆盖旧措辞。**注（O-3）**：HB-17 的 pattern 不含「多平台发布」这一**新措辞**，即新句的合规性靠「意向式框架」而非词表保护——建议在词表 notes 说明，避免下次改文案时误判 |
| L-5 | 分享页「现在就能看到」无兑现位 | **部分闭合 → 需再改（R-1）** | `copy_pack.json` 侧已降级 ✓；但**同类「已可用」暗示未一并清理**：`community_scripts.md` 脚本 A ③「曝光数字在作品页**能**自己回读，不用问客服」、脚本 B「作品页**可以**直接回读，不需要问后台」仍为现在时断言，而 `docs/08` §9.2 L-5 自己写明「未实现，**不得对外暗示已可用**」 |
| L-6 | 创作者个人事实写死 | **部分闭合 → 需再改（R-2）** | 同上（脚本 C 三处「三条 / ×3」） |
| L-7 | 「可回读」依赖未实现的曝光埋点 | **未闭合（未如实暴露）→ 需再改（R-1）** | 文档层已如实登记（`docs/08` §6.1 的 `exposure` 埋点「❌ 未实现（最高优先）」+ 新增「分享页『推荐理由』渲染位 ❌ 未实现」；§8 诚实限定保留）✓；**但物料层仍以现在时断言该位可用**（脚本 A ③ / 脚本 B），即「被文案改写掩盖」未发生、但「未兑现暗示仍在投放物料里」这一事实**未被 §9.2 该行披露**（该行记「判定不变（合规）」）。结论：**L-7 应改判为「部分闭合：文档层已登记，物料层需再改」** |
| L-8 | 第二批命名测试不可开跑 | **已闭合（装置侧，已独立复跑）** | 见 A-13；另 `copy_pack.json` `known_gap` 与 `naming_test_brief.md` §1 前置条件 1 均已写明「未部署前不得开跑第二批」 |

**B 块小计：已闭合 5 / 部分闭合 2 / 未闭合 1。**

### 2.3 C 块 —— `docs/08` §9.3 的 4 项装置盲区（**独立复跑，不引用本单结论**）

| # | 盲区 | 复审结论 | 我的独立复跑输出 |
|---|---|---|---|
| D-1 | `naming_candidates` 是否进入扫描 | **已闭合** | `compliance.SCAN_ROOTS == ('assets', 'naming_candidates', 'ui')`。反证：向候选 `pre_t0.slogan` 注入「发出来，就被看见」→ 命中 `TG-06`（路径 `naming_candidates.…pre_t0.slogan`）；注入 `t0.slogan` → `pre_t0` 不命中 |
| D-2 | `share_landing` 是否登记为必含项 scope 并被执行 | **已闭合** | `scan_manifest.required_token_scopes == ['recruit_form','share_landing']`；`lexicon.required_tokens` 含 `RT-04 scope=share_landing`；**反证**：把 `assets.share` 里所有 `先看/不用注册/无需注册/直连可播放` 抽掉后，`compliance_lint.check_required_tokens()` 与 `compliance.validate_copy_pack()` **均报** `RT-04 required_missing`（`path=copy_pack.json[assets.share]`）；同理抽掉 consent 文案 → 报 RT-01/02/03。反证完毕已还原文件（`git status` 干净） |
| D-3 | `--phase t0` 是否被 T0 证据门禁拦住 | **已闭合** | `python3 marketing/landing/app.py --phase t0 --port 0` → 打印「未提供 --t0-evidence」→ **EXIT=3**；`--t0-evidence marketing/data/t0_evidence.template.json`（默认全 false）→ 打印「pro7_delivered / preconditions_all_green 必须为 true、evidence_date 缺失」→ **EXIT=3**；`--phase pre_t0` → 正常启动（被 timeout 杀掉，无门禁误拦）。另见 R-6：门禁只校验**证据信封**，不校验证据内容真伪 |
| D-4 | `app.py` 是否还有用户可见中文硬编码 | **部分闭合 → 需再改（R-4）** | **机制已闭合**：`ui.*` 12 个必需键存在、纳入同一扫描、缺失即 exit 4（`check_required_ui_keys` + `REQUIRED_UI_KEYS`）。**但「全部抽出」不成立**：`app.py` 仍有用户可见中文硬编码——页脚 `页面版本={phase} · 变体={variant}`、`候选=`（L720/721）、`视频=… · 页面版本=… · 变体=…`（L749）；渲染兜底默认值 `联系方式`（L712）、`提交申请`（L716）、页面标题 `内测名额申请`（L732）/`分享`（L758）；`/api/*` 错误文案（`未知变体`、`缺少 asset`、`服务内部错误…` 等，L884–1012）。且 `scan_manifest.device_files` 对 `app.py` 的登记理由写「代码内不再保留对外文案字面量」——**与事实不符** |

**C 块小计：已闭合 3 / 部分闭合 1。**

### 2.4 D 块 —— 新增可读物（PRO-12 未审）

| 审查对象 | 复审结论 | 依据 |
|---|---|---|
| `marketing/data/t0_evidence.template.json` | **通过（附 R-6、O-4）** | 默认全 `false`/`pending`，刻意 fail-closed ✓；八项前置逐条列出 ✓（#7 标 `ready` 并注「本团队已交付」✓）；`how_to_use` 明确「不要把模板本身当证据」✓。**待改**：门禁只验信封（R-6）；#1/#8 的 `name` 仍是「见 docs/05 §0.3 #1/#8」占位，未写实（O-4） |
| `copy_pack.json` 的 `ui.*` 与 `naming_candidates` 按臂结构 | **通过（附 R-4 的文档口径问题）** | `ui.*` 12 键齐备、`purpose` 与 `scan_scope` 说明清楚；`naming_candidates` 每个候选 `pre_t0` / `t0` 双节点、`note` 说明处置理由；`phase_gate` 与 `rules[3]` 一致；A 方向 `risk` 已含子批顺延前置条件。渲染侧与文案侧一致（`naming_candidate()` 按 `self.phase` 取臂） |
| `docs/08` §9.4 / §9.5 的诚实性 | **§9.5 基本诚实，§9.4 复核口径不准 → 需再改（R-5）** | §9.5 七项残留**未回避**：人工通读未做（且明确移交本单）✓、词表正则盲区（举了 `creator_outreach` §1 问句这一真实例子）✓、上游未同步 ✓、兑现位缺口 ✓、业务判据全不可计算 ✓、#18 为合并行 ✓、未做项 ✓。**但两条残留未登记**：(a) 未兑现暗示（曝光「可回读」，脚本 A/B）；(b) 个人事实残余（脚本 C 的「三条 / ×3」）→ 亦即 §9.5 的「诚实」在**自己漏检的两类**上不完整。§9.4 的复核枚举不完整见 R-5 |

### 2.5 复审范围内我另外主动做的核对（未被要求，但属通读范围）

1. **`725f5b9` 的 5 个改动逐行核对**：机制措辞 3 处替换位置正确（`share_scripts.md` §1 / `creator_outreach.md` §2 / `community_scripts.md` 脚本 C），且与 `copy_pack.json` V1 `pre_t0` `sub` **逐字同口径**；`docs/08` §9.4 新增、原 §9.4 顺延为 §9.5 的编号连续、无交叉引用断裂；`asset-compliance-report.md` 仅时间戳变化（数值一致：`hard_ban 18 / t0_gated 6 / 阻断 0 / 告警 0`）。
2. **`marketing/README.md` 是否因返修而失准**：核对后**未失准**——已同步为「18 hard_ban + 6 t0_gated + 4 必含项」「`naming_candidates` 按臂 + `ui` 界面文案」「`t0_evidence.template.json`」「42 项单测」「三道启动门 ①②③（exit 2/3/4）」。此项 PRO-12 未审，我判**通过**。
3. **`docs/08` §1.3 表 6 格取值与 `copy_pack.json` 逐一相符**：V1/V2/V3 × 两臂，全部逐字相符（含 V3 两臂同 h1 这一点）。
4. **承诺卡 `creator_promise_card.md` 全文重读**：结语已闭环 ✓；§1 三条数字与 `docs/05` §3.5 白名单一致 ✓；§2 兑现界面的 `exposure` 依赖与「上传成功响应不含曝光数字」仍为工程约束 ✓；**未发现 T0 前误用**（7 份物料均标注其 T0 门禁）。⚠️ 唯一遗留：该卡第 4 条承诺「创作者可**在自己的作品页回读**（推荐理由）」与 R-1 是同一兑现位——卡本身是 `t0` 相位、判合规，但**兑现位登记只覆盖了「分享页渲染位」，未覆盖「作品页回读位」**，建议一并登记到 `docs/08` §6.1（见 O-5）。
5. **跨臂/跨变体规则一致性**：`copy_pack.json` `rules[2]` ↔ `docs/08` §1.3 ↔ `naming_test_brief.md` §4 检查 2，三处措辞为同一两轴表述 ✓。

---

## 3. 需再改清单（原文 + 改法，退回 Marketing Strategist 处置）

> 全部为**就地修订**，不另建 V2 文件。R-1 涉及投放安全，优先级最高。

### R-1（高）未兑现暗示：「曝光数字可在作品页回读」以现在时断言，而该位未实现

- **原文**
  - `marketing/data/community_scripts.md` 脚本 A ③：`③ 曝光数字在作品页能自己回读，不用问客服。`
  - `marketing/data/community_scripts.md` 脚本 B 正文：`我怎么拿到这些数字：作品页可以直接回读，不需要问后台。`
- **问题**：`exposure` 埋点与「曝光可见位」在实现中**不存在**（`docs/08` §6.1 登记「❌ 未实现（最高优先）」；PRO-7 看板状态 `blocked`）。两句用现在时（「能」「可以直接」）断言该能力已可用 → 属**未兑现暗示**，且直接违反 `docs/08` §9.2 L-5 自己写明的红线「未实现，**不得对外暗示已可用**」。脚本以创作者**第一人称**发布，是被照抄概率最高的物料。
- **改法（逐句，二选一）**
  - 方案 ①（保留信息、降级为条件式）：
    - 脚本 A ③ → `③ 曝光数字可以在自己的作品页回读（回读位随功能开放）。`
    - 脚本 B → `我怎么拿到这些数字：来自作品页的回读位（随功能开放，开放后不用问后台）。`
  - 方案 ②（更严，删除能力断言）：
    - 脚本 A 删除 ③（保留 ①②）；
    - 脚本 B → `这些数字的取数口径：内测社区作品页（回读位开放后可直接查看）。`
- **同类、建议一并统一（不单独计为需改，但同一能力不宜三种强度）**
  - `marketing/data/creator_outreach.md` §4「…而且**你会看到**每条内容的推荐理由。」→ 建议补「（展示位随功能开放）」，与 `copy_pack.json` share V2 `pre_t0` 的降级口径一致；
  - `marketing/data/community_scripts.md` §3 异议表「…理由在作品页。」→ 同款限定。

### R-2（中）个人事实占位未闭合：脚本 C 仍写死「三条 / ×3」

- **原文**（`community_scripts.md` 脚本 C）
  - `我把三条的真实曝光数和推荐理由贴在评论区了。`
  - `【实证】三条 24h 曝光：⟨三条 24h 真实曝光数，未拿到数据前不得发布⟩`
  - `【推荐理由】⟨原文 ×3⟩`
- **问题**：同脚本正文已把条数改为 `〈真实条数〉`（「拍了〈真实条数〉条」），上述三处仍写死 3。若创作者真实条数 ≠ 3，脚本**自相矛盾**（第一人称虚假陈述），且违反 §2「个人事实不得照抄」约束。
- **改法**：三处分别改为 `我把〈真实条数〉条的真实曝光数和推荐理由贴在评论区了。` / `【实证】〈真实条数〉条 24h 曝光：⟨…⟩` / `【推荐理由】⟨原文 ×〈真实条数〉⟩`。

### R-3（中）「不可删」第③类指向一句不存在的话

- **原文**（`share_scripts.md` §0 第 2 条）：`社群版的身份披露句——「我是创作者，不是官方号」。`
- **问题**：§2 社群版正文中**没有**身份披露句（§2 只有 3 行 + 链接），全文亦无该句的 `〈不可删〉` 标记 → 「不可删」清单第③类 0 实例，创作者「保留身份披露句」的义务无法执行；而 `community_scripts.md` §1 硬门 3 明确要求公开场合必须披露身份（同类场景要求不一致）。
- **改法（二选一，推荐 ①）**：① 在 §2 正文补一行并标标记：`我是创作者，不是官方号。〈不可删〉`；② 从 §0 第 2 条删去第③类，并把清单改为「两类不可删句」。

### R-4（中）D-4 口径「所有用户可见文案」与实现不符（含 `device_files` 登记理由不实）

- **原文**
  - `docs/08` §1.3 表：`界面文案 | 页面与内联脚本里**所有**用户可见文案统一取自 copy_pack.json 的 ui.*（本轮从 app.py 硬编码抽出）`
  - `docs/08` §9.3 D-4：`**已闭合**：文案抽到 copy_pack.json 的 ui.*，纳入同一扫描`
  - `marketing/compliance/scan_manifest.json` `device_files` 对 `app.py`：`渲染代码：自 PRO-13 起其用户可见文案已抽到 copy_pack.json 的 ui.* 节点（T-01 扫描），**代码内不再保留对外文案字面量**`
- **问题**：`app.py` 仍保留用户可见中文硬编码（页脚版本/变体/候选标签 L720-721、L749；渲染兜底默认值 L712/716/732/758；`/api/*` 错误文案 L884-1012）。机制（ui.* 受同一扫描 + 必需键 fail-closed）确已闭合，但「所有」「不再保留」这两个量化claim不成立；`device_files` 的登记理由会让人误以为 `app.py` 已抽空 → 未来若有营销句写进页脚，**不会被 T-01 扫到**。
- **改法**
  - §1.3 表该行 → `界面文案 | 对外文案（页面正文与内联脚本提示语）已抽到 copy_pack.json 的 ui.* 并受同一合规扫描；页脚版本/变体/候选标签（运维核对用）、渲染兜底默认值与 /api/* 错误文案仍留在 app.py，不属投放文案`
  - §9.3 D-4 → `**已闭合（机制）**：对外文案抽到 ui.*、纳入同一扫描、必需键缺失 exit 4。**范围限定**：app.py 仍保留页脚标签/兜底值/接口错误文案（非投放文案），故「所有用户可见文案」应读作「对外投放文案」`
  - `scan_manifest.json` `device_files.app.py.reason` → `渲染代码：对外投放文案已抽到 copy_pack.json 的 ui.*（T-01 扫描）；本文件仍含页脚版本/变体标签（运维核对用）、渲染兜底默认值与 /api/* 错误文案`
- **说明**：本项**不是投放违规**（残留字符串无禁用表述），属**文档口径与登记理由的准确性**问题。

### R-5（低）§9.4 复核口径的枚举不完整

- **原文**（`docs/08` §9.4 末）：`复核口径：grep -rn "曝光下限\|产品里的一条规则\|写进产品的曝光规则" marketing/ docs/08 **只命中** ① lexicon.json 的 HB-18 规则定义 与 ② 本文档的处置记录行`
- **问题**：独立复跑同一条命令，**还命中** `marketing/reports/asset-review-editor.md`（4 行，PRO-12 报告原文引用）、`marketing/reports/asset-compliance-report.md`（原始命中明细行）、`marketing/tests/test_compliance.py`（L47 注释、L186 单测样例）、`lexicon.json` TG-02 的 reason。**结论（无投放文案命中）成立**，但枚举不完整——正是本单要求拒绝的「笼统结论」。
- **改法**：`复核口径：grep -rn "…" marketing/ docs/08 的命中只出现在四处语境：lexicon.json 的规则定义、PRO-12 审阅报告与被审报告的原始命中明细、单测样例、本文件处置记录；**无一处是投放文案**（已逐条确认）。`

### R-6（中）门禁能力表述过强：T0 门禁只验「证据信封」，不验证据真伪

- **原文**
  - `docs/08` §8.9：`本轮新增的三道启动硬门……证明的是「不可能在 T0 前误投量化承诺」这一装置性质`
  - `docs/08` §0.1 ②：`--phase t0 必须同时给 --t0-evidence，且证据里「PRO-7 交付」与「§0.3 八项前置全绿」均为 true、并列出八项、写明采集日；否则拒绝启动`
- **问题**：`t0_evidence_gate()` 实际校验的是**信封**——`pro7_delivered` / `preconditions_all_green` 为 `true`、`preconditions` 为长度 ≥ 8 的数组、`evidence_date` 非空。**不校验八项各自的 `status`/`evidence` 内容**。因此一份「两个 true + 8 个空条目 + 任意日期字符串」的文件即可放行 `--phase t0`。门禁真实防的是「**无证据启动**」，不是「**证据不实**」。
- **改法**：§0.1 ② 与 §8.9 各补一句限定：`门禁校验的是「是否提交了结构完整的 T0 证据」，不校验证据内容真伪；证据真实性由「证据文件留档 + 人工复核」负责（模板 t0_evidence.template.json 的 notes 同此限定）。`

### R-7（低 · 连带项）`docs/08` 中三行因后续单完成 / 本报告覆盖而失准

- **7a（本报告的覆盖动作引起）**
  - 原文（§7 文件清单）：`marketing/reports/ | …（另含 PRO-12 人工终审报告，由 Editor 维护）`
  - 问题：该文件（`asset-review-editor.md`）已由**本单增量复审就地覆盖**（唯一现行版本），不再是「PRO-12 人工终审报告」。
  - 改法：`…（另含 Editor 维护的 marketing/reports/asset-review-editor.md：现行版本 = PRO-14 增量复审，复审基线 commit 725f5b9）`
- **7b（PRO-15 `e3414ef` 完成后引起）** —— 以下三处的「上游条款未同步」已不再成立：
  - §4.4 未决项 #5：`上游条款（docs/05 §0.3/§4.3、docs/06 §4.3）仍含收紧前的示例文案 | … 已单列建议移交` → 建议改为 `**已由 PRO-15 同步**（commit e3414ef）：docs/05 §0.3 示例已替换、§4.3 三条 Slogan 加注 pre_t0 禁用、docs/06 §4.3 V1 主标题已就地替换；本包物料与该口径一致`
  - §9.5 残留项 #3：`上游条款未同步：docs/05 §0.3/§4.3、docs/06 §4.3 仍含收紧前的示例文案（超出本单授权）→ §4.4 #5` → 建议改为 `**已闭合（PRO-15 / e3414ef）**`
  - §4.3 表「上游条款同步」行：`docs/05/docs/06 的原文修改超出本单授权，已作为建议单列（见 §4.4 #5）` → 建议补 `（已由 PRO-15 于 e3414ef 完成）`
  - 说明：7b 是把 `docs/08` 的三处自评更新为现状，不改任何判据与物料。

---

## 4. 观察项（不阻断，不计入需再改；供后续修订采用）

- **O-1 判读口径（第二批命名测试的归因边界）**：每个候选的 `pre_t0` 刺激 = 「候选名 + 该候选自己的 slogan」，同一子批内**名称与 slogan 同时变化** → `cta_click` 差异**无法分离「名称效果」与「Slogan 效果」**；A 方向尤甚（结果承诺型名称 × 机制类 Slogan）。子批内同质 + 子批间不混算使 A/B/C 之间仍可比，故**不失焦**；但建议在 `naming_test_brief.md` §5 或 §3 补一句归因边界：`本批复盘只能判定「名称+Slogan 组合」的相对优劣，不得归因到名称或 Slogan 单项。`
- **O-2 对 PRO-13 主动暴露的三点的复审意见**
  1. `creator_outreach.md` §1 第 1 步问句「你更在意『流量规则说清楚』，还是『发出去有人看』？」→ **同意「不改」**：该句是**向创作者提问**（非对外的承诺陈述），且 §4.3 收紧裁定的影响清单第⑤项只覆盖「分享话术与社群脚本」，未含 1v1 招募话术。**建议**加一行使用注记：`该问句仅用于 1v1 内部问询，不是对外承诺；不得复用到任何对外物料。`（`docs/08` §9.5 #2 已如实登记该句为词表未覆盖项，此点予以肯定。）
  2. `creator_outreach.md` §1 第 2 步尾句「现在还不方便给数字，给了就是空头承诺」→ **确认保留**（主动披露口径，本包处理最干净的一处）。
  3. 命名方向 A 的**命名语义**仍为「结果承诺型」而 `pre_t0` 只换 Slogan、名称不变（`naming_test_brief.md` §2 已加注）→ **同意保留**：名称是品牌名（非承诺句），合规上不构成结果承诺；判读上的归因边界见 O-1。
- **O-3 词表覆盖面提示**：HB-17 覆盖「多平台一键分发 / 一键分发 / 多个平台各发一份 / 被推给谁 / 汇总在一张表…」，但**不含** V3 改后的新措辞「一个入口管理多平台发布」——该句的合规性依赖「内测目标 + 支持清单以开放时公开说明为准」的意向式框架，而非词表保护。建议在 `lexicon.json` 的 `notes` 或 §9.3 加一句说明，避免下次改文案时误以为该句在规则保护下。
- **O-4 `t0_evidence.template.json` 填写便利性**：`#1`/`#8` 的 `name` 仍是「见 docs/05-gtm-plan.md §0.3 #1/#8」（`#2`–`#7` 已写实）→ 建议把八项全部写实，减少填写人二次查阅与漏填。
- **O-5 兑现位登记范围**：`creator_promise_card.md` §1 第 4 条承诺「创作者可**在自己的作品页回读**（推荐理由）」——`docs/08` §6.1 本轮只新增登记了「**分享页**『推荐理由』渲染位」，**未登记「作品页回读位」**。两者是不同兑现位，建议 §6.1 与 §6.3 一并补登（仍属 PRO-7）。

---

## 5. 独立复跑输出（原文，可复核）

命令与工作目录：`marketing/` 所在仓库根（分支 `PRO-1-pro-1`，取值时 HEAD = `725f5b9`）

**① 单测（判据 ③）**

```
$ python3 -m unittest discover -s marketing/tests -t .
...............................
----------------------------------------------------------------------
Ran 42 tests in 19.889s

OK
[参考比对] dim=49(version=8) ref_mask=[2] auto_mask=6 一致=False | 样本=https://lcnn.example.com/?utm_source=douyin&utm_…
[参考比对] dim=69(version=13) ref_mask=[2] auto_mask=2 一致=True | 样本=https://lcnn.example.com/?utm_source=wechat&utm_…
exit=0
```
> 版本 8 的掩码不一致为**编码器自选项**（ISO/IEC 18004 允许自选合法掩码），已在 `docs/08` §1.1/§8.6 如实限定；该用例不 fail，属预期。

**② 合规审查器（判据 ③）**

```
$ python3 marketing/scripts/compliance_lint.py
[compliance-lint] hard_ban=18 t0_gated=6 扫描目标=9
[compliance-lint] 阻断级违规：0，告警级：0
[compliance-lint] 报告：marketing/reports/asset-compliance-report.md
exit=0
```
> 复跑会就地重写 `asset-compliance-report.md`（仅时间戳变化，数值与 `5e34952` 时一致）。为把本次复审的改动范围收敛到 1 个文件，我已把该报告还原为 `HEAD` 版本；上表即复跑的实际输出。

**③ D-3 实测（判据 ③）**

```
$ python3 marketing/landing/app.py --phase t0 --port 0
[compliance] phase=t0 缺少有效的 T0 前置证据，拒绝启动（exit 3）：
  - 未提供 --t0-evidence（T0 证据文件）
exit=3

$ python3 marketing/landing/app.py --phase t0 --port 0 --t0-evidence marketing/data/t0_evidence.template.json
[compliance] phase=t0 缺少有效的 T0 前置证据，拒绝启动（exit 3）：
  - 证据字段 pro7_delivered 必须为 true（当前：False）
  - 证据字段 preconditions_all_green 必须为 true（当前：False）
  - 证据字段 evidence_date 缺失（须写明证据采集日）
exit=3

$ timeout 3 python3 marketing/landing/app.py --phase pre_t0 --port 0
（正常启动，被 timeout 终止；exit=124）
```

**④ 装置反证探针（本报告独立构造，非仓库自测）**

```
SCAN_ROOTS = ('assets', 'naming_candidates', 'ui')

[D-1] 注入候选 pre_t0.slogan = "发出来，就被看见"
  → t0_gated TG-06 naming_candidates.directions.A.candidates[0].pre_t0.slogan '发出来，就被看见'
[D-1 分臂] 注入候选 t0.slogan = "发出来，就被看见"（phase=pre_t0）
  → 0 命中（分臂隔离正确）
[D-1 UI] 注入 ui.share.gate_text = "保证有人看"
  → t0_gated TG-06 ui.share.gate_text '保证有人看'
[D-2] 抽掉 assets.share 中全部 RT-04 关键词后
  → RT-04 required_missing @ copy_pack.json[assets.share]（matched=缺失：直连可播放|无需注册|不用注册|先看）
  → 同一探针抽掉 consent 文案 → RT-01/RT-02/RT-03 全部 required_missing
  （探针后文件已还原，git status 干净）
```

**⑤ `725f5b9` 相对 `5e34952` 的改动规模，以及复审期间的分支快照**

```
$ git diff --stat 5e34952..725f5b9
 docs/08-gtm-channel-execution-pack.md        | 27 +++++++++++++++++++++++++--
 marketing/data/community_scripts.md          |  2 +-
 marketing/data/creator_outreach.md           |  2 +-
 marketing/data/share_scripts.md              |  2 +-
 marketing/reports/asset-compliance-report.md |  2 +-
 5 files changed, 29 insertions(+), 6 deletions(-)

$ git show --stat --oneline e3414ef     # 复审进行中的并发提交（PRO-15）
e3414ef fix(PRO-15): 上游条款同步收紧裁定口径 …
 docs/05-gtm-plan.md                           | 52 ++++++++++++++++++++++-----
 docs/06-user-needs-map-and-assumption-plan.md | 13 +++++--
 2 files changed, 55 insertions(+), 10 deletions(-)
（仅 docs/05、docs/06 —— 均在本单「明确不做」范围内）
```

> 本报告全部判定的取值依据是**物料文件在 `725f5b9` 时的内容**；本单未修改任何物料（唯一改动 = 本报告文件 `marketing/reports/asset-review-editor.md`，就地覆盖）。

---

## 6. 移交

| 接收方 | 内容 |
|---|---|
| **PRO-10 / Marketing Strategist** | §3 的 7 项「需再改」（原文 + 改法已给出）；**R-1 优先**（投放安全）。就地修订，不另建 V2 文件。修订完成后**须再按新 commit 重跑一次增量复审**（本报告 §7.6 同款要求）。 |
| **PRO-7（Prototype Engineer）** | R-1/O-5 的兑现位：作品页**曝光回读位**与**推荐理由回读位**（`docs/08` §6.1 需一并登记）；仍是全部对外物料解锁 T0 的前置。 |
| **Director of Go-to-Market** | 上线时**不得以 `--phase t0` 启动**（会被 T0 证据门禁拦住）；且注意 R-6：门禁只验证据信封，证据真实性由留档与人工负责。 |
| **PRO-15（上游条款同步）** | 与 §4.4 #5 / §9.5 #3 同一件事，已另有单，不重复创建。 |

---

## 7. 诚实限定（不得省略）

1. 本报告是**人工通读判定**，未重写任何文案、未改 code、未改 `docs/05`/`docs/06`/`docs/07`、未重跑物料生成脚本。所有「需再改」一律以「原文 + 改法」提出，由 Marketing Strategist 处置。
2. 本报告的「需再改」**不构成阻断级判定**：物料零数字、无禁止表述（机器 0/0）。但 **R-1 是未兑现暗示**，按本包 §9.2 L-5 的自定红线，清除前不得进入对外投放。
3. **机器自检不等于合规**：本轮独立复跑得到「42 项单测 OK + 阻断级 0 / 告警级 0」，而本报告发现的 6 个独立问题**全部**不在词表与装置的可判定范围内。
4. 本报告的复审基线为 commit **`725f5b9`**（含 `5e34952`）；若物料在此之后再次修订，须按新 commit 重跑本复审（本报告就地覆盖，不留历史版本）。
5. 本报告未覆盖的对象：`docs/05`/`docs/06`/`docs/07` 上游条款（超出授权，另有 PRO-15）、`marketing/out/` 生成物、商标与域名初筛、对外部署。
6. 装置反证探针**曾临时改写 `marketing/landing/copy_pack.json`**（内存构造 + 临时落盘两种方式），探针结束后已逐字节还原（校验一致，`git status` 干净）；未对任何受冻结输入留下改动。
7. 本次复审的**全部文件改动 = `marketing/reports/asset-review-editor.md`（本文件，就地覆盖）**；`compliance_lint.py` 复跑产生的 `asset-compliance-report.md` 时间戳变化已还原，以把改动范围收敛到 1 个文件。

---

*报告结束 · Editor（PM Toolkit）· PRO-14 增量人工复审（唯一现行版本，复审基线 commit `725f5b9`）*
