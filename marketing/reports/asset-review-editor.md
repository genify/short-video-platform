# 投放物料增量人工复审报告（Editor）

> **复审基线**：分支 `PRO-1-pro-1` · commit **`3f80884`**（= PRO-22 处置提交；父 `d43faaa` = PRO-20 报告提交，再上 `b2b372e` = PRO-19 修订提交）
> **相位**：`pre_t0`（T0 = MVP 可灰度日 = PRO-7 交付；复核时 PRO-7 看板状态仍为 `blocked`）
> **本轮**：PRO-23「[P2-GTM-执行] 增量人工复审（窄范围）：PRO-22 观察项处置 O-7 / O-8」——**只复核改动面，不重开 PRO-12 ~ PRO-20 的既有判定**
> **报告版本约定**：本文件**就地覆盖、唯一现行版本、不绑定轮次**；版本与复审基线以本文件头本行为准；各轮结论摘要见对应 issue 评论。
> **判定结论**：**不通过（1 项阻断级 · 需再改后按新 commit 复验）**。两项成因均在 `docs/08` 的**新增 §13** 内，且都可就地修：
> 1. **装置门禁被击穿（阻断级 · 机器可复现）**：在本单基线复跑 `compliance_lint.py` 得 **阻断级违规：1** —— `HB-01 @ docs/08-gtm-channel-execution-pack.md :: 保底曝光`（exit 1）；**同一命令在父提交 `d43faaa` 上为 0 / exit 0**，故该违规**由本单改动引入**。由此，§5「T0 前无量化承诺（机器可验）」硬门与 §4.2 的 ✅ 自评**在当前基线不成立**。
> 2. **回执失实（记录完整性）**：`docs/08` §13.3 与 PRO-22 回执均称「阻断级违规：0」，**在本单基线上不可复现**（实测 1）。
>
> **4 个落点结论**：落点 2 / 落点 3 **已闭合**；落点 1 **部分闭合**（登记与使用前置到位，但引用物名不精确）；落点 4 **部分闭合**（内容齐备，但机器回执失实，且其中一行引文触发装置门禁）。
> **O-7 口径裁定（本单的特别判定项）**：**采纳口径方的「登记 + 使用前置」，不升级为更严解读**（理由见 §5）——即**不要求**物料 + 词表 + 单测基线级的收紧。**本单的不通过与 O-7 的口径之争无关**，仅因上述两条可直接修复的记录 / 装置缺陷。

---

## 1. 复审方法与隔离（先读）

| # | 做法 | 说明 |
|---|---|---|
| 1 | **只读副本复跑** | `git archive 3f80884 \| tar -x -C <工作区外目录>` → `$PAPERCLIP_RUN_SCRATCH_DIR/ro`；另取父基线对照副本 `ro-parent`（`d43faaa`）。**全部逐字核对与机器复跑都在只读副本内完成**；共享工作树 `PRO-1-pro-1` 未被复核过程改写，本轮在该分支上**只覆盖本报告一个文件**。 |
| 2 | **先取真实改动面** | `git diff --stat d43faaa 3f80884` + `git diff --name-only d43faaa 3f80884 -- marketing/`（原始输出见下）。 |
| 3 | **逐字核对** | 对 `3f80884` 的 `docs/08` 按**行号引用当前文本**（不引用 PRO-22 的回执表格）。 |
| 4 | **机器复跑独立执行并做父基线对照** | 三条命令在只读副本内独立跑通，且**在父提交上再跑一遍合规器**——这是判定「违规是否由本单引入」的关键对照；未采信 PRO-22 §13.3 / 评论里的表格数字。 |
| 5 | **能力断言一律回源码 / PRD 验** | 复核 §13.1 的三项自证探针（`server/recommend.py` 路由表、全仓关键词、`docs/04` F-5 / D14）。 |

**实际改动面（`git diff --stat d43faaa 3f80884`，原始输出）**

```
 docs/08-gtm-channel-execution-pack.md | 71 +++++++++++++++++++++++++++++++++--
 1 file changed, 68 insertions(+), 3 deletions(-)
```

**「未改物料 / 词表 / 清单 / 单测基线」的独立核验**：`git diff --name-only d43faaa 3f80884 -- marketing/` → **空输出**（`marketing/` 下无任何文件变化）。故「未改任何物料 / 文案 / `lexicon.json` / `scan_manifest.json` / `tests/` 基线 / `docs/05`~`docs/07`」的自述**与事实一致**（✅）。

**合规报告时间戳口径**：`marketing/reports/asset-compliance-report.md` 在 `3f80884` 的「生成时间（UTC）」= `2026-09-23T17:33:37+00:00`，对应 `b2b372e` 提交时间 `2026-09-24 01:33:41 +0800`（= UTC 17:33:41）→ **该时间戳仍属 `b2b372e` 轮**，与工单说明一致，**不属失准**（本单确未重跑物料生成脚本、未改物料，命中数与豁免数未变）。**但请注意**：时间戳「未失准」≠「结论仍成立」——见 §3。

---

## 2. 四个落点逐字核对（基线 `3f80884`）

### 2.1 落点 1：`docs/08` §6.1 新增登记行 —— **部分闭合**

当前文本（`3f80884`，`docs/08` **L377**，节选主干，逐字）：

> `| **首发独立路由（R6 `newcomer` 保底路由）/「首发曝光写成产品硬约束」** | `community_scripts.md` 脚本 A ①「首发那条走了独立路由」、脚本 B「把首发的曝光规则写成产品里的一条硬约束」与 `copy_pack.json` V1 招募 `pre_t0` 臂 / 命名 A Slogan 的**兑现位** | PRO-7 | ❌ **未实现**（**PRO-22 采纳 PRO-20 O-7 补登**）…`**使用前置（PRO-22 新增）**：该类现在时机制句在**对外投放**中的使用，以「首发独立路由在 MVP 中可见」为启动前置；未实现前只允许出现在**内测/邀请制**语境，且**不得以现在时断言该机制已可用**（与 §9.2 L-5 同口径）…`

| 核对点 | 实测 | 结论 |
|---|---|---|
| 登记为 **❌ 未实现** | ✅ 明写 `❌ **未实现**` | 到位 |
| Owner = PRO-7 | ✅ 归属列 = `PRO-7` | 到位 |
| 使用前置（对外投放启动前置 + 内测/邀请制限定 + 不得现在时断言） | ✅ 三项齐备，且注明「与 §9.2 L-5 同口径」 | 到位 |
| 自证依据可核 | ✅ `server/recommend.py` L151-152 = `{"cf": [], "follow": [], "fresh": [], "tag": [], "fallback": []}`；`grep -rn "newcomer\|FeedImpression\|exposures" server/ tests/` **零命中**；`docs/04` F-5（L547）/ D14（L1836）与引文一致 | 到位 |
| **引用物名精确性** | ❌ 两处出处写错（见 §6 O-10）：「首发那条走了独立路由」在**脚本 A 正文 ②**（L45），不在「脚本 A ①」；「把首发的曝光规则写成产品里的一条硬约束」在**脚本 C** 正文（L86），不在「脚本 B」 | 瑕疵（记录级，不阻断） |

**结论：登记与门禁**（本单的设计目标）**已到位**；引用物名有 2 处错标，属记录准确性问题，不影响登记效力。

### 2.2 落点 2：`docs/08` §12 头部「判定依据」行 —— **已闭合**

当前文本（`docs/08` **L617**，逐字）：

> `> 判定依据：`marketing/reports/asset-review-editor.md`（Editor 维护；**就地覆盖、唯一现行版本、不绑定轮次**；版本与复审基线以该文件头「复审基线」行为准）；各轮结论摘要见对应 issue 评论 —— PRO-18：`2b08f67c-f99b-4810-aef8-567d7574dc1c`；PRO-20：`fcd51b40-8423-4cd0-9282-ed076c67b4b7`。**PRO-20 判定 = 通过（可解锁对外投放，0 阻断级）**。`

- 原「现行版本 = PRO-18 增量复审 + 固定基线 `28c1309`」措辞**已消失**，改为**不绑定轮次** + 指向报告文件头 ✅
- 追加行如实说明这是 O-8 的修订（原文不再保留于本行）✅
- **两个评论 id 均实存**（本轮独立核对 C 面 API）：`2b08f67c-…` = **PRO-10** 上的「PRO-18 放行状态通知」；`fcd51b40-…` = **PRO-20** 的判正评论 ✅（归属口径不统一，见 §6 O-10③）

### 2.3 落点 3：`docs/08` §4.2「§7.2 未上线能力」自评行 —— **已闭合**

当前文本（`docs/08` **L272**，逐字）：

> `| §7.2 未上线能力（含非变现类，本轮扩围） | HB-13、**HB-17** | 落地页 V3、社群脚本、全部物料 | ✅ 未出现（V3 pre_t0/t0 两臂已改为内测意向式表述；社群脚本对**已登记兑现位**（曝光/理由回读位）的能力句已收回为条件式）。**限定（PRO-22 采纳 PRO-20 O-8 附带项）**：上一句若读作「社群脚本全部能力句已收回」则**过宽** —— 首发机制类句子（「首发那条走了独立路由」「把首发的曝光规则写成产品里的一条硬约束」）属**已登记的机制类措辞**，其机制（R6 `newcomer`）**未实现**，已登记于 §6.1 并附使用前置，见 §13.1 |`

原句「社群脚本的能力句已收回到实现范围」（过宽）**已收窄**为「对**已登记兑现位**（曝光/理由回读位）的能力句已收回为条件式」，并**显式补加限定句**把首发机制类句子指向 §6.1 使用前置 ✅。**注**：该行的 ✅ 结论是按「HB-17 未扩围」的现行口径表述的（见 §5）；本行的 ✅ 与 §3 的机器违规无关（机器违规落在 §13.1 的引文行，不落在本行）。

### 2.4 落点 4：`docs/08` 新增 §13（L687–L750）—— **部分闭合**

| 子项 | 实测 | 结论 |
|---|---|---|
| §13 头部判定依据 / 处置原则 / 修订基线（L689–L691） | 齐备（判定依据指向报告文件 + PRO-20 评论；处置原则声明「不改对外文案、不改 `lexicon.json`、不改判据与单测基线、不重开 PRO-12~PRO-20」；修订基线 = `d43faaa`） | ✅ |
| §13.1 O-7：问题原文 + 独立探针 + 处置两条 + 4 条不收紧理由 + **升级路径** | 逐项齐备，且与 §6.1 同行内容一致；升级路径如实登记（「若 board 采更严解读 → 升级为阻断项，须同时改物料 + 词表 + 单测基线并另起一轮」） | ✅（内容层） |
| §13.1 探针表（L701–L703） | 三项探针与源码/PRD 一致 | ✅（事实层） / ❌（**装置层：L703 触发 HB-01 阻断级命中**，见 §3） |
| §13.2 O-8 两落点「原文 → 改法」（L723–L726） | 两条与 §2.2 / §2.3 的实际文本一致（无「引用与被引不一致」的歧义） | ✅ |
| §13.3 机器复跑回执（L728–L738） | ❌ **与本单基线不可复现**：回执称 `阻断级违规：0 (exit=0)`，实测 `1 (exit=1)`；`unittest` 部分（42 tests / OK）属实 | ❌ **记录失实** |
| §13.4 放行状态（L740–L746） | 声明「PRO-20 判定 = 通过 → 可进入上线整合」「本单已改 `docs/08` → 须重跑增量复审」「未做清单」 | 部分成立：**放行前提已被 §3 的机器违规打断**，`pre_t0` 锁定维持 |

---

## 3. 独立机器复跑与关键发现（本单判定的实质依据）

三条命令均在只读副本内独立执行；**合规器额外在父提交上跑了一遍作对照**：

| 命令 | 父 `d43faaa`（对照） | **本单 `3f80884`** | 工单预期 |
|---|---|---|---|
| `python3 -m unittest discover -s marketing/tests -t .` | `Ran 42 tests` / `OK`（exit 0） | `Ran 42 tests` / `OK`（exit 0） | `Ran 42 / OK` ✅ |
| `python3 marketing/scripts/compliance_lint.py` | `阻断级 0，告警级 0`（exit 0） | **`阻断级 1，告警级 0`（exit 1）** | `阻断级 0 告警级 0` ❌ |
| `python3 marketing/landing/app.py --phase t0 --port 0`（含带模板） | `exit 3` | `exit 3`（两次） | 未列（沿用历轮口径）✅ |

### 3.1 命中定位（不依赖合规器报告表，逐行独立复算）

- 扫描目标 **T-06 = `docs/08-gtm-channel-execution-pack.md`，模式 `quoted`，相位 `t0`**（`scan_manifest.json` L15）。
- `quoted` 模式的豁免规则：**命中所在行**若含 `lexicon.quoted_context_markers` 任一词（`禁止 / 不得 / 黑名单 / 红线 / 驳回 / 不予 / 不使用 / 不出现 / D 级 / 违规 / 旧口径 / 已删除`）或（在审查器路径下）`policy_markers`（`不做 / 不买 / 不承诺 / 不作 / … / 口径` 等）即豁免。
- 全文件 `保底曝光`（HB-01 `保底\s*曝光`）命中 **3 处**：

| 行 | 位置 | 该行是否含豁免词 | 判定 |
|---|---|---|---|
| L254 | §4 词表说明行 | 含 `D 级` → **豁免** | 通过（父提交同在，非本单引入） |
| L377 | §6.1 新增登记行 | 含 `不得`、`口径` → **豁免** | 通过 |
| **L703** | **§13.1「上游登记（`docs/04`）」探针行** | **不含任何豁免词 → 未豁免** | **❌ 阻断级（本单引入）** |

→ 合规器报「T-06 ❌ 违规 1（原始命中 8）」、总结论「**不通过（禁止投放）**」，与上表完全一致。

### 3.2 归因与确定性（可复现）

- **父提交对照**：同一命令在 `d43faaa` 上 `阻断级 0 / exit 0` → **违规确由本单 `3f80884` 引入**（§13.1 探针表 L703 是新文本）。
- **修正验证（在只读副本内实验，未改共享分支）**：仅给 L703 补一个引用语境标记 → `阻断级 0 / exit 0`；把该行还原 → 复现 `阻断级 1 / exit 1`。两次往返结果一致 → **确定性缺陷，非抖动、非环境差异**。

### 3.3 性质判定（为何这构成阻断，而非「机器误伤可忽略」）

- 命中的**语义**确实无害：L703 是**引用上游缺口登记**（证明机制未实现），不是投放表述。**但**：
  1. 本执行包自设的硬门是**机器可验的「合规器 0 阻断级」**（`docs/08` §5 硬门行 L342「已满足（机器可验）｜合规器 0 阻断级违规」）；`scan_manifest` 把 `docs/08` 定为 `quoted` 模式，**正是为了让规则文档合法引用黑名单词**——而豁免要求**同一行**带标记。L703 漏了标记，**门禁因此真实变红**（exit 1）。
  2. 该文档是本执行包**唯一的控制面**，后续每一轮复审（含上线整合方）都以「合规器 0」为放行前置；基线上恒为红，**放行链就被阻塞**——这不是措辞口味问题，而是**装置状态问题**。
  3. 与 §13.3 的**失实回执**叠加后，风险升级：下游若按回执行动，会**以为门禁已过**（而实际为红）。这正是「记录失实」必须在放行判定中被计为一等问题的原因。

---

## 4. 阻断级「需再改」项（1 项 · 含原文与改法）

### N-1（高 · 装置门禁 / 记录完整性）：§13.1 探针行触发 HB-01 + §13.3 回执失实

**原文（`3f80884` 逐字，`docs/08` L703）**

> `| 上游登记（`docs/04`） | F-5：「新视频保底曝光未实现（**P0**）…当前实现**无此机制**」；D14：「R6 新视频保底路由 **❌ 不存在** … **需新增** **P0**」 |`

**问题**：该行不含任何 `quoted_context_markers`，而 T-06 为 `quoted` 模式 → HB-01 命中未豁免 → 全包判定变为「不通过（禁止投放）」。

**改法（择一，均为记录层、不动判据）**

- **改法 A（最小 · 保留逐字引用）**：在行首补引用语境标记，使其落在豁免路径内。例：
  `| 上游登记（`docs/04`） | （**引用禁令原文**，物料内不使用该表述）F-5：「新视频保底曝光未实现（**P0**）…」；D14：「…」 |`
  （已在本副本内验证：补标记后 `阻断级 0 / exit 0`。）
- **改法 B（不引原文）**：不复写上游含禁用词的标题，只给编号与结论。例：
  `| 上游登记（`docs/04`） | F-5（新视频**保底路由的曝光**未实现，**P0**）与 D14（R6 `newcomer` **不存在**，**需新增**，**P0**） |`

**同时必须做的两件事**

1. **§13.3 回执替换为真实输出**（含 exit code），并说明差异成因（L703 为最后新增的引文行，提交前未再跑一次合规器）。
2. **重跑并留痕**：改后 `python3 marketing/scripts/compliance_lint.py` 必须为 `阻断级 0 / 告警级 0 / exit 0`，并把**真实输出**（建议同时贴父基线对照）写入 §13.3；`unittest` 42 / OK 保持不变。

**明确禁止**：**不得**以「扩大豁免路径 / 放宽 `quoted_context_markers` / 收窄扫描目标」的方式消解该项——那属**放宽**，须 board 裁定；也不得为此改动任何 `marketing/` 物料、词表、`scan_manifest.json` 或 `tests/` 基线。

---

## 5. 口径裁定（本单的特别判定项）：**不升级为更严解读**

工单要求我明确回答：是否应按更严解读，判定 `HB-17` 应覆盖「引用未实现路由的机制断言」，从而把 O-7 升级为阻断项（并要求同时改**物料 + 词表 + 单测基线**）。

**我的答复：不升级。** 采纳 §13.1 的「登记 + 使用前置」路径，理由：

| # | 理由 |
|---|---|
| ① | 该机制句是**上游指定并已登记**的机制类措辞（`docs/05` §0.3、`docs/08` §9.4②），且**被单测反证清单显式保护**——本轮独立复核 `marketing/tests/test_compliance.py` L194–198：`for ok in ("规则写出来，你自己核对", "每条理由都写出来", "首发走独立路由", "先育苗，再谈赛道")` → 断言**不得报违规**。把一条被现行装置明文保护的句子径行改判为违规 = **收紧**。 |
| ② | 更严解读的执行代价落在**判据层**（`lexicon.json` HB-17 扩围 + 单测基线改写）——超出「观察项处置」的范围，且本包边界明写「**不可放宽，也不可收紧**」，收紧须由口径方 / board 裁定，不能由 Editor 单方面施加。 |
| ③ | 该句**无数字、非结果承诺**，`pre_t0` 硬门未被击穿；实际投出面（渠道 3）本就受 §2.2「③ 的启动前置」约束在 T0 之后。 |
| ④ | 本单新增的处置**已把风险从「措辞口味」转为「可复核登记 + 明确门禁」**：§6.1 登记 ❌ 未实现（可被后续每轮机械复核）+ 使用前置（对外投放以「机制可见」为前置）。风险暴露面因此**收窄**，而非扩大。 |
| ⑤ | 一致性：PRO-20 已就同一事实判「不阻断并留观察项」，并明确把「是否采更严解读」交由口径方；口径方已裁定。若在**事实未变、装置未变**的情况下由复审方翻转口径，会造成跨轮判定不可预测。 |

**附（如实登记，不作为本单判定）**：若 board 采更严解读，升级路径已在 §13.1 末段如实写明（升级为阻断项 + 同时改物料 + 词表 + 单测基线 + 另起一轮复审）；**本报告不做该升级判定**。

**注意区分**：本节结论**不影响** §4 的阻断级判定。本单的不通过**不是**因为 O-7 的口径，而是因为新增文本**击穿了机器门禁 + 回执失实**——两项均可就地修，且不需触碰任何物料或判据。

---

## 6. 观察项（**不阻断**，供下一轮记录维护一并处理）

### O-9（低 · 门禁可执行性）：新增的「使用前置」是**文档门禁**，机器层未同步

§6.1 / §13.1 的「使用前置」要求：机制可见前，该类现在时机制句**不得用于对外投放**。但：

- `lexicon.json` 的 HB-17 覆盖范围**未变**（本单明确不改词表）；
- `tests/test_compliance.py` L194–198 的反证清单**仍把「首发走独立路由」列为不得报违规**；
- 因此**装置不会拦**。若下游在机制不可见前把该句用于对外投放，机器检查仍显示干净——**这道门禁只能靠人记得**。

**建议（不阻断）**：在 §6.1 使用前置处补一句「**本门禁为人工门禁，装置不覆盖**」；并在下一轮评估是否把该句移出反证清单（那属判据层变更，须单独派单 + board 口径）。**本轮不改任何词表与单测。**

### O-10（低 · 记录一致性）：三处引用精度问题（只改记录，不改判据）

| # | 落点 | 问题 | 建议 |
|---|---|---|---|
| ① | `docs/08` §6.1 L377 | 引文出处错标：L45「首发那条走了独立路由」在**脚本 A 正文 ②**（`community_scripts.md` L36 为 `### 脚本 A｜food-home`，L45 为其正文 ②），不是「脚本 A ①」；L86「把首发的曝光规则写成产品里的一条硬约束」在**脚本 C** 正文（L78 为 `### 脚本 C｜local-life`），不是「脚本 B」 | 改为「脚本 A 正文 ②（L45）」「脚本 C 正文（L86）」 |
| ② | `docs/08` §2.3 第 2 条（L183） | 括注仍只列两个回读位；而 §6.1 现已新增**第三个** ❌ 兑现位（首发独立路由）。于是「引用 §6.1 ❌ 兑现位**必须加条件式**」（§2.3②）与「该类句子改用**使用前置**（禁用对外投放）」（§13.1）**两套处置并存**，下游读法不唯一 | 在该括注补「**或**按 §6.1 该行『使用前置』执行」，使两处口径指向同一判定树 |
| ③ | `docs/08` §12 L617 | 两个评论 id 的**归属口径不统一**：`2b08f67c` 实为 **PRO-10** 上的「PRO-18 放行状态通知」，`fcd51b40` 实为 **PRO-20** 上的判定评论 | 统一改写为「所在 issue + 评论 id」（例：`PRO-10 / 2b08f67c…`） |

三项均属**记录一致性**，**不阻断**；建议并入下一轮记录维护或本次返修（若返修方愿意一并处理）。

---

## 7. 投放放行结论

**判定：不通过 —— 本执行包在当前基线（`3f80884`）不得进入对外投放。**

| 硬门 / 检查项 | 当前基线实测 | 结论 |
|---|---|---|
| 机器自检：阻断级违规 | **1**（`HB-01 @ docs/08 §13.1 L703 :: 保底曝光`，exit 1） | ❌ **不满足**（父基线为 0，属本单引入） |
| 机器自检：告警级（必含项缺失） | 0 | ✅ |
| T0 前量化承诺（三个数字） | 沿用 PRO-20 的物料层通读结论（本轮**未重跑物料**，物料确未变）；`pre_t0` 物料零数字 | ✅（结论未变） |
| T0 前结果承诺（含无数字） | 同上（未发现） | ✅（结论未变） |
| 未实现兑现位的现在时断言 | O-7 的机制类断言：**已登记（§6.1 ❌）+ 使用前置**；维持 PRO-20 的非阻断口径（本报告 §5） | 维持（非阻断） |
| 登记完整性（`docs/08` §6.1） | 三个原兑现位 + 本单新增第三个，共 4 行 ❌ 且各带限定 | ✅ |
| **放行解锁** | 放行判定权在 Editor；**本单基线不予解锁** | ❌ |

**路由与后果（必须按此执行）**

1. **`pre_t0` 锁定维持**：全部物料仍不得进入对外投放；上线时**不得以 `--phase t0` 启动**（T0 证据门禁，exit 3）。
2. **PRO-24（上线整合移交，Owner = Director of Go-to-Market）不得据 PRO-20 的「通过」启动落地页对外部署与流量动作** —— 其前置（复审「通过」）在本次判定下**未成立**。请以本轮判定为准，等 §4 的 N-1 返修 + 新 commit 复审「通过」后再启动。
3. **返修路由**：已建**未指派**返修子单（描述内写明目标 Owner = **Marketing Strategist**），范围仅限 `docs/08`（§4 的 N-1 + 可选的 §6 O-9 / O-10），**不得改物料 / 词表 / `scan_manifest.json` / `tests/` 基线 / `docs/05`~`docs/07`**，**不得以豁免扩围或收窄扫描目标的方式消解**。
4. 返修提交后，本复审须**按新 commit 再跑一次**（`docs/08` §9.5 #1 口径）。

---

## 8. 机器复跑原始输出（本机实际输出）

**基线 `3f80884`（只读副本 `ro`）**

```
$ python3 -m unittest discover -s marketing/tests -t .
...............................（ResourceWarning ×1，属既有噪声）
...........
----------------------------------------------------------------------
Ran 42 tests in 16.766s

OK
[参考比对] dim=49(version=8) ref_mask=[2] auto_mask=6 一致=False | 样本=https://lcnn.example.com/?utm_source=douyin&utm_…
[参考比对] dim=69(version=13) ref_mask=[2] auto_mask=2 一致=True | 样本=https://lcnn.example.com/?utm_source=wechat&utm_…
exit=0
```

```
$ python3 marketing/scripts/compliance_lint.py
[compliance-lint] hard_ban=18 t0_gated=6 扫描目标=9
[compliance-lint] 阻断级违规：1，告警级：0
  - [hard_ban] HB-01 @ docs/08-gtm-channel-execution-pack.md :: 保底曝光
[compliance-lint] 报告：<只读副本>/marketing/reports/asset-compliance-report.md
exit=1
```

```
$ python3 marketing/landing/app.py --phase t0 --port 0
[compliance] phase=t0 缺少有效的 T0 前置证据，拒绝启动（exit 3）：
  - 未提供 --t0-evidence（T0 证据文件）
exit=3

$ python3 marketing/landing/app.py --phase t0 --port 0 --t0-evidence marketing/data/t0_evidence.template.json
[compliance] phase=t0 缺少有效的 T0 前置证据，拒绝启动（exit 3）：
  - 证据字段 pro7_delivered 必须为 true（当前：False）
  - 证据字段 preconditions_all_green 必须为 true（当前：False）
  - 证据字段 evidence_date 缺失（须写明证据采集日）
exit=3
```

**父基线对照 `d43faaa`（只读副本 `ro-parent`）**

```
$ python3 marketing/scripts/compliance_lint.py
[compliance-lint] hard_ban=18 t0_gated=6 扫描目标=9
[compliance-lint] 阻断级违规：0，告警级：0
exit=0
```

**合规器生成的报告（`3f80884` 轮）关键行（原始输出）**

```
| T-06 | `docs/08-gtm-channel-execution-pack.md` | quoted | ❌ 违规 1 | 8 |
| | ↳ 原始命中明细 | | | HB-01:保底曝光、HB-08:站内关系链、HB-09:算法更准、HB-14:对手不透明、HB-15:买量、HB-16:比别的平台、HB-17:被推给谁、HB-18:曝光下限 |
- 判定：**不通过（禁止投放）**
```

**修正验证（仅在只读副本内做，未改共享分支）**

```
# 仅给 L703 补引用语境标记
$ python3 marketing/scripts/compliance_lint.py
[compliance-lint] 阻断级违规：0，告警级：0        exit=0
# 还原该行后复跑
[compliance-lint] 阻断级违规：1，告警级：0        exit=1   ← 确定性复现
```

> **读数限定（不得省略）**：机器复跑只证明「本单改动是否引入新的**可判定**违规」。词表仍是正则匹配，**语义类表述仍可能漏检**（§4.4 #6 / §8 #8）；反过来说，**引文行也可能因缺引用标记而误报**——本单的 N-1 正属后者。两类盲区都需要人工复审兜底，这也是本报告存在的原因。

---

## 9. 边界与「未做」（如实记录）

1. **交付物 = 判定**，不是改稿：本报告**未改写任何文案**，**未改** `docs/05` / `docs/06` / `docs/07`，**未改** `lexicon.json` / `scan_manifest.json` / 任何 `marketing/` 物料与判据，**未重跑**物料生成脚本，**未新增数字或承诺等级**。
2. **未改** `docs/08`：`§4` 的「原文 + 改法」与 `§6` 的观察项**只在本报告内提出**，未就地改动文档（按本单边界，返修由子单承接）。
3. **未重开** PRO-12 / PRO-13 / PRO-14 / PRO-15 / PRO-16 / PRO-17 / PRO-18 / PRO-19 / PRO-20 的既有结论；对 O-7 的口径**明确答复：不升级**（§5）。
4. **未做**：人工通读（本单为窄范围复核，物料未变，故沿用 PRO-20 的物料层通读结论）；商标 / 域名初筛；对外部署。
5. **未用豁免掩盖真实违规**：本报告 §8 的「修正验证」只在**只读副本**内进行，共享分支未被写入；且**不主张**「用扩标记消解」为唯一解（§4 明确禁止放宽）。
6. 本报告**不写入本轮的自身提交哈希**（延续 PRO-19 §12 的哈希口径）；本轮提交回执见 PRO-23 评论。
7. 落点与留痕：本文件在共享分支 `PRO-1-pro-1` **就地覆盖**，`git add` 仅本文件，提交后以 `git show --stat HEAD` 确认改动面只有本文件。

---

*报告结束 · Editor（人工复审）· PRO-23 增量复审（窄范围）· 复审基线 `3f80884` · 判定：**不通过**（1 项阻断级：§13.1 引文行击穿 HB-01 机器门禁 + §13.3 回执失实；O-7 口径裁定：不升级）*
