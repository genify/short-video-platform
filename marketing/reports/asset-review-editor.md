# 投放物料增量人工复审报告（Editor）

> **复审基线**：分支 `PRO-1-pro-1` · commit **`b2b372e`**（= PRO-19 修订提交；其父 `2384bdf` = PRO-18 报告提交，再上 `28c1309` = PRO-16 处置提交）
> **相位**：`pre_t0`（T0 = MVP 可灰度日 = PRO-7 交付；复核时 PRO-7 看板状态仍为 `blocked`）
> **本轮**：PRO-20「[P2-GTM-执行] 增量人工复审：PRO-19 判定处置物料」——对 PRO-19 的就地修订做**复验**并出**投放放行判定**
> **报告版本约定**：本文件**就地覆盖、唯一现行版本、不绑定轮次**；版本与复审基线以本文件头本行为准；各轮结论摘要见对应 issue 评论。
> **判定结论**：**通过（可解锁对外投放）** —— 0 阻断级；N-1 / N-2 / N-3 / 附② / O-1 / O-5 / O-6 **逐项已闭合**；另附 **2 项不阻断观察项（O-7 / O-8）**，已建未指派跟进子单（Owner = Marketing Strategist）。

---

## 1. 复审方法与隔离（先读）

| # | 做法 | 说明 |
|---|---|---|
| 1 | **只读副本复跑** | `git archive b2b372e \| tar -x -C <工作区外目录>`；本轮**全部逐字核对与机器复跑都在只读副本内完成**（`$PAPERCLIP_RUN_SCRATCH_DIR/pro20-ro`）。共享工作树 `PRO-1-pro-1` 未被复核过程改写；本轮在该分支上**只新增/覆盖本报告一个文件**。 |
| 2 | **逐字核对方式** | ① `git diff 2384bdf b2b372e` 取 PRO-19 的**实际改动面**（4 文件 / +88 −15）；② 对 `b2b372e` 的文件原文按**行号逐字引用**当前文本；③ 对 PRO-19 自述的改法逐条比对，**不引用 PRO-19 的回执**作为依据。 |
| 3 | **机器复跑独立执行** | 三条命令均在只读副本内独立跑通（原始输出见 §6）；未采信 PRO-19 §12.5 的表格数字。 |
| 4 | **能力断言一律回源码核对** | 凡文案涉及「产品能力」，一律回到 `server/`、`docs/04`（PRD v2.0 现状表/缺口表）核对实现状态，避免「文案加了条件式 ⇒ 能力已实现」的误推。 |

**PRO-19 的实际改动面（`git diff --stat 2384bdf b2b372e`，原始输出）**

```
 docs/08-gtm-channel-execution-pack.md        | 88 +++++++++++++++++++++++++---
 marketing/README.md                          |  5 +-
 marketing/data/community_scripts.md          |  8 +--
 marketing/reports/asset-compliance-report.md |  2 +-
 4 files changed, 88 insertions(+), 15 deletions(-)
```

**合规报告时间戳与提交时间的自洽性核对**：`marketing/reports/asset-compliance-report.md` 头部「生成时间（UTC）：`2026-09-23T17:33:37+00:00`」；`b2b372e` 提交时间 `2026-09-24 01:33:41 +0800`（= UTC 17:33:41）。两者相差 4 秒且同属 PRO-19 那一轮 → **该报告确为 PRO-19 修订后的复跑**，且 diff 显示该文件**只有时间戳一行变化**（命中数与豁免数未变），与「未新增违规、未新增豁免」一致。

---

## 2. 逐项复验结果（PRO-18 判定 → PRO-19 修订 → 本轮结论）

### 2.1 N-1（高 · 投放安全）：`community_scripts.md` 现在时能力断言 —— **已闭合**

PRO-18 指明的 **4 处**落点，本轮逐字核对 `b2b372e` 的当前文本如下（行号 = `b2b372e` 的 `marketing/data/community_scripts.md`）：

| # | 落点 | 当前文本（`b2b372e` 逐字引用） | 条件式 | 结论 |
|---|---|---|---|---|
| 1 | 占位替换规则（L30） | `**占位符替换规则（缺一不发）**：脚本里的 `⟨…⟩` 一旦被替换为数字，必须同时满足 ① 有真实数据、② 有推荐理由原文（从作品页复制，不得改写；该回读位开放后）、③ 有带来源参数的专属链接。` | `；该回读位开放后` | ✓ 已加 |
| 2 | 脚本 A ①（L44） | `① 每条内容都写了一行推荐理由，我能看到它为什么被推（理由的展示位随功能开放）；` | `（理由的展示位随功能开放）` | ✓ 已加 |
| 3 | 脚本 A 评论区模板（L53） | `【推荐理由】⟨从作品页原文复制，不得改写（回读位开放后）⟩` | `（回读位开放后）` | ✓ 已加 |
| 4 | 脚本 C 正文（L87） | `而且每条都会给一行推荐理由——至少我们知道东西发出去之后发生了什么（推荐理由回读位随功能开放）。` | `（推荐理由回读位随功能开放）` | ✓ 已加 |

**diff 侧证（`git diff 2384bdf b2b372e -- marketing/data/community_scripts.md`）**：4 行 hunk 恰好、且仅落在这 4 处，无夹带改动（全文 diff 已逐行通读）。

> **引用精确性注（非缺陷，供下游读准）**：PRO-19 §12.1 表中 L87 那行写作 `…至少我们知道东西发出去之后发生了什么。` → `…（推荐理由回读位随功能开放）。`，用的是「`…` 省略前文」的缩写约定；**实际文本并未删除前句**，而是把条件式追加在原句末尾（与上表第 4 行一致）。下游不得据此认为该句被替换。

**为什么这 4 处是真正改到位（而不是形式上加字）**：条件式所指的兑现位，正是 §6.1 登记为 ❌ 未实现的那两个（作品页**曝光回读位** / **推荐理由回读位**）。加限定式后，句子从「现在就能回读」变成「该位开放后可回读」，**不再以现在时断言未实现的能力**，与 PRO-16 已通过的 6 处落点（PRO-18 报告 §表格第 1–6 行）同款。

**⚠️ 本轮仍需重申的界定（PRO-18 的告戒，本轮严格沿用）**：**「文案已加条件式」≠「兑现位已实现」**。`docs/08` §6.1 的两行登记在本轮**未发生任何变化**，仍为 ❌ 未实现（PRO-7）：

- `| 分享页「推荐理由」渲染位 | … | PRO-7 | ❌ 未实现（本轮新增登记：pre_t0 文案已降级为「随功能开放」，t0 前不构成未兑现） |`
- `| **作品页「曝光回读位」+「推荐理由回读位」** | … | PRO-7 | ❌ 未实现（**PRO-16 采纳 PRO-14 O-5 补登**：…**PRO-19 补**：亦为 §2.2 排期纪律 ③ 与 community_scripts.md §1 硬门 1 的**取数来源前置** —— 未实现前该纪律与硬门 1 均不可执行） |`

本轮的「闭合」仅指**文案层的现在时断言清零**；**兑现位本身仍未实现**，且 §6.1 的「全部对外物料解锁 T0 的前置」这一口径**未被放宽**。

### 2.2 N-1 的反向依据（`docs/08` §2.3 第 2 条）—— **已闭合**

当前文本（`b2b372e`，`docs/08` L182）逐字引用（节选其判定主干）：

> `2. **能力描述不得超出实现**：脚本里的能力句只有两类合法形式 —— ① **已实现的机制可直述**（例：`server/recommend.py::_reason_for()` 已实现 `reason` 字段，故「每条内容都带一行推荐理由」可写）；② **引用本包 §6.1 登记为 ❌ 未实现的兑现位**（作品页曝光回读位 / 推荐理由回读位）时，**必须加条件式「随功能开放」**…**不得**以现在时断言该兑现位可用；也**不得**写「能看到它被推给谁」…（**PRO-19 改写**：本条原把 L44「我能看到它为什么被推」列为「可写」的正面示例，与 §6.1 的兑现位登记 + §9.2 L-5 红线**互斥**；现改为上述两分法，三处口径已统一。处置见 §12.1。）`

**我把它当「事实主张」来验**（不只看措辞是否改过）——两分法的**①支**引用了代码事实，故回源码核对：

- `server/recommend.py` L434–L453 确有 `def _reason_for(cand: Candidate) -> str`，返回值形如 `f"{label}主导（召回路径：{routes}）"`（人话理由，含主导特征 + 召回路径）；
- L408–L423 的 feed item 组装确有 `"reason": it.reason` 字段；路由标签为 `cf/follow/fresh/tag/fallback`，**确不含受众信息**（故「不得写『能看到它被推给谁』」成立）；
- `docs/04` §3.2.7 `FR-2.6.a`：`**reason` 覆盖率 100%` —— 「已实现（`_reason_for`）」。

→ ①支（`reason` 字段已实现）与②支（引用 ❌ 兑现位必须加条件式）**均与源码/PRD 一致**；§2.3 原「互斥」已消解。

**三处口径一致性核对（PRO-19 §12.6 的复核请求 ①）**：

| 处 | 当前口径（`b2b372e`） | 一致？ |
|---|---|---|
| `docs/08` §2.3 第 2 条 | ①已实现机制直述 ／ ②引用 §6.1 ❌ 兑现位必须加条件式 | 基准 |
| `docs/08` §6.1 | 三个兑现位（分享页渲染位 / 曝光回读位 / 推荐理由回读位）均登记 **❌ 未实现（PRO-7）** | ✅ 与 §2.3②支一致 |
| `docs/08` §9.2 L-5 | 追加「PRO-19 记录修正」，写明清理口径 = 「引用 §6.1 ❌ 未实现兑现位的能力句一律加条件式」 | ✅ 与 §2.3②支一致 |

### 2.3 N-2（中 · 文档准确性）：`marketing/README.md` §3 尾句 —— **已闭合**

当前文本（`b2b372e`，`marketing/README.md` L77–L78）：

> `页面与脚本里的**对外投放文案**已全部来自受扫描的文案包，不再硬编码在代码里。`
> `仍留在 `app.py` 的页脚版本/变体/候选标签（运维核对用）、渲染兜底默认值与 `/api/*` 错误文案**不属投放文案**（见 `docs/08` §1.3 / §9.3 D-4 的口径收窄）。`

原句「用户可见的**每一句话**都来自受扫描的文案包」**已消失**（`git grep` 确认无残留），收窄到位并**显式补了一行**列出非投放文案。

**五处口径一致性核对（PRO-19 §12.6 的复核请求 ②）**：

| 处 | 对「仍留在 `app.py` 的非投放文案」的枚举 | 一致？ |
|---|---|---|
| `marketing/README.md` §3（L78） | 页脚版本/变体/候选标签（运维核对用）、渲染兜底默认值、`/api/*` 错误文案 | 基准 |
| `docs/08` §1.3 界面文案行（L130） | 同上三类，并给出行号（页脚 L720-721、L749；兜底 L712/716/732/758） | ✅ |
| `docs/08` §9.3 D-4（L493） | 同上三类（该项标「已闭合（机制）+ 范围限定」） | ✅ |
| `docs/08` §7 文件清单 `app.py` 行（L408） | 同上三类 | ✅ |
| `marketing/compliance/scan_manifest.json` `device_files.app.py.reason` | 同上三类（并注明原登记理由不实已改写） | ✅ |

→ 由 2 处口径（README vs §1.3）扩到 5 处**完全同款**，N-2 目标（消除下游「`app.py` 已抽空」的误读）达成。

### 2.4 N-3（低 · 可追溯性）：报告版本行与失效指针 —— **已闭合（含结构性修复）；另见 O-8**

| 落点 | 当前文本 / 状态（`b2b372e`） | 结论 |
|---|---|---|
| `docs/08` §7 文件清单 `marketing/reports/` 行（L424） | `…由 Editor 维护，**就地覆盖、唯一现行版本、不绑定轮次** —— 版本与复审基线以该文件头的「复审基线」行为准；判定正文见该报告自身，各轮结论摘要见对应 issue 评论。` | ✓ 已改为**不绑定轮次** |
| `marketing/README.md` §1 目录树（L25） | `asset-review-editor.md（人工复审报告：Editor 维护，就地覆盖、唯一现行版本、不绑定轮次；版本与复审基线以该文件头「复审基线」行为准）` | ✓ 同款 |
| `docs/08` §10 首行（L534） | 历史原文**保留**，尾部追加：`（**PRO-19 提示（采纳 PRO-18 N-3）**：本行是 PRO-16 轮的**历史快照**，保留不改写；报告现行版本已由 PRO-18 按 `28c1309` 就地覆盖，并自 PRO-19 起改为**不绑定轮次**的表述 —— 见 §7 与 §12.3。下游请以该报告文件头的「复审基线」行为准。）` | ✓ 原文 + 提示齐备 |
| 4 处 `e3414ef` 指针（§4.3 L293 / §4.4 #5 L304 / §6.2 #5 L388 / §9.5 #3 L524） | 均已是分支实际提交 `0830dd7`，且均注明 amend 前哈希 `e3414ef` | ✓ 已由 PRO-17 闭合，本轮未回退 |
| `docs/08` 文档头「修订记录」（L8） | `PRO-10 首版（9b032e5）→ PRO-13 终审返修 → PRO-15（0830dd7）→ PRO-14（7127d64）→ PRO-16 → PRO-17（1273783 / 36d398e）→ PRO-18（2384bdf；结论：不通过，3 项需再改 N-1/N-2/N-3）→ PRO-19` | ✓ 链式记录补全（O-5） |

**失效指针全量扫描（PRO-19 §12.6 的复核请求 ③）**：`grep -n "现行版本\|725f5b9\|28c1309\|1fead09" docs/08 marketing/README.md` 逐条判定 ——

- 命中中属于**「现行版本」入口**者只有 §7（L424）与 README §1（L25），**均已改为不绑定轮次** ✅；
- §10 首行（L534）、§12.3 处置表（L644–L646）内的旧指针属**处置记录/历史快照行**，已就地标注为历史 ✅；
- `docs/08` §9 首行（L446）`> 判定基线：…（commit `1fead09`，相位 `pre_t0`）` —— 该行是 **PRO-13 轮**的判定基线记录，位于已标注为历史节的 §9 内；`docs/08` §9 状态行（L448）明确「§9 = PRO-13 承接 PRO-12；§10 = PRO-16…**两级记录均保留，不互相覆盖**」。属历史记录行，**不构成「现行版本」入口**。
- **但发现 1 处同类措辞在 PRO-19 新增节内复发** → 见 **§4 O-8**（低 · 不阻断）。

### 2.5 附② §9.2 L-5 收口句（记录修正）—— **已闭合**

当前文本（`b2b372e`，`docs/08` L481）逐字引用收口段：

> `**PRO-19 记录修正**：上句措辞**过宽** —— 它未含同文件脚本 A ①（L44）/ 脚本 C 正文（L87）与 2 处模板（占位替换规则、脚本 A 评论区模板）；该 4 处已于 PRO-19 一并清理，清理口径 = 「引用 §6.1 ❌ 未实现兑现位的能力句一律加条件式」，处置明细见 §12.1`

核对要点：

1. **事实正确**：L44 / L87 / L30（占位替换规则）/ L53（脚本 A 评论区模板）四处在 `28c1309` 时确为无条件式，确未被原收口句覆盖 → 原文「过宽」的判断成立（原文确为「同类现在时断言已按 R-1 一并清理（脚本 A ③ / 脚本 B 正文 / §3 异议表 / `share_scripts.md` §2-§3 / `creator_outreach.md` §4）」）。
2. **未改判定**：L-5 行的处置状态仍为「**已改物料**（降级为「随功能开放」）；**兑现位登记移交 PRO-7**」，未改为「已实现」或「已闭合装置」→ 符合 PRO-19 「只改记录准确性，不改任何判定」的自述。
3. **口径可执行**：新写下的是**可判定的清理口径**（按「是否引用 §6.1 ❌ 兑现位」分类），而非泛化表述 → 下一轮可据此机械复核。

### 2.6 观察项 O-1 / O-5 / O-6 —— **逐项已闭合**

| 项 | 当前文本（`b2b372e` 逐字引用） | 结论 |
|---|---|---|
| O-1（§0.1 相位表把「推荐理由可回读」列为 `pre_t0` 允许表述） | L18：`| `pre_t0` | 现在 → T0 前 | 机制类表述：规则先公开、推荐理由**回读位**（随功能开放后；见 §6.1 兑现位登记）、邀请制内测 | …` | ✓ 与 N-1 同口径（已去掉无条件的「可回读」） |
| O-5（文档头修订记录只记到 PRO-13） | L8：完整链式记录（见 §2.4 上表末行） | ✓ 已补全 |
| O-6（§2.2 纪律 ③ 的取数来源依赖未实现位） | L170 新增段：`**③ 的启动前置（PRO-19 采纳 PRO-18 O-6）**：③ 依赖 §6.1 的两个回读位（作品页**曝光回读位** / **推荐理由回读位**）实现 —— 未实现前该「展示」无合法取数来源（`community_scripts.md` §1 硬门 1「有真实数据」同理）。因此**不得先按排期启动**，与 §6.1「全部对外物料解锁 T0 的前置」同口径。` **且** §6.1 兑现位行（L376）补登 `亦为 §2.2 排期纪律 ③ 与 community_scripts.md §1 硬门 1 的**取数来源前置** —— 未实现前该纪律与硬门 1 均不可执行` | ✓ 两处联动闭合（既声明不得先启动，又把依赖登记回 §6.1） |

### 2.7 逐项结论汇总

| 项 | PRO-18 判定 | 本轮结论 |
|---|---|---|
| N-1（4 处现在时能力断言 + 2 处模板预设） | 高 · 投放安全 | **已闭合**（4 处条件式逐字到位；兑现位登记未被误升为「已实现」） |
| N-1 反向依据（`docs/08` §2.3） | 必须显式消解 | **已闭合**（两分法成立且与源码事实一致；§2.3 / §6.1 / §9.2 L-5 三处口径一致） |
| N-2（README「每一句话」过宽） | 中 · 文档准确性 | **已闭合**（收窄 + 补列；与另外 4 处口径完全同款） |
| N-3（版本行 / 失效指针） | 低 · 可追溯性 | **已闭合**（两处入口改不绑定轮次；§10 原文+提示；4 处指针已在 PRO-17 归位）；1 处同类措辞复发 → **观察项 O-8** |
| 附② §9.2 L-5 收口句过宽 | 部分闭合（记录准确性） | **已闭合**（记录修正准确、不改判定、口径可执行） |
| O-1 / O-5 / O-6 | 观察项 | **逐项已闭合** |
| **新增阻断级需改项** | — | **无（0 项阻断级）** |

---

## 3. 全文通读：除上表落点外的独立复核

本轮按 §9.5 #1 / §12.6 的口径重跑了**受影响文件的全文通读**（不是只核对上表）：

| 文件 | 核对内容 | 结果 |
|---|---|---|
| `marketing/data/community_scripts.md`（130 行，`pre_t0` / strict 扫描目标 T-02） | 清点全部「能力/回读」类句子：`随功能开放`/`开放后` 类限定共 **8 处**（L30 / L44 / L46 / L53 / L68 / L87 / L102 / L103），**无一处无条件式现在时断言**；`⟨…⟩` 占位统一为「未拿到数据前不得发布」形式；个人事实统一为 `〈真实年限〉/〈真实店龄〉/〈真实条数〉`；身份披露句齐备 | ✅ 无新发现 |
| `marketing/data/share_scripts.md`（T-03） | 3 版话术的「理由可读」句均为条件式（L34 / L44）；§0 三类「不可删」句与正文一一对应 | ✅ 无新发现 |
| `marketing/data/creator_outreach.md`（T-04） | §4 异议表条件式齐备（L59）；§5 内部口径声明在 | ✅ 无新发现 |
| `marketing/landing/copy_pack.json`（T-01，phase=both） | `pre_t0` 臂无数字、无结果承诺；`naming_candidates` 按臂存放；`t0` 臂第 4/5 条非数字承诺的兑现位在 `known_gap` 中如实登记 | ✅ 无新发现（另见 §4 O-7 的关联落点） |
| `marketing/data/naming_test_brief.md`（T-05） / `creator_promise_card.md`（T-09，t0 相位） | 上线前检查项含「无数字结果承诺」；承诺卡仍锁在 T0 相位的清单内（`legacy` 无豁免） | ✅ 无新发现 |
| `docs/08`（685 行，T-06 / quoted） | 本轮 diff 全量逐行通读：仅「记录/口径」类改动，未新增数字、未改判据、未改「不做」清单；§9/§10/§11 历史节未被改写 | ✅ 无新发现 |

**通读产生的唯一 2 项非阻断发现**记录在 §4（O-7 / O-8）；其性质与理由均写明，供口径方裁定。

---

## 4. 观察项（**不阻断本轮放行**，已建未指派跟进子单 · Owner = Marketing Strategist）

### O-7（中 · 投放安全风险暴露 · 需口径方显式消解）：首发机制的**现在时**能力断言

**原文（`b2b372e` 逐字引用）**

| # | 位置 | 原文 |
|---|---|---|
| 1 | `marketing/data/community_scripts.md` 脚本 C 正文（L86） | `在一个内测社区里，他们把首发的曝光规则写成产品里的一条硬约束（规则开放时会公开原文），` |
| 2 | 同文件 脚本 A 正文 ②（L45） | `② 首发那条走了独立路由，没有和一堆内容挤在一起；` |
| 3 | `marketing/landing/copy_pack.json` 招募 V1 `pre_t0` `bullets`（L38） | `首发走独立路由，不与其他内容抢同一口径` |
| 4 | `marketing/landing/copy_pack.json` 招募 V1 `pre_t0` `h1` | `把「能不能被看见」变成一条明写的规则` |
| 5 | `marketing/landing/copy_pack.json` 命名候选 A 向 `pre_t0` `slogan`（L224） 与 `naming_test_brief.md` L28 | `首发走独立路由` |

**证据链（实现状态）**

- `docs/04`（PRD v2.0）§3.3.1 现状表：`| 新视频 | 由 `tag` / `fresh` / `fallback` 路由承接可获得初始曝光；无标签则仅 `fallback` | ✅ 机制存在，**但无保底额度**（F-5） |`
- 同文 **F-5**：`新视频保底曝光未实现（D1 的机制缺口，**P0**）`…`当前实现**无此机制**…**无任何下限保证**`；缺口表 **D14**：`R6 新视频保底路由 | ❌ 不存在 | 需新增 | **P0** |`
- 同文 §3.3.4：`R6` 需 `recommend.py` 加 `newcomer` 路 + `db.py` 加 `exposures` 计数；`docs/03` §6.2 的「保底流量池（500~1000 次）」属设计。
- **源码实测（本轮独立核对）**：`grep -rn "newcomer\|FeedImpression\|exposures" --include=*.py --include=*.json --include=*.sql .` → **零命中**；`server/recommend.py` 的召回路由只有 `cf / follow / fresh / tag / fallback`（L152）；`server/` 内**无任何曝光/展现计数字段**。
- 因 `scan_manifest.json` 中 **T-02 `community_scripts.md` 的相位登记 = `pre_t0`（strict）**、T-01 为 `both`，上述句子在 `pre_t0` 相位下被判；而「未上线能力」是**任何相位都禁**的 `hard_ban` 类（HB-17 的 `reason` 明写「未上线能力（非变现类）…超出已实现范围」）。

**为何本轮仍判「不阻断」（判决理由，请口径方复核）**

1. **它是本包已登记的机制类替代句**：`marketing/tests/test_compliance.py` L194–L198 有**反证断言**「机制类替代句不得被误伤」，其中**明确把 `首发走独立路由` 列为不应报违规的句子**；`docs/05` §0.3（PRO-15 已同步）也把「把首发的曝光规则写成产品里的一条硬约束（具体数值规则开放时一并公开）」指定为 **T0 前唯一可用**的机制类替代句，`docs/08` §9.4② 将其登记为「统一机制措辞」。
2. 改它要动**词表/判据/单测基线**，与 PRO-19 对 O-3 的处置口径同性质（「属词表扩围…须单独评估；建议单独派单」），**超出「N-x 返修」与「增量复验」的范围**。
3. 它**不含任何数字、不构成量化承诺或结果承诺**（硬门未破）；且 `docs/08` §2.2 的「③ 的启动前置」（O-6）已使渠道 3 在兑现位实现前**不得启动**，社群脚本按排期（D45→D75，T0 相对时间）**在 T0 之后**才会实际投出。
4. 本单边界明写「**不可放宽，也不可收紧**」——把一条已登记、已被单测保护的机制类替代句径行改判为违规，属**收紧**，应由口径方裁定而非由 Editor 单方面执行。

**建议改法（任一，均不需改 `docs/05`）**

- 文案侧（最小改动）：把**完成式**改为 `docs/05` §0.3 授权示例的**进行式/意向式** —— 例如 L86 改 `他们**正在**把首发的曝光规则写成产品里的一条硬约束（规则开放时会公开原文）`；L45 改 `首发那条**会**走独立路由…`（或加 `（该机制随 T0 交付开放）`）。
- 判据侧（更彻底）：在 `docs/08` §6.1 增登记一行「**首发保底曝光路由 R6（`newcomer`）** | 社群脚本/招募文案所引「首发规则/独立路由」的兑现位 | PRO-7 | ❌ 未实现（`docs/04` F-5 / D14）」，使该口径在后续轮次**可被持续复核**（并同步 `test_compliance.py` 的反证清单）。
- 无论选哪条，均**不得**新增数字或承诺等级。

**若 board 采更严解读**（认定 HB-17「未上线能力」涵盖对未实现路由的机制断言）→ 本项即升级为**阻断级需再改项**，需再走一轮「返修 → 复审」；本轮按上述 4 条理由判为**不阻断并升级为跟进项**，请口径方明示取舍。

### O-8（低 · 记录一致性）：版本行同类措辞在 PRO-19 新增节内复发

**原文（`b2b372e`，`docs/08` L616）**

> `> 判定依据：`marketing/reports/asset-review-editor.md`（**PRO-18 增量复审**，就地覆盖的唯一现行版本；复审基线 `28c1309`）；…`

**问题**：这正是 N-3 要消除的措辞模式（「现行版本 = 第 N 轮 + 固定基线」）。PRO-19 §12.3 声明「版本行**不再写**『现行版本 = 第 N 轮』…**下一轮覆盖时无需再改 `docs/08` 与 `README`**」，并在 §12.3 末行声明残余只覆盖「§10 / §11 内的历史快照行」——**§12 头部未落入该残余声明**，属同一次提交内的自我不一致：本轮之后该行立即失准（报告已按 `b2b372e` 覆盖）。

**建议改法**

- 最小：在 §12 头部追加一行提示，与 §10 同款 —— `（**PRO-20 提示**：本行是 PRO-19 轮的历史快照，保留不改写；报告现行版本以该文件头「复审基线」行为准。）`
- 更彻底：把 §12.3 末行的残余声明由「§10 / §11」扩写为「§9 / §10 / §11 / §12（各轮『判定依据』快照行）」，并统一约定：**节头快照行一律附加「历史快照」提示**，避免下一轮再逐处判定。

### 另：`docs/08` §4.2 自评行措辞过宽（并入 O-7 处置）

`docs/08` L272 自评行仍写：`| §7.2 未上线能力（含非变现类，本轮扩围） | HB-13、**HB-17** | 落地页 V3、社群脚本、全部物料 | ✅ 未出现（V3 pre_t0/t0 两臂已改为内测意向式表述；社群脚本的能力句已收回到实现范围） |`

在 O-7 的口径未消解前，「社群脚本的能力句**已收回到实现范围**」这句自评**过宽**（脚本 A ② / 脚本 C L86 仍在断言首发的路由与硬约束）。建议在 O-7 处置时同步收窄该括注（例如改为「已按 §2.3 两分法处置；首发路由/保底机制的措辞另见 §6.1 登记与 O-7 处置」）。**只改记录准确性，不改判据。**

---

## 5. 投放放行结论

**判定：通过（可解锁对外投放）。**

| 项 | 结论 |
|---|---|
| 阻断级违规（机器） | **0**（`compliance_lint.py`：`hard_ban=18 t0_gated=6 扫描目标=9` / `阻断级违规：0，告警级：0`，exit 0） |
| 告警级（必含项缺失） | **0** |
| 人工通读：T0 前量化承诺 | **未发现**（`P10 ≥ 50` / `Gini ≤ 0.6` / 单 feed ≤ 2 条 三数字均只在 `t0` 臂与承诺卡内；`pre_t0` 物料零数字） |
| 人工通读：T0 前结果承诺（含无数字） | **未发现** |
| 人工通读：未实现兑现位的**现在时**断言 | **N-1 的 4 处已清零**；另有 O-7 的机制类断言（已登记 + 需口径消解，见 §4） |
| 硬门不变 | T0 前不得对外发布任何量化承诺；承诺发出后 7 天内未以真实数据兑现即立即停投 —— **口径未被放宽** |
| 「不做」清单 | 未改动（不买量、不买创作者、不做站内关系链、不做变现承诺） |
| 兑现位缺口 | `docs/08` §6.1 三项登记（分享页渲染位 / 曝光回读位 / 推荐理由回读位）**仍为 ❌ 未实现（PRO-7）**；本轮**未**把「文案已加条件式」当作该位已实现 |

**通过后的路由（按 `docs/08` §10.2 / §12.6 与工单口径）**

1. 本执行包**方可进入对外投放的上线整合**，由 **Director of Go-to-Market** 承接（落地页对外部署、流量决定、T0 后承诺卡发放与 7 天兑现回收）。
2. **T0 当日必须提交 `--t0-evidence` 并留痕**；**不得直接以 `--phase t0` 启动**（会被 T0 证据门禁以 exit 3 拦住）。
3. **前置未解除**：`pre_t0` 既有锁定不变；渠道 3（社群脚本）仍受 §2.2「③ 的启动前置」约束，兑现位未实现前不得启动。
4. 本轮判定**未**放宽任何承诺等级，**未**新增数字。

**建议（不阻断放行）**：O-7 请在**渠道 3 实际投产前**消解（其排期在 T0 之后，时间上充裕）；O-8 / §4.2 括注在下一轮记录维护时一并处理。两项已建未指派跟进子单，描述内写明目标 Owner = **Marketing Strategist**。

---

## 6. 机器复跑原始输出（本机实际输出，独立于 PRO-19 的回执）

在只读副本（`git archive b2b372e` 解包）内执行，三条命令均为**本轮独立复跑**：

```
$ python3 -m unittest discover -s marketing/tests -t .
...............................marketing/tests/test_links.py:84: ResourceWarning: unclosed file <_io.TextIOWrapper name='.../tmp2yk7r9nu/out/links.csv' mode='r' encoding='utf-8'>
  rows = list(csv.DictReader(registry.open(encoding="utf-8")))
ResourceWarning: Enable tracemalloc to get the object allocation traceback
...........
----------------------------------------------------------------------
Ran 42 tests in 17.990s

OK
[参考比对] dim=49(version=8) ref_mask=[2] auto_mask=6 一致=False | 样本=https://lcnn.example.com/?utm_source=douyin&utm_…
[参考比对] dim=69(version=13) ref_mask=[2] auto_mask=2 一致=True | 样本=https://lcnn.example.com/?utm_source=wechat&utm_…
exit=0
```

```
$ python3 marketing/scripts/compliance_lint.py
[compliance-lint] hard_ban=18 t0_gated=6 扫描目标=9
[compliance-lint] 阻断级违规：0，告警级：0
[compliance-lint] 报告：<只读副本>/marketing/reports/asset-compliance-report.md
exit=0
```

```
$ python3 marketing/landing/app.py --phase t0 --port 0
[compliance] phase=t0 缺少有效的 T0 前置证据，拒绝启动（exit 3）：
  - 未提供 --t0-evidence（T0 证据文件）
  说明：T0 = MVP 可灰度日（PRO-7 交付 + §0.3 八项前置全绿）。
  模板：marketing/data/t0_evidence.template.json
exit=3

$ python3 marketing/landing/app.py --phase t0 --port 0 --t0-evidence marketing/data/t0_evidence.template.json
[compliance] phase=t0 缺少有效的 T0 前置证据，拒绝启动（exit 3）：
  - 证据字段 pro7_delivered 必须为 true（当前：False）
  - 证据字段 preconditions_all_green 必须为 true（当前：False）
  - 证据字段 evidence_date 缺失（须写明证据采集日）
  说明：T0 = MVP 可灰度日（PRO-7 交付 + §0.3 八项前置全绿）。
  模板：marketing/data/t0_evidence.template.json
exit=3
```

**与工单预期逐项比对**：`Ran 42 tests — OK` ✅ ／ `hard_ban=18 t0_gated=6 扫描目标=9`、`阻断级 0 告警级 0` ✅ ／ `--phase t0` 两次均 `exit 3` ✅。`version=8` 掩码不一致为**编码器自选项**（已登记的预期输出，不影响解码），不构成失败。

> **读数限定（不得省略）**：机器复跑只证明「修订未引入新的**可判定**违规」与「装置行为不变」。词表仍是正则匹配，**语义类表述仍可能漏检**（`docs/08` §4.4 #6 / §8 #8）——O-7 正是这一类。

---

## 7. 边界与「未做」（如实记录）

1. **交付物 = 判定**，不是改稿：本轮**未改写任何文案**，**未改** `docs/05` / `docs/06` / `docs/07`，**未改** `lexicon.json` / `scan_manifest.json` / 任何 `marketing/` 物料与判据，**未重跑**物料生成脚本，**未新增数字或承诺等级**。
2. **未改** `docs/08` 与 `marketing/README.md`：N-1/N-2/N-3 的复核结论只写在本报告内；O-7 / O-8 的「原文 + 改法」也**只在本报告内提出**，未就地改动物料/文档（改由跟进子单处置）。
3. 本轮**未**对 PRO-12 / PRO-13 / PRO-14 / PRO-15 / PRO-16 / PRO-17 / PRO-18 / PRO-19 的既有结论提出重开；对 §9.2 L-5 的核对结论是**认可其记录修正**。
4. **未做**：商标/域名初筛、对外部署（属上线整合，Director of Go-to-Market）。
5. 本报告**不写入本轮的自身提交哈希**（延续 PRO-19 §12 的哈希口径：文档内自指哈希在 amend/重排下必然失真）；本轮提交回执见 PRO-20 评论。
6. 落点与留痕：本文件在共享分支 `PRO-1-pro-1` **就地覆盖**，`git add` 仅本文件，提交后以 `git show --stat HEAD` 确认改动面只有本文件。

---

*报告结束 · Editor（人工复审）· PRO-20 增量复审 · 复审基线 `b2b372e` · 判定：通过（附 O-7 / O-8 两项不阻断观察项）*
