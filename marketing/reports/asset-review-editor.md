# 投放物料增量人工复审报告（Editor）

> **复审基线**：分支 `PRO-1-pro-1` · commit **`6e8ba1a`**（= PRO-27 处置提交；父 `8e717bd` = PRO-26 报告提交，再上 `c12aa59` = PRO-25 返修提交）
> **相位**：`pre_t0`（T0 = MVP 可灰度日 = PRO-7 交付；复核时 PRO-7 看板状态仍为 `blocked`）
> **本轮**：PRO-28「[P2-GTM-执行] 窄范围增量复审：PRO-27 观察项处置 O-2/O-3/O-4/O-11」——**只复核 PRO-27 的改动面（5 个文件，`+110 −9`），不重开 PRO-12 ~ PRO-26 的既有判定**。
> **报告版本约定**：本文件**就地覆盖、唯一现行版本、不绑定轮次**；版本与复审基线以本文件头本行为准；各轮结论摘要见对应 issue 评论。
> **判定结论**：**通过（0 项阻断级 · 可解锁对外投放的上线整合）**。O-2 / O-3 / O-4 / O-11 逐条**已闭合**；一致性专项扫**无自相矛盾**；计数与放宽**未变**（含 `docs/08` 无豁免原始命中 30 = 30、行号集合相同）；硬门**维持**。另附 **3 项低观察项（记录精度，不阻断）**。
>
> **七项复核速览**
>
> | # | 复核项 | 结论 |
> |---|---|---|
> | 1 | O-2 命名测试归因边界（§1.1 + §4 检查项 7 + §5 组合口径） | **已闭合**（附 O-1 低观察项） |
> | 2 | O-3 `HB-17` 覆盖说明（`lexicon.json` `notes` 第 4 条） | **已闭合**（附 O-2 低观察项） |
> | 3 | O-4 T0 证据模板 `#1` / `#8` 写实 | **已闭合**（逐字核对上游 `docs/05` §0.3） |
> | 4 | O-11 §12 头部 / §13 头部 / §14.6 时效指针 | **已闭合**（无「单读 = 可解锁」失效行） |
> | 5 | 新增 §15 与全文一致性 + §15.6 与报告头一致 | **已闭合**（无自相矛盾；附 O-3 低观察项） |
> | 6 | 是否新增违规 / 是否放宽 | **已闭合**（`hard_ban=18 / t0_gated=6 / 扫描目标=9` 不变；词表 / 清单 md5 不变；原始命中 30 = 30） |
> | 7 | 硬门（T0 前无量化 / 结果承诺；`pre_t0` 锁定；§6.1 ❌ 登记） | **维持** |

---

## 1. 复审方法与隔离（先读）

| # | 做法 | 说明 |
|---|---|---|
| 1 | **只读副本复跑** | `git archive 6e8ba1a \| tar -x -C $PAPERCLIP_RUN_SCRATCH_DIR/ro`；另取 **父基线对照** `ro-parent`（`8e717bd`）。全部逐字核对与机器复跑都在只读副本内完成；共享工作树 `PRO-1-pro-1` 未被复核过程改写（合规器复跑会就地重写副本内的 `asset-compliance-report.md`），本轮在该分支上**只覆盖本报告一个文件**。 |
| 2 | **先取真实改动面** | `git diff --stat 8e717bd 6e8ba1a` → `docs/08` +89 / `lexicon.json` +3−1 / `naming_test_brief.md` +21−1 / `t0_evidence.template.json` +4−1 / `asset-compliance-report.md` +2−1（共 5 文件、`+110 −9`），与 PRO-27 自述的改动面**逐文件一致**。 |
| 3 | **逐字核对** | 对 `6e8ba1a` 的文件**按行号引用当前文本**（不引用 PRO-27 的回执表格）。 |
| 4 | **能力断言一律回源码 / 上游验** | O-3 的「`HB-17` 未覆盖该措辞」→ 回 `lexicon.json` 的 `pattern` 与 `copy_pack.json` 实文；O-4 的写实 → 回 `docs/05` §0.3 原表；O-2 的判据 → 回 `docs/08` §3.1 / §3.4。 |
| 5 | **机器三项 + 父提交对照** | 在 `ro` 与 `ro-parent` 上分别跑；结论必须是「本基线 = 父基线」才可判定「非本轮引入」。 |
| 6 | **「未放宽」硬证据** | 三份来源（父 / 基线 / 工作树 HEAD）的 `lexicon.json`、`scan_manifest.json` `md5sum`；`compliance.find_violations()` **两轮复算**（无豁免原始命中 vs 豁免后判定）；`docs/08` 的**全量 occurrence 级**原始命中逐行打印（含行号）。 |

---

## 2. 七项复核逐条判定（基线 `6e8ba1a`）

### 2.1 复核项 1 —— O-2（命名测试归因边界）：**已闭合**

| 落点 | 基线现状（`6e8ba1a` 实文） | 判定 |
|---|---|---|
| §1.1 预注册声明 | `naming_test_brief.md` **L22** 新增 `### 1.1 第二批（命名）的自变量清单与不可归因项（PRO-21 O-2 处置）`，L24 明写「本节为**预注册声明**：先写死自变量与判定对象，再看数。不改变任何判据。」L30 自变量 = 「① 名称本身（A1/B1/C1…）；② 该候选在当前相位下唯一可展示的 Slogan（`pre_t0` 臂，见 §2 表）」并标注「同批同时变化 → **不可分离**」；L32 不可归因项 4 项（名称单独效应 / Slogan 单独效应 /「某名称更好」类结论 / **跨子批**比较）；L35 给出「三选一中择 ③『明示不分离』」的理由 | **已闭合** |
| §4 检查项 7 | **L79** 新增「出结论前已核对 §1.1：判定对象是**名称+Slogan 组合**；结论文本中**不含**名称单独效应、Slogan 单独效应或跨子批比较（PRO-21 O-2）」 | **已闭合** |
| §5 末行口径 | **L89** 由「命名子批内 3 个候选差异 < 3pp」改为「命名子批内 3 个候选的**名称+Slogan 组合**差异 < 3pp（组合口径见 §1.1）」，动作列追加「**不得**据此表述为名称单独效应」；**阈值仍为 3pp** | **已闭合** |
| 与 `docs/08` §3.1 / §3.4 是否冲突 | §3.1（`docs/08` L200-205）规定「第二批 = 命名 9 个候选，分 3 个子批（每子批 3 个）… 主判据 `cta_click` 差异 ≥ 3pp」；§3.4（L238-249）预注册判定规则含「命名子批内差异 < 3pp → 记无差异、不得凭偏好选名」+ board 裁定 2 行。§1.1 自述「只补自变量与不可归因项声明，**不改**判据、**不改**样本目标、**不放宽也不收紧** 3pp」——与 §3.1/§3.4 **一致，无冲突**（§1.1 未新增/削减任何判定行） | 一致 |
| 判据与样本目标是否未改 | `naming_test_brief.md` §1 两批设计表（L11-14）**逐字未变**；§5 判定规则表除末行口径措辞外**逐行未变**（留资 ≥ 8% / ≥ 3pp / 每格 ≥ 100 / 样本 500 + MDE 行均原样）。**判据未改、样本目标未改** | **未改** |

> **附带观察（O-1，见 §4）**：§1.1 L37 的括注写作「**不改**样本目标（300–500）」，而第二批的实际样本目标是 §1（L14）登记的「**每子批合计 ≥ 300**」（300–500 是第一批合计）。属**记录精度**问题，非判据变更，不阻断。

### 2.2 复核项 2 —— O-3（`HB-17` 覆盖说明）：**已闭合**

| 项 | 基线现状（`6e8ba1a` 实文） | 判定 |
|---|---|---|
| `notes` 第 4 条落盘 | `lexicon.json` **L64** 新增第 4 条（原 3 条 → **4 条**，diff `+3 −1` 仅此一处）；文内明写该措辞为「**内测意向式**（括号内自带「内测逐步开放」限定，且 sub/bullets/footer_note 三处均写明…），主语是即将逐步开放的能力，**不构成「已上线能力」断言**」 | **已闭合** |
| 裁定 = 不扩围 | 同条明写「**本轮裁定 = 不扩围**（扩围会改变全局扫描结果、可能产生新告警级命中，且须同步改单测基线）」 | **已闭合** |
| 升级路径 | 同条明写「若日后决定扩围覆盖该措辞，**必须同步新增单测并重跑既有单测基线，且不得用豁免掩盖新命中**」 | **已闭合** |
| 事实核查（回源码） | `HB-17.pattern = 多平台一键\|多平台分发\|一键分发\|多个平台各发一份\|被推给谁\|推给谁\|汇总在一张表\|分发结果与数据汇总\|同时发布到所有平台\|全平台自动 ` —— **确不含**「多平台发布」；`copy_pack.json` 实文 `assets.recruit.variants.V3.pre_t0.h1 = 同一份素材，一个入口管理多平台发布（内测逐步开放）`，且 `sub` / `bullets/1` / `footer_note` 均带开放口径限定。故「该措辞不落在 `HB-17` pattern 内、其合规性由内测意向式框架承担」**属实** | 属实 |
| 条目数与扫描目标数未变 | `hard_ban` id 集 = HB-01…HB-18（**18**）；`t0_gated` id 集 = TG-01…TG-06（**6**）；`scan_manifest.targets` = **9**。父 / 基线**完全一致** | **未变** |

> **附带观察（O-2，见 §4）**：`notes` 第 4 条称三处「**均写明「支持清单以开放时的公开说明为准，不预先承诺覆盖范围」**」；实测 `sub` 仅有前半句（「支持清单以开放时的公开说明为准。」），`footer_note` 的用词为「不构成对已可用范围的承诺」。实质结论（三处皆有开放口径限定）成立，措辞精度可收敛。

### 2.3 复核项 3 —— O-4（T0 证据模板写实）：**已闭合**

| 项 | 基线现状（`6e8ba1a` 实文） | 判定 |
|---|---|---|
| `#1` 写实 | `t0_evidence.template.json` **L13**：`{"id": "#1", "name": "最小鉴权 + 限流（docs/05 §0.3 #1）", "status": "pending", "evidence": ""}` —— 上游 `docs/05` §0.3 `#1` 原表为「最小鉴权 + 限流」 | **逐项属实** |
| `#8` 写实 | **L20**：`{"id": "#8", "name": "最小可点上传页 + 首页 feed 页 + dislike/report 最小 API（docs/05 §0.3 #8）", …}` —— 上游 `docs/05` §0.3 `#8` 原表为「最小可点上传页 + 首页 feed 页 + `dislike`/`report` 最小 API」 | **逐项属实** |
| 未改项 | 编号含义、门禁校验范围未动；`status` 仍 `pending`（`#7` 保持 `ready`）；顶层 `preconditions_all_green=false` 与 `notes` 内的 **R-6 限定**（「启动校验只检查证据信封的结构完整性 … 它不校验这八项证据内容的真伪」）**逐字未变** —— diff 仅 2 行 `name` 字段 | **未改** |
| 验收 | `--phase t0` 仍 **exit 3**（含带模板，见 §6） | **维持** |

### 2.4 复核项 4 —— O-11（时效指针）：**已闭合**

| 落点 | 基线现状（`6e8ba1a` 实文） | 判定 |
|---|---|---|
| §12 头部 | **L618** 在「**PRO-20 判定 = 通过（可解锁对外投放，0 阻断级）**」之后补「（**O-11 时效指针（PRO-27）**：该行为 PRO-20 时点的历史判定；其后已被 PRO-23 判定接力覆盖，现行放行状态一律以 **§15.6** 为准。）」 | **已闭合** |
| §13 头部 | **L690** 在「**PRO-20 判定 = 通过，0 阻断级**」括注内补「（**O-11 时效指针（PRO-27）**：时点结论；其后续状态见 §13.4 与 §15.6）」 | **已闭合** |
| §14.6 | **L819** 节首新增`> **⚠️ 时效指针（PRO-27 补注）**：本节第 1 条为 **PRO-23 时点**的放行状态。其后 **PRO-26** 已在基线 `c12aa59` 上出具增量复审**通过（0 阻断级）** → 现行放行状态以 **§15.6** 为准；以下各条保留为该时点记录。` | **已闭合** |
| 失效行专项扫 | `grep -n "可解锁\|可进入对外投放\|可进入上线整合\|放行状态\|不得进入"` 命中 12 处，逐处判读：**唯二**的「可解锁 / 可进入对外投放」陈述（L618 §12 头部、L760 §13.4 的 PRO-20 时点行）**均已带指针或被前置 ⚠️ 补注覆盖**；其余为**条件式门禁句**（「在下一轮增量人工复审出具『通过』之前…仍不得进入对外投放」，L583 / L610 / L682）——条件是「出具通过」，PRO-26 已达成，句子**自我限定、不产生「已解锁」误读**。**无「单读会读到可解锁」的失效行** | **已闭合** |

### 2.5 复核项 5 —— 一致性专项扫：**已闭合**（无自相矛盾；附 O-3 低观察项）

| 检查 | 现状 | 判定 |
|---|---|---|
| §15 是否与实际一致 | §15（L828-905）逐节与实际相符：§15.1 落点 / §15.2 落点与裁定 / §15.3 落点 / §15.4 三处指针 **均能在对应文件按行核到**（见 §2.1–§2.4）；§15.5 回执与本报告 §6 独立复跑**逐字一致** | 一致 |
| §15.6 vs §14.6（已加指针） | §14.6 首条 = 「**PRO-23 判定 = 不通过（1 项阻断级）** → …全部物料仍不得进入对外投放的上线整合」（PRO-23 时点），顶部指针已声明「以下各条保留为该时点记录」；§15.6 = 「**PRO-26 判定 = 通过（0 阻断级，基线 `c12aa59`）** → 可进入…上线整合」。两者**互为时点关系且由指针显式消解**，**不构成自相矛盾** | 相容 |
| §15.6 vs §13.4 | §13.4 有「⚠️ 状态已被 PRO-23 推翻（PRO-25 轮补注）…现行放行状态以 **§14.6** 为准」；§14.6 指针再指向 §15.6 → **两跳后收敛，结论一致** | 相容 |
| §15.6 vs §12.6 | §12.6 为条件式（「出具『通过』之前…不得进入对外投放」），条件已由 PRO-26 达成；§12 头部（L618）指针已把现行状态归口 §15.6 | 相容 |
| §15.6 的 PRO-26 判定 vs 报告头 | `marketing/reports/asset-review-editor.md`（PRO-26 版，于 `8e717bd`）头写明「**判定结论：通过（0 项阻断级 · 可解锁对外投放的上线整合）**」「**复审基线** … commit **`c12aa59`**」——与 §15.6「PRO-26 判定 = 通过（0 阻断级，基线 `c12aa59`）」**逐项一致**；§15 开头「报告提交 `8e717bd`」亦与提交记录一致 | **一致** |
| 交叉事实验证 | board 三问裁定回源码：`GET /api/issues/eeceb59a…/interactions` → 交互 `ba1deb68` `answered`，`answers` = `src_natural` / `pw_directional` / `tight…`，即「零预算自然流量 / 维持 300–500 + 方向性 + 标注低功效 / 确认收紧」——与 §6.2 / §14.4 / §15.6 的复述**一致**；PRO-24 `assigneeId = None`（**未指派，属实**） | 一致 |

> **附带观察（O-3，见 §4）**：§15.6 的「已另发交互请 board 指定承接代理」在本机可查的 issue 交互记录中**未检索到对应项**（PRO-24 / PRO-27 / PRO-21 / PRO-10 的 `interactions` 均为 0 或旧项）。该句与本单 issue 描述**逐字一致**（属上游给定的事实），且不影响放行实质（PRO-24 未指派已独立核实）。

### 2.6 复核项 6 —— 是否新增违规 / 是否放宽：**已闭合（未新增、未放宽）**

| 证据 | 父基线 `8e717bd` | 本基线 `6e8ba1a` | 判定 |
|---|---|---|---|
| `hard_ban` / `t0_gated` / 扫描目标 | 18 / 6 / 9 | 18 / 6 / 9 | **未变** |
| `lexicon.json` `quoted_context_markers` | 12 项（禁止…已删除） | 12 项，**逐项相同** | **未放宽** |
| `lexicon.json` `policy_markers` | 16 项（不做…口径） | 16 项，**逐项相同** | **未放宽** |
| `scan_manifest.json`（md5） | `917f9ee945d2a578238d81c6aba8c6c7` | `917f9ee945d2a578238d81c6aba8c6c7` | **未改 / 未收窄** |
| `lexicon.json`（md5） | `59e3e1e7…` | `3688f19e…`（**仅 `notes` +1 条**，见 §2.2） | 变更**仅限说明行**，不涉词表条目 |
| `docs/08` 无豁免原始命中（全量 occurrence 级，`phase=pre_t0`） | **30** | **30** | **未变** |
| 命中行号集合 | `27,31,94,108,182,255,267,268,272,291,302,344,378,386,460,482,517,520,704,805` | **完全相同** | **相同** |
| 新增区（L819–L907）命中 | — | **0**（`awk 'NR>=819' …` 对 10 个 HB 词零命中） | **未引入新原始命中** |
| 豁免后判定（合规器） | 阻断级 0 / 告警级 0 | 阻断级 0 / 告警级 0 | **本基线 = 父基线 = 0** |

> **归因结论**：本轮 `docs/08` 的新增/修改文本（§12 头部括注、§13 头部括注、§14.6 指针、§15 全节）**未落入任何 HB/TG pattern 的原始命中**；亦未通过「扩豁免 / 放宽 `quoted_context_markers` / 收窄扫描目标」消解任何项。**无新增违规、无放宽动作。**

### 2.7 复核项 7 —— 硬门：**维持**

| 硬门 | 现状 | 判定 |
|---|---|---|
| T0 前不得出现量化承诺 / 结果承诺（含无数字） | §15 全节为**处置记录与状态推进**，无对外的效果/收益/保底承诺；唯一新增数字表述在 §15.6：「300–500 访客的 MDE 为 10–14pp，**不具备 3pp 判定力**」——属**功效限定 + board 裁定复述**（与 §14.4 / §3.4 board 裁定 2 行同源），**不是承诺**；且 §15.6 同段反向重申「`pre_t0` 既有锁定（**T0 前不得出现任何结果承诺，含无数字表述**）」 | 维持 |
| `pre_t0` 锁定 | §15.6「**维持不变**：`pre_t0` 既有锁定…§2.2 渠道 3 的启动前置（供给门）；上线**不得**以 `--phase t0` 启动（T0 证据门禁，exit 3）」 | 维持 |
| §6.1 未实现兑现位登记 | `docs/08` §6.1 各行 ❌ 登记（`exposure` 埋点 / 来源参数归因落库 / 首刷·退出·ground truth·feed 日志 / T0 / 分享页理由渲染位 / 作品页曝光回读位 + 推荐理由回读位 / R6 首发独立路由）**逐条在册**；本轮改动面**未触碰 §6.1** | 维持 |
| 未实现 ≠ 已实现 | 未出现「文案加条件式即视为兑现」的改判；§15.6 仍把 PRO-7 记为 `blocked`、业务判据记为「不可计算」 | 维持 |

---

## 3. 全文通读（除上述落点外）

对本轮 5 个改动文件的**改动面**做一次通读，另对 `docs/08` 的**全量新文本**（L616-907 的相关区块）做一次串读：

1. **改动面之外无溢出改动**：`git diff 8e717bd 6e8ba1a` 的 5 个 hunk 集合与 PRO-27 自述**逐条对应**；`docs/08` 的另 3 处改动（修订记录 L8、§12 头部 L618、§13 头部 L690、§14.6 L819、§15 L828-905、文档结束行 L907）**全部为记录层**，无判据/物料改动。
2. **命名测试说明**：§1.1 为纯增节；§4 为纯增检查项；§5 仅末行口径；§7 版本表加一行。**未删任何既有句**（`git diff` 侧证：`−` 行仅 1 行，为 §5 末行的旧措辞）。
3. **`docs/08` §15 的措辞纪律**：§15 通篇使用「已闭合 / 落点 / 裁定 / 维持不变 / 未做」，未出现「已实现 / 已上线 / 已可用」类现在时断言；对未实现项一律沿用 ❌ 与「不可计算」。
4. **§15.6 与 §15.1–§15.4 的自洽**：§15.6 陈述的「新增人工门禁（§6.1 / §14.3）：首发机制类句子在机制可见前不得用于对外投放」与 §6.1 的 **O-9 补注**（人工门禁、合规器不拦）**同一口径**，未把「人工门禁」误述为「装置已覆盖」。
5. **一处措辞精度（不阻断）**：`docs/08` §15.1 引「样本目标（300–500）」，同 §1.1 —— 口径同源问题，见 O-1。

---

## 4. 观察项（**不阻断**，供下一轮记录维护一并处理）

> 依本包既有口径：**已由上游条款/登记口径授权、改它会动词表/判据/单测基线、或属记录一致性问题**的，列为观察项 + 跟进单，**不阻断放行**；若 board 采更严解读则升级为阻断项并另起一轮。

### O-1（低 · 记录精度）：`naming_test_brief.md` §1.1 的样本目标括注

- **原文**（`naming_test_brief.md` L37）：「本节**只补自变量与不可归因项声明**，**不改**判据、**不改**样本目标（300–500）、**不放宽也不收紧** 3pp」
- **问题**：第二批（命名）的样本目标在同文件 §1（L14）登记为「**每子批合计 ≥ 300**」；「300–500」是**第一批**的合计样本。括注把第一批数字套到第二批语境，属记录精度问题（**不构成判据变更** —— 实测 §1/§5 的表与阈值逐字未改）。
- **建议改法**：将括注改为「不改样本目标（第一批 300–500；命名子批每子批 ≥ 300，见 §1）」，或改为不写数字的「不改样本目标（见 §1 表）」。
- **不阻断理由**：① 纯记录措辞；② 判据与阈值**实测未改**；③ 改它会新起一轮复审。
- **升级条件**：若 board 认定预注册文件中的数字引用必须逐项精确，则升级为阻断项并另起一轮。

### O-2（低 · 记录精度）：`lexicon.json` `notes` 第 4 条的「三处均写明」括注

- **原文**（`lexicon.json` L64）：「…且 `sub` / `bullets` / `footer_note` 三处**均写明「支持清单以开放时的公开说明为准，不预先承诺覆盖范围」**」
- **实测**：`copy_pack.json` `assets.recruit.variants.V3.pre_t0.sub` = 「…内测阶段按平台逐步开放，支持清单以开放时的公开说明为准。」（**仅前半句**）；`bullets/1` = 「发布支持清单以开放时的公开说明为准，不预先承诺覆盖范围」（**完整**）；`footer_note` = 「…支持清单以开放时的公开说明为准，**不构成对已可用范围的承诺**。」（**近义换词**）。
- **建议改法**：改为「三处**均带开放口径限定**（`bullets/1` 为「…不预先承诺覆盖范围」，`sub` / `footer_note` 为同义表述）」。
- **不阻断理由**：实质结论（三处皆有开放口径限定、不构成已上线能力断言）**成立且已回源码核实**；该 `notes` 行为本轮**新增的说明行**，不影响任何扫描判定。
- **升级条件**：同上（board 采更严解读）。

### O-3（低 · 记录一致性）：§13.4 指针一跳 + §15.6「已另发交互」不可核

- **(a)** `docs/08` §13.4（L759）写明「现行放行状态以 **§14.6** 为准」，而 §14.6（L819）又指向 §15.6 → **两跳后才到达现行版本**。建议 §13.4 补注直接写「现行以 §15.6 为准（经 §14.6 指针）」，或把 §14.6 的标题一并读清单补上 §15.6。
- **(b)** §15.6「**仍需 board 路由**：PRO-24 当前**未指派**，**已另发交互**请 board 指定承接代理」——`PRO-24` 未指派已独立核实（`assigneeId = None`）；但「已另发交互」在本机可查的 `interactions` 中**无对应项**（PRO-24 / PRO-27 / PRO-21 / PRO-10 均无）。建议补交互 id，或改为「需 board 指定承接代理（本单另发交互，若未创建则补发）」。
- **不阻断理由**：① 均为记录一致性；② (b) 与本单 issue 描述**逐字一致**（属上游给定的事实，非 PRO-27 自撰）；③ 不影响放行实质（PRO-24 未指派、上线不得 `--phase t0` 均已成立）。
- **升级条件**：同上。

---

## 5. 投放放行结论

**判定：通过（0 项阻断级 · 可解锁对外投放的上线整合）**

| 硬门复核项 | 结论 |
|---|---|
| O-2 / O-3 / O-4 / O-11 逐条 | **已闭合** |
| 新增 §15 与全文一致性 / §15.6 与报告头一致 | **一致，无自相矛盾** |
| 新增违规 / 放宽动作 | **无**（`hard_ban=18 / t0_gated=6 / 扫描目标=9` 不变；词表 / 清单 md5 不变；原始命中 30 = 30、行号集合相同；新增区零命中） |
| 硬门（T0 前承诺 / `pre_t0` 锁定 / §6.1 ❌ 登记） | **维持** |
| 观察项 | **3 项低观察项**（O-1 / O-2 / O-3），**不阻断**；按本包惯例改由**未指派跟进子单**承接，不就地改写 |

**放行陈述（截至 `6e8ba1a`）**

- **PRO-26 判定 = 通过（0 阻断级，基线 `c12aa59`）** 维持有效；本轮窄范围复核对 PRO-27 的处置面**未发现阻断级问题** → 本执行包**可进入上线整合**，承接方 = **Director of Go-to-Market**（**PRO-24**，当前**未指派**）。
- **不得**以 `--phase t0` 直接启动：T0 证据门禁 **exit 3**（含带模板亦 exit 3，见 §6）。T0 当日须提交 `--t0-evidence` 并留痕；门禁（PRO-14 R-6）只校验证据信封的结构完整性，**不校验真伪**。
- **维持**：`pre_t0` 既有锁定；§2.2 渠道 3 启动前置（供给门）；§6.1 全部未实现兑现位；新增人工门禁（首发机制类句子在机制可见前不得对外投放）。
- **业务判据**：全部仍为**不可计算**（需真实访客 + PRO-7 的 T0 埋点；PRO-7 = `blocked`），300–500 访客的 MDE 为 10–14pp、**不具备 3pp 判定力**（board 裁定 2：维持方向性判定 + 每次结论必须标注低功效）。
- **如实限定**：PRO-24（上线整合）当前**未指派**；本复审**不裁定**上线动作本身。

---

## 6. 机器复跑原始输出（本机实际输出，只读副本内）

```text
########## 基线 6e8ba1a（ro）##########
$ python3 marketing/scripts/compliance_lint.py
[compliance-lint] hard_ban=18 t0_gated=6 扫描目标=9
[compliance-lint] 阻断级违规：0，告警级：0
[compliance-lint] 报告：…/ro/marketing/reports/asset-compliance-report.md
exit=0

$ python3 -m unittest discover -s marketing/tests -t .
Ran 42 tests in 21.492s
OK
exit=0

$ python3 marketing/landing/app.py --phase t0 --port 0
[compliance] phase=t0 缺少有效的 T0 前置证据，拒绝启动（exit 3）：
  - 未提供 --t0-evidence（T0 证据文件）
exit=3

$ python3 marketing/landing/app.py --phase t0 --port 0 --t0-evidence marketing/data/t0_evidence.template.json
[compliance] phase=t0 缺少有效的 T0 前置证据，拒绝启动（exit 3）：
  - 证据字段 pro7_delivered 必须为 true（当前：False）
  - 证据字段 preconditions_all_green 必须为 true（当前：False）
  - 证据字段 evidence_date 缺失（须写明证据采集日）
exit=3

########## 父基线对照 8e717bd（ro-parent）##########
$ python3 marketing/scripts/compliance_lint.py
[compliance-lint] hard_ban=18 t0_gated=6 扫描目标=9
[compliance-lint] 阻断级违规：0，告警级：0
exit=0
```

```text
$ md5sum（父 ro-parent / 基线 ro / 工作树 HEAD 三份来源）
marketing/compliance/scan_manifest.json
  917f9ee945d2a578238d81c6aba8c6c7（父 = 基线 = HEAD，三点一致 → 未收窄扫描目标）
marketing/compliance/lexicon.json
  59e3e1e733f81ad068ff686628e8d2a6（父） vs 3688f19e1da15909d322f697b3837b03（基线 = HEAD）
  → 差异仅来自 notes 第 4 条（diff +3 −1），hard_ban / t0_gated 条目与 markers 逐项相同
```

```text
$ 全量 occurrence 级 无豁免原始命中（phase=pre_t0；docs/08-gtm-channel-execution-pack.md）
父基线 8e717bd：30
本基线 6e8ba1a：30
命中行号集合：27,31,94,108,182,255,267,268,272,291,302,344,378,386,460,482,517,520,704,805  → 父 = 基线，完全相同
新增区（L819 之后）：0 命中
```

> **读数限定（不得省略）**：机器复跑只证明「本单基线未出现**可判定**违规」。词表仍是正则匹配，**语义类表述仍可能漏检**（§4.4 #6 / §9.5 #2）；反之引文行也可能因缺引用标记而**误报**（PRO-23 的 N-1 属后者）。两类盲区都需人工复审兜底，这也是本报告存在的原因。

> **自洽性检查**：`marketing/reports/asset-compliance-report.md` 提交版本的时间戳为 `2026-09-24T14:53:32+00:00`；基线 `6e8ba1a` 的提交时间为 `2026-09-24 22:54:09 +0800`（= `14:54:09 UTC`）——报告生成**早于提交 37 秒**，属「复跑后提交」的正常顺序；该文件 diff **仅时间戳一行**，物料未变。

---

## 7. 边界与「未做」（如实记录）

1. **交付物 = 判定**，不是改稿：本报告**未改写任何文案**，**未改** `docs/05` / `docs/06` / `docs/07`，**未改** `lexicon.json` / `scan_manifest.json` / 任何 `marketing/` 物料与判据，**未重跑**物料生成脚本，**未新增数字或承诺等级**。
2. **未改** `docs/08`：§4 的三项低观察项**只在本报告内提出**（按窄范围边界，观察项由跟进单承接）。
3. **未重开** PRO-12 / PRO-13 / PRO-14 / PRO-15 / PRO-16 / PRO-17 / PRO-18 / PRO-19 / PRO-20 / PRO-22 / PRO-23 / PRO-25 / PRO-26 的既有判定；本轮**只复核 PRO-27 的改动面**。
4. **未做**：人工通读物料层（本单为窄范围复核，`marketing/landing/` 与 `marketing/data/` 的对外物料本轮未变，故沿用 PRO-26 的物料层结论）；商标 / 域名初筛；对外部署；上线整合动作的裁定。
5. **未用豁免掩盖真实违规**：§6 的「md5 + 两轮复算 + 行号集合」只在**只读副本**内进行，共享分支未被写入；本报告**明确禁止**以「扩豁免 / 放宽 `quoted_context_markers` / 收窄扫描目标」消解任何项（§2.6 已核无此类动作）。
6. 本报告在共享分支 `PRO-1-pro-1` **就地覆盖**，`git add` 仅本文件；**不写入本轮的自身提交哈希**（延续 §12 的哈希口径）；本轮提交与审查回执见 PRO-28 评论。

---

*报告结束 · Editor（人工复审）· PRO-28 增量复审（窄范围）· 复审基线 `6e8ba1a` · 判定：**通过**（0 项阻断级；O-2 / O-3 / O-4 / O-11 逐条已闭合；一致性 / 计数与放宽 / 硬门 逐项已核；另附 O-1 / O-2 / O-3 三项低观察项，不阻断）*
