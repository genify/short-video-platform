# 投放物料增量人工复审报告（Editor）

> **复审基线**：分支 `PRO-1-pro-1` · commit **`c12aa59`**（= PRO-25 返修提交；父 `b2bd459` = PRO-23 报告提交，再上 `3f80884` = PRO-22 修订提交、`d43faaa` = PRO-20 报告提交）
> **相位**：`pre_t0`（T0 = MVP 可灰度日 = PRO-7 交付；复核时 PRO-7 看板状态仍为 `blocked`）
> **本轮**：PRO-26「[P2-GTM-执行] 增量人工复审（窄范围）：PRO-25 返修 —— N-1 装置门禁修复 / N-1b 回执重写 / O-9·O-10 处置 / §14 裁定落地」——**只复核 PRO-25 的改动面（1 个文件 `docs/08`，`+95 −19`），不重开 PRO-12 ~ PRO-23 的既有判定**
> **报告版本约定**：本文件**就地覆盖、唯一现行版本、不绑定轮次**；版本与复审基线以本文件头本行为准；各轮结论摘要见对应 issue 评论。
> **判定结论**：**通过（0 项阻断级 · 可解锁对外投放的上线整合）**。PRO-25 的 1 项阻断级（N-1）+ 记录完整性项（N-1b）+ 4 项观察项（O-9 / O-10①②③）**逐条已闭合**；本轮另附 **1 项低观察项（O-11，记录一致性）**，**不阻断**。
>
> **八项复核速览**
>
> | # | 复核项 | 结论 |
> |---|---|---|
> | 1 | N-1 装置门禁击穿（引文行补标记 + 引用逐字保留 + 复跑回绿） | **已闭合** |
> | 2 | N-1b §13.3 三段式重写 + 与 PRO-22 回执口径一致 | **已闭合** |
> | 3 | O-9「人工门禁，装置不覆盖」补注（不动词表 / 单测） | **已闭合** |
> | 4 | O-10① 脚本出处更正（脚本 A 正文 ② / 脚本 C 正文） | **已闭合** |
> | 5 | O-10② §2.3 第 2 条补第三个 ❌ 兑现位 + 两套处置显式化 | **已闭合** |
> | 6 | O-10③ §12 头部改「所在 issue + 评论 id」 | **已闭合** |
> | 7 | §14（14.1~14.6）与实际一致 + §13.4 补注 + 自相矛盾专项扫 | **已闭合**（另附 O-11 观察项） |
> | 8 | 计数不变 / 无新增违规 / 无放宽动作 | **已闭合** |

---

## 1. 复审方法与隔离（先读）

| # | 做法 | 说明 |
|---|---|---|
| 1 | **只读副本复跑** | `git archive c12aa59 \| tar -x -C $PAPERCLIP_RUN_SCRATCH_DIR/ro`；另取 **父基线对照** `ro-parent`（`b2bd459`）与 **上上基线对照** `ro-d43`（`d43faaa`，用于核对 §14.1 的「父提交为 0」）。**全部逐字核对与机器复跑都在只读副本内完成**；共享工作树 `PRO-1-pro-1` 未被复核过程改写（合规器复跑会就地重写副本内的 `asset-compliance-report.md`），本轮在该分支上**只覆盖本报告一个文件**。 |
| 2 | **先取真实改动面** | `git diff --stat b2bd459 c12aa59` + `git diff --name-only b2bd459 c12aa59 -- marketing/`（原始输出见下）。 |
| 3 | **逐字核对** | 对 `c12aa59` 的 `docs/08` 按**行号引用当前文本**（不引用 PRO-25 的回执表格）。 |
| 4 | **机器复跑独立执行 + 三点对照** | 三条命令在只读副本内独立跑通，且**在 `b2bd459` 与 `d43faaa` 上各再跑一遍合规器**——这是判定「违规是否由本单引入 / 是否真的修复」的关键对照；未采信 PRO-25 评论里的表格数字。 |
| 5 | **豁免路径逐行独立复算** | 不依赖合规器的报告表，直接调 `marketing/landing/compliance.py::find_violations()` 对 `docs/08` 做「无豁免原始命中」与「带标记豁免后判定」两轮复算（见 §3）。 |
| 6 | **能力断言与裁定来源回外部事实验** | 复核 §14.4 的 board 裁定 → 实查 PRO-10 交互 `ba1deb68`（C 面 API）；复核 §6.1 引用物名 → 实查 `marketing/data/community_scripts.md` L45 / L86；复核 O-9 → 实查 `marketing/tests/test_compliance.py` 反证清单。 |

**实际改动面（`git diff --stat b2bd459 c12aa59`，原始输出）**

```
 docs/08-gtm-channel-execution-pack.md | 114 +++++++++++++++++++++++++++++++++-----
 1 file changed, 95 insertions(+), 19 deletions(-)
```

**「未改物料 / 词表 / 清单 / 单测基线 / docs/05~07」的独立核验**

- `git diff --name-only b2bd459 c12aa59 -- marketing/` → **空输出**（`marketing/` 下无任何文件变化）。
- `git diff --name-only b2bd459 c12aa59` → 仅 `docs/08-gtm-channel-execution-pack.md`。
- 词表与清单跨三点基线 **md5 逐字一致**（`lexicon.json` = `59e3e1e7…`；`scan_manifest.json` = `917f9ee9…`）。

**合规报告时间戳口径（如实说明）**：`marketing/reports/asset-compliance-report.md` 是**版本受控文件**，在 `c12aa59` 的「生成时间（UTC）」仍为 `2026-09-23T17:33:37+00:00`（属 `b2b372e` 轮），**不等于**本轮基线时间。原因：本轮**未改 `marketing/`**，故物料层命中数与豁免数未变、无需重跑生成脚本——与 §14.5 的自述一致，**不属失准**（`git diff b2bd459 c12aa59 -- <该报告>` 为空输出）。

---

## 2. 八项复核逐条判定（基线 `c12aa59`）

### 2.1 复核项 1 —— N-1（阻断级 · 装置门禁击穿）：**已闭合**

当前文本（`docs/08` **L704**，逐字；修订点用 **粗体** 标出）：

> `| 上游登记（`docs/04`） | （**引用上游禁令原文；本包物料不使用该表述** —— **N-1 修正**：本行原不含引用语境标记，在 `3f80884` 上被合规器判为阻断级违规 1，现补标记走豁免路径，引用内容逐字保留）F-5：「新视频保底曝光未实现（**P0**）…当前实现**无此机制**」；D14：「R6 新视频保底路由 **❌ 不存在** … **需新增** **P0**」 |`

| 核对点 | 实测 | 结论 |
|---|---|---|
| 补引用语境标记 | ✅ 行首含「引用上游禁令原文；本包物料不使用该表述」——含豁免词 **`不使用`**（另含 `违规`、`豁免`，三条任一条即可） | 到位 |
| **引用内容逐字保留** | ✅ 与 `b2bd459` L703 **逐字一致**（F-5 / D14 引文未删、未改写） | 到位 |
| 复跑回绿 | ✅ `c12aa59` = **阻断级 0 / exit 0** | 到位 |
| 父基线对照（复现） | ✅ `b2bd459` = **阻断级 1（HB-01 @ docs/08 :: 保底曝光）/ exit 1** | 与工单预期一致 |
| 上上基线对照 | ✅ `d43faaa` = **阻断级 0 / exit 0**（证明该违规确由 PRO-22 引入、由 PRO-25 消除） | 与工单预期一致 |
| 未放宽 | ✅ 未改 `quoted_context_markers`、未新增豁免条目、未收窄扫描目标（md5 一致，见 §1） | 到位 |

**豁免路径逐行复算（独立于合规器报告表）**：全文件 `保底曝光`（HB-01）命中 **3 处** → L255（含 `D 级` → 豁免）、L378（含 `不得`/`违规`/`口径` → 豁免）、**L704（含 `不使用`/`违规`/`豁免` → 豁免）**。无豁免原始命中仍为 **8** 条（与 `b2bd459` 相同，**未新增原始命中**），带标记豁免后判定 **0**。
→ 该项由「**机器可验的门禁击穿**」恢复为「**机器可验的通过**」；`docs/08` §5 L343 的硬门自评行「已满足（机器可验）｜合规器 0 阻断级违规」**在当前基线重新成立**。

**结论：已闭合。**

### 2.2 复核项 2 —— N-1b（记录完整性）：**已闭合**

当前文本（`docs/08` **§13.3，L729–L756**）：标题已改为「机器复跑回执（**PRO-25 轮按「复现 → 修订 → 复跑」三段重写**；均在 tar 快照内执行）」。

| 核对点 | 实测 | 结论 |
|---|---|---|
| 三段结构 | ✅ 「**① 返修前**（基线 `3f80884` / `b2bd459`）—— 复现 PRO-23 判定」→「**② 返修后**（§14.1 补引用语境标记）」+ 单测段 | 到位 |
| 真实 exit code | ✅ ① `exit=1`；② `exit=0`；单测 `Ran 42 tests — OK (exit=0)` | 到位 |
| 差异成因 | ✅ 明写「L703 是最后一处新增引文行，**提交前未再跑合规器**；此前各轮的「0/0」在其**当时基线**上成立，属「未在最终基线上复核」，非历史伪造」 | 到位 |
| 与 PRO-22 回执口径一致 | ✅ §13.3 末句「本节的图与 PRO-22 回执中的同一行，**均以本节为准**」——以显式声明把失实回执收口到本节；PRO-22 的 issue 评论（对话记录，不可改写）保留为历史 | 到位（口径一致） |
| 与本节实跑一致 | ✅ 本轮独立复跑与 PRO-25 的三段数值**完全一致**（1 / 0 / 0） | 到位 |

**结论：已闭合。**（说明：PRO-22 的 **issue 评论文本**本身仍是旧值且不可编辑；本轮以「文档 §13.3 + PRO-23/PRO-26 判定」为唯一现行口径，与 §13.3 的声明一致。）

### 2.3 复核项 3 —— O-9（人工门禁 + 装置不覆盖）：**已闭合**

当前文本（`docs/08` **§6.1，L378** 末尾新增，逐字）：

> `**门禁性质（O-9 补注）**：本使用前置是**人工门禁** —— `lexicon.json` 未扩围（HB-17 未覆盖该类断言）、`tests/test_compliance.py` 反证清单仍把「首发走独立路由」列为**不得报违规**，故**合规器不会拦该句**，须由人工通读执行（见 §13.1 / §14.3）。`

| 核对点 | 实测 | 结论 |
|---|---|---|
| 「人工门禁，装置不覆盖」已注明 | ✅ 三要素齐备（人工门禁 / 装置不覆盖 / 须人工通读执行） | 到位 |
| 词表未扩围的自述属实 | ✅ `lexicon.json` md5 跨三点基线一致；HB-17 未覆盖该类断言 | 到位 |
| 单测反证清单放行该句属实 | ✅ 独立复核 `marketing/tests/test_compliance.py` **L194–195**：`for ok in ("规则写出来，你自己核对", "每条理由都写出来", "首发走独立路由", "先育苗，再谈赛道")` → 断言**不得报违规** | 到位 |
| **未**为此改词表或单测基线 | ✅ `marketing/` 零改动 | 到位 |

**结论：已闭合。**（本轮**不**主张把该句移出反证清单——那属判据层收紧，须单独派单 + board 口径，沿用 PRO-23 §5 的「不升级」裁定。）

### 2.4 复核项 4 —— O-10①（脚本出处更正）：**已闭合**

当前文本（`docs/08` **§6.1，L378** 兑现位列，逐字节选）：

> `…`community_scripts.md` 脚本 A **正文 ②**「首发那条走了独立路由」、脚本 C **正文**「把首发的曝光规则写成产品里的一条硬约束」（**O-10① 更正**：原标「脚本 A ①」「脚本 B」有误，已按 `community_scripts.md` L45 / L86 逐行核对）…`

| 核对点 | 实测 | 结论 |
|---|---|---|
| 出处改为「脚本 A 正文 ②」 | ✅ `community_scripts.md` **L45** = `② 首发那条走了独立路由，没有和一堆内容挤在一起；`，该行位于「### 脚本 A｜food-home」（L36）正文的**第 ② 条** | 到位 |
| 出处改为「脚本 C 正文」 | ✅ `community_scripts.md` **L86** = `在一个内测社区里，他们把首发的曝光规则写成产品里的一条硬约束（规则开放时会公开原文），`，位于「### 脚本 C｜local-life」（L78）正文 | 到位 |
| 残留错标 | ✅ 全文仅剩 1 处「脚本 A ①」字样，位于**更正说明自身**（「原标…有误」），属如实留痕；PRO-19 记录的「脚本 A ①（L44）」（§9.2 L-5 / §12.1）指**物料侧行号**，与 §6.1 的**出处键名**语境不同，非同类缺陷 | 到位 |

**结论：已闭合。**

### 2.5 复核项 5 —— O-10②（§2.3 第 2 条两套处置显式化）：**已闭合**

当前文本（`docs/08` **§2.3 第 2 条，L182** 括注，逐字节选）：

> `② **引用本包 §6.1 登记为 ❌ 未实现的兑现位**（作品页曝光回读位 / 推荐理由回读位 / **首发独立路由（R6 `newcomer`）与「首发曝光写成产品硬约束」**——第三项为本轮按 §13.1 同步）时，**必须加条件式「随功能开放」**，且第三项另受 §6.1 **使用前置**约束（**人工门禁，装置不覆盖**；见 §13.1 / §14.3）…`

| 核对点 | 实测 | 结论 |
|---|---|---|
| 补入**第三个** ❌ 兑现位 | ✅ 三个并列（曝光回读位 / 理由回读位 / 首发独立路由与「首发曝光写成产品硬约束」） | 到位 |
| 指明其另受 §6.1 **使用前置**约束 | ✅ 明写「另受 §6.1 **使用前置**约束（**人工门禁，装置不覆盖**）」 | 到位 |
| 两套处置关系**显式化** | ✅ 在同一句内给出判定树：①/② 两类合法形式 + 第二类必须加条件式 + 第三项叠加使用前置；与 §6.1 L378、§13.1 L708–L720 **三处指向同一判定** | 到位 |

**结论：已闭合。**

### 2.6 复核项 6 —— O-10③（评论 id 归属口径）：**已闭合**

当前文本（`docs/08` **§12 头部，L618**，逐字）：

> `…各轮结论摘要见对应 **issue + 评论 id** —— PRO-18 放行通知：**PRO-10** `2b08f67c-f99b-4810-aef8-567d7574dc1c`；PRO-20 判定：**PRO-20** `fcd51b40-8423-4cd0-9282-ed076c67b4b7`（**O-10③ 口径统一**：不再只写评论 id）。`

| 核对点 | 实测 | 结论 |
|---|---|---|
| 改为「所在 issue + 评论 id」 | ✅ 两个 id 均带 issue 归属（且与 PRO-23 §2.2 的独立核对结论一致：`2b08f67c…` 在 PRO-10、`fcd51b40…` 在 PRO-20） | 到位 |
| 未遗留无归属的裸 id | ✅ §12 本节内已无裸 id 表述（§13 头部 L690 保留的 `fcd51b40…` 属 PRO-20 节语境，其 issue 在 §12 已明示） | 到位 |

**结论：已闭合。**

### 2.7 复核项 7 —— 新增 §14 与全文一致性：**已闭合**（另附观察项 O-11，不阻断）

**§14（L768–L826）实际一致性核对**

| 子项 | 实测 | 结论 |
|---|---|---|
| §14 头部（判定依据 / 处置原则 / 修订基线 / 哈希口径） | ✅ 判定依据指向报告文件 + **PRO-23** 评论 `b09acb33-…`；处置原则含「只改 `docs/08`」「未以扩豁免/放宽引用标记/收窄扫描目标消解」；修订基线 = `b2bd459`；不写自指哈希 | ✅ 与实际一致 |
| §14.1 N-1 修正记录 | ✅ 命中行、成因（`quoted` 只豁免同一行含标记）、改法（方案 A）、三项计数不变——**与 §2.1 独立复算完全一致** | ✅ |
| §14.2 N-1b 记录 | ✅ 与 §13.3 实际文本一致 | ✅ |
| §14.3 O-9 / O-10①②③ 处置表 | ✅ 四项落点与 §2.3–§2.6 的实际文本**逐项对得上** | ✅ |
| §14.4 board 三项裁定 | ✅ **实查 PRO-10 交互 `ba1deb68`（`ask_user_questions`，`status=answered`，`resolvedAt=2026-09-24T14:37:37Z`）**：`q1_traffic_source → src_natural`（零预算自然流量）、`q2_3pp_power → pw_directional`（维持 300–500 + 方向性 + 每次结论写 low power，且选项文案即「10 to 14pp」）、`q3_pre_t0_promise → tight_confirm`（确认收紧）——**三条与 §14.4 表格逐字对应** | ✅ 与实际一致 |
| §14.5 回执指向 §13.3 | ✅ 一致 | ✅ |
| §14.6 放行状态 | ✅ 与 PRO-23 判定（不通过）一致；PRO-24 不启动的理由（前置未成立）与 §14.4 裁定 1（仅解除访客来源前置）**逻辑相容、无互斥** | ✅ |

**§13.4 补注核对（L757–L759）**：标题已扩链为「必须与 §9.5 #1、§10.2、§12.6、**§14.6** 一并读」；L759 新增
> `**⚠️ 状态已被 PRO-23 推翻（PRO-25 轮补注）**：PRO-23 增量复审判定 **不通过（1 项阻断级）** → 本节原「可进入上线整合」结论**暂停生效**；现行放行状态以 **§14.6** 为准，`pre_t0` 锁定维持。以下各条为 PRO-22 时点的历史记录。`

→ 已补注 ✅，且把本节后续各条明确定义为**历史记录**。

**放行状态类缺陷专项扫（全文）**：逐节核对 §0.1 相位表 / §3.4 / §4.3 / §4.4 / §5 L343 / §6.2 / §6.3 / §9.5 #1 / §10.2 / §12 头部 / §12.6 / §13 头部 / §13.4 / §14.4 / §14.6：

| 位置 | 表述 | 与「PRO-23 = 不通过 / 物料不得进入上线整合」是否冲突 |
|---|---|---|
| §9.5 #1 / §10.2 / §12.6 | 「在下一轮增量人工复审出具『通过』之前，全部物料仍不得进入对外投放」 | **不冲突**（锁定式表述，与当前状态同向） |
| §13.4 L760（历史条） | 「PRO-20 判定 = 通过 → 可进入上线整合」 | **不冲突**（L759 已显式定义为「PRO-22 时点的历史记录」，并指向 §14.6） |
| §14.6 | 「PRO-23 判定 = 不通过 → 全部物料仍不得进入上线整合」 | 现行权威，无冲突 |
| §12 头部 **L618** / §13 头部 **L690** | 「**PRO-20 判定 = 通过（可解锁对外投放，0 阻断级）**」 | **无「时效指针」**：单读该行会读到「可解锁」。见 **§4 观察项 O-11**（低 · 记录一致性，**不阻断**） |

**结论：已闭合**——§14 内容与实际一致；§13.4 补注到位；**未发现新的实质互斥**。唯一残项为 O-11（记录一致性），其性质与本轮改动无关（`b2bd459` 即已存在，且 PRO-23 已对同两行判「已闭合 / 达标」，见 PRO-23 §2.2、§2.4），故**不构成本轮阻断项**。

### 2.8 复核项 8 —— 是否新增违规 / 是否放宽：**已闭合**

| 检查 | 实测 | 结论 |
|---|---|---|
| `hard_ban` | **18**（`lexicon.json` 实读；与 `b2bd459` / `d43faaa` md5 一致） | 不变 ✅ |
| `t0_gated` | **6**（同上） | 不变 ✅ |
| 扫描目标 | **9**（`scan_manifest.json` 实读，T-01~T-09） | 不变 ✅ |
| `quoted_context_markers` | 12 条，逐字未变（`禁止/不得/黑名单/红线/驳回/不予/不使用/不出现/D 级/违规/旧口径/已删除`） | **未放宽** ✅ |
| `policy_markers` | 17 条，逐字未变 | **未放宽** ✅ |
| 扫描目标收窄 | 无（未从任何目标移出文件 / 未改 `mode` / 未改 `phase`） | **未收窄** ✅ |
| 新增原始命中 | T-06 无豁免原始命中 = **8**（与 `b2bd459` 相同）→ `+95` 行**未新增**任何原始命中 | 无新增 ✅ |
| 新增可判定违规 | `c12aa59` = **阻断级 0 / 告警级 0** | 无新增 ✅ |
| 放宽动作判定 | **无**「扩豁免（新增豁免条目）/ 放宽 `quoted_context_markers` / 收窄扫描目标」三类动作。本轮唯一与豁免相关的变化是**给一条既有引文行补该行自身的引用语境标记**——即 PRO-23 §4 明确指定的**改法 A**，属词表外的**逐行**豁免路径，不是放宽词表或清单 | 无放宽 ✅ |

**结论：已闭合。**

---

## 3. 全文通读（除上述落点外）

1. **物料层通读结论沿用 PRO-20，本轮不重跑物料**：`marketing/` 在本单基线**零改动**（§1 核验），故 §5 硬门表的「T0 前量化承诺 / 结果承诺」等物料层结论**不发生漂移**；本轮 `docs/08` 的全部 `+95` 行均为**控制面记录**（处置记录、裁定落地、状态指针），**不含任何对外文案**。
2. **相位与裁定落地一致性**：§0.1 相位表 `pre_t0` 禁止列补注「board 裁定 3 已确认，PRO-25 轮」；§3.4 新增「任何情形」强制标注行（低功效 / MDE 10–14pp，且不得在低功效样本上宣称 3pp）；§4.3「需 board 确认」→「board 裁定：已确认」；§4.4 #1/#2 与 §6.2 #1/#2/#3 标为「已裁定」并附如实代价。**四处与 §14.4 的裁定逐条同向**。
3. **数字纪律复核**：`docs/08` 的新增行中出现的量化值仅为**判据参数与功效说明**（300–500 访客、10–14pp、4,500、3pp 等），**均属内部判据/口径，不是对外承诺**；对外物料零数字的结论未变。**未新增数字或承诺等级**。
4. **未实现兑现位登记完整性**：§6.1 现有登记行 = 分享页理由渲染位 / 作品页曝光回读位 + 推荐理由回读位 / 首发独立路由与「首发曝光写成产品硬约束」，**四类均带 ❌ 与限定**，且第三类带**使用前置（人工门禁）**；与 §2.3、§13.1 三处口径一致。
5. **未发现新的措辞级违规**：全文对 `docs/08` 的 `quoted` 模式复算见 §2.1；无豁免判定 0。

---

## 4. 观察项（**不阻断**，供下一轮记录维护一并处理）

### O-11（低 · 记录一致性）：§12 / §13 头部仍留「PRO-20 判定 = 通过（可解锁对外投放）」且无时效指针

**原文（`c12aa59` 逐字）**

- `docs/08` **L618**（§12 头部）：
  > `…（**O-10③ 口径统一**：不再只写评论 id）。**PRO-20 判定 = 通过（可解锁对外投放，0 阻断级）**。`
- `docs/08` **L690**（§13 头部）：
  > `> 判定依据：`marketing/reports/asset-review-editor.md`（就地覆盖、唯一现行版本、不绑定轮次；**PRO-20 判定 = 通过，0 阻断级**）…`

**问题**：两行都在陈述 PRO-20 的**历史判定**，但**未附时效指针**。文档现行放行状态是 §14.6（PRO-23 = 不通过 → 物料不得进入上线整合），而 §13.4 已按本轮同步补了「状态已被 PRO-23 推翻」补注——同类的两行**未同步**，因此单读 §12 / §13 头部会读到「可解锁对外投放」。

**改法（择一，均为记录层，不动判据）**

- 在 L618 与 L690 的 PRO-20 判定括注后各补一句：
  `（**该判定其后已被 PRO-23 推翻；现行放行状态见 §14.6**）`
- 或把两行的措辞统一改为**已带指针的引用式**：
  `…**PRO-20 判定 = 通过**（时点结论；其后续状态见 §13.4 / §14.6）…`

**为何**不**阻断（判定依据）**

| # | 理由 |
|---|---|
| ① | 两行均为**对已发生判定的时点陈述**（PRO-20 = 通过是事实），不是**新的**放行主张；同节内的 §12.6 已写「在下一轮复审出具『通过』之前，全部物料仍不得进入对外投放」，与 §14.6 同向。 |
| ② | **非本轮引入**：两行文本在 `b2bd459` 已存在（`git diff` 所示，本轮仅改了同行的评论 id 归属与报告版本措辞）。 |
| ③ | **已由 PRO-23 复核并判「已闭合 / 达标」**：PRO-23 §2.2 逐字引用了含「（可解锁对外投放，0 阻断级）」的同一行并判落点 2「已闭合」；PRO-23 §2.4 亦核过 §13 头部而未列缺陷。窄范围复审**不重开**该判定。 |
| ④ | 属**记录一致性**类（版本行 / 状态指针），**不触及**任何对外物料、词表、判据、`scan_manifest.json` 或单测基线；按本包既定口径（PRO-20 O-8 / PRO-22 §13.2 同类处置）列**观察项 + 跟进单**。 |
| ⑤ | 若就地改它，`docs/08` 即产生新 commit → 按 §9.5 #1 须**再起一轮增量复审**；为两行指针升级为阻断项会形成无收益的复审循环，与本包「不新增数字、不放宽、不收紧」的稳定口径相悖。 |

**升级条件（如实登记，不得省略）**：若 board 采**更严解读**（要求控制面文档中任何「可解锁」字样必须带时效指针，或要求本轮先行就地补注），则 O-11 **升级为阻断项**，须就地修订 §12 / §13 头部并**另起一轮复审**。**本报告不做该升级判定。**

**另附（本轮通过后的同类记录项，供下一轮记录维护一并处理）**：本判定为**通过**后，§14.6 第一条（「PRO-23 判定 = 不通过 → 全部物料仍不得进入上线整合」）将随之成为**历史条**；沿用本包既有轮次惯例（PRO-20 通过时亦未就地改写相邻轮次的状态节），建议在**下一轮处置 / 记录维护**时给 §14.6 补一句时效指针（方式同 O-11），并同上不阻断。

---

## 5. 投放放行结论

**判定：通过 —— 本执行包在当前基线（`c12aa59`）**可**进入对外投放的上线整合（Owner = Director of Go-to-Market）。**

| 硬门 / 检查项 | 当前基线实测 | 结论 |
|---|---|---|
| 机器自检：阻断级违规 | **0**（exit 0）；父 `b2bd459` = 1（exit 1）、上上 `d43faaa` = 0 | ✅ **满足**（N-1 修复已消除唯一违规） |
| 机器自检：告警级（必含项缺失） | **0** | ✅ |
| 单测基线 | `Ran 42 tests — OK`（exit 0） | ✅ |
| T0 证据门禁 | `--phase t0` → **exit 3**（无证据 / 带模板均 exit 3） | ✅（上线**不得**以 `--phase t0` 启动） |
| T0 前量化承诺（三个数字） | 物料未变，沿用 PRO-20 物料层通读结论；`pre_t0` 物料零数字 | ✅（结论未变） |
| T0 前结果承诺（含无数字） | 未发现；board 裁定 3 已落地（§0.1 / §4.3 / §6.2 / §14.4） | ✅ |
| 未实现兑现位的现在时断言 | §6.1 四类登记 ❌ + 使用前置（**人工门禁，装置不覆盖**，O-9 已补注）；不升级为更严解读（沿用 PRO-23 §5 裁定） | 维持（非阻断） |
| 计数 / 词表 / 清单 | `hard_ban=18 / t0_gated=6 / 扫描目标=9`｜`lexicon.json`、`scan_manifest.json` md5 跨三点基线一致 | ✅ 未变 |
| 是否存在放宽动作 | 无（未扩豁免、未放宽 `quoted_context_markers`、未收窄扫描目标） | ✅ |
| **放行解锁** | 放行判定权在 Editor；**本单基线予以解锁** | ✅ **通过** |

**路由与后果（必须按此执行）**

1. **PRO-24（上线整合移交，Owner = Director of Go-to-Market）启动前置已成立**：本轮「通过」即 §14.6 第二、三条所要求的前置。**但**：① 上线**不得**以 `--phase t0` 启动（T0 证据门禁，exit 3）；② T0 当日须提交 `--t0-evidence` 证据文件并留痕；③ 「零预算自然流量 + 每次结论标注低功效」两项 board 裁定（§14.4）对实测全程有效。
2. **`pre_t0` 相位锁定保持不变**：本轮解锁的是**投放放行（上线整合）**，**不是**相位。T0 相位物料（承诺卡、t0 臂文案、三个承诺数字）的解锁仍取决于 **PRO-7 交付 + §0.3 八项前置全绿**（PRO-7 看板仍为 `blocked`）。
3. **不创建返修子单**：本单 0 阻断级，无需返修。
4. **观察项 O-11 路由**：另建**未指派**跟进子单（描述内写明目标 Owner = **Marketing Strategist**），范围仅限 `docs/08` §12 / §13 头部的时效指针补注，**不阻断放行**，且**不得**借机改动物料 / 词表 / `scan_manifest.json` / `tests/` 基线 / `docs/05`~`docs/07`。
5. **返修后复审口径**：若后续任何轮次改动 `docs/08` 或 `marketing/`，按 §9.5 #1 须**按新 commit 重跑本复审**。

---

## 6. 机器复跑原始输出（本机实际输出，只读副本内）

**基线 `c12aa59`（只读副本 `ro`）**

```
$ python3 marketing/scripts/compliance_lint.py
[compliance-lint] hard_ban=18 t0_gated=6 扫描目标=9
[compliance-lint] 阻断级违规：0，告警级：0
[compliance-lint] 报告：<只读副本>/marketing/reports/asset-compliance-report.md
exit=0
```

```
$ python3 -m unittest discover -s marketing/tests -t .
Ran 42 tests in 17.472s

OK
[参考比对] dim=49(version=8) ref_mask=[2] auto_mask=6 一致=False | 样本=https://lcnn.example.com/?utm_source=douyin&utm_…
[参考比对] dim=69(version=13) ref_mask=[2] auto_mask=2 一致=True | 样本=https://lcnn.example.com/?utm_source=wechat&utm_…
exit=0
```

```
$ python3 marketing/landing/app.py --phase t0 --port 0
[compliance] phase=t0 缺少有效的 T0 前置证据，拒绝启动（exit 3）：
  - 未提供 --t0-evidence（T0 证据文件）
  说明：T0 = MVP 可灰度日（PRO-7 交付 + §0.3 八项前置全绿）。
  模板：marketing/data/t0_evidence.template.json
exit=3
```

**父基线对照 `b2bd459`（只读副本 `ro-parent`）**

```
$ python3 marketing/scripts/compliance_lint.py
[compliance-lint] hard_ban=18 t0_gated=6 扫描目标=9
[compliance-lint] 阻断级违规：1，告警级：0
  - [hard_ban] HB-01 @ docs/08-gtm-channel-execution-pack.md :: 保底曝光
[compliance-lint] 报告：<只读副本>/marketing/reports/asset-compliance-report.md
exit=1
```

**上上基线对照 `d43faaa`（只读副本 `ro-d43`）**

```
$ python3 marketing/scripts/compliance_lint.py
[compliance-lint] hard_ban=18 t0_gated=6 扫描目标=9
[compliance-lint] 阻断级违规：0，告警级：0
exit=0
```

**合规器生成的报告（`c12aa59` 轮）关键行（原始输出）**

```
| T-06 | `docs/08-gtm-channel-execution-pack.md` | quoted | ✅ 通过 | 8 |
| | ↳ 原始命中明细 | | | HB-01:保底曝光、HB-08:站内关系链、HB-09:算法更准、HB-14:对手不透明、HB-15:买量、HB-16:比别的平台、HB-17:被推给谁、HB-18:曝光下限 |
- 阻断级违规（带标记词豁免后的判定，即真正会进入投放的表述）：**0**
- 判定：**通过（可投放）**
```

**豁免路径逐行独立复算（不依赖报告表）**

```
[c12aa59] raw hits=8  exempted-judgement hits=0
  L255: ctx_markers=['D 级'] policy_markers=[]
  L378: ctx_markers=['不得','违规'] policy_markers=['口径']
  L704: ctx_markers=['不使用','违规'] policy_markers=['豁免']      ← N-1 新增标记所在行
[b2bd459] raw hits=8  exempted-judgement hits=1  → [hard_ban] HB-01 :: 保底曝光
[d43faaa] raw hits=8  exempted-judgement hits=0
```

**改动面与词表/清单不变性的原始核验**

```
$ git diff --stat b2bd459 c12aa59
 docs/08-gtm-channel-execution-pack.md | 114 +++++++++++++++++++++++++++++++++-----
 1 file changed, 95 insertions(+), 19 deletions(-)

$ git diff --name-only b2bd459 c12aa59 -- marketing/
（空输出）

$ md5sum (ro / ro-parent / ro-d43)/marketing/compliance/lexicon.json
59e3e1e733f81ad068ff686628e8d2a6   ×3（一致）
$ md5sum (ro / ro-parent)/marketing/compliance/scan_manifest.json
917f9ee945d2a578238d81c6aba8c6c7   ×2（一致）
```

> **读数限定（不得省略）**：机器复跑只证明「本单基线未出现**可判定**违规」。词表仍是正则匹配，**语义类表述仍可能漏检**（§4.4 #6 / §9.5 #2）；反之引文行也可能因缺引用标记而**误报**（N-1 属后者）。两类盲区都需人工复审兜底，这也是本报告存在的原因。

---

## 7. 边界与「未做」（如实记录）

1. **交付物 = 判定**，不是改稿：本报告**未改写任何文案**，**未改** `docs/05` / `docs/06` / `docs/07`，**未改** `lexicon.json` / `scan_manifest.json` / 任何 `marketing/` 物料与判据，**未重跑**物料生成脚本，**未新增数字或承诺等级**。
2. **未改** `docs/08`：§4 的 O-11「原文 + 改法」**只在本报告内提出**（按窄范围边界，观察项由跟进单承接）。
3. **未重开** PRO-12 / PRO-13 / PRO-14 / PRO-15 / PRO-16 / PRO-17 / PRO-18 / PRO-19 / PRO-20 / PRO-22 / PRO-23 的既有结论；对 O-7 的口径**沿用 PRO-23 的「不升级」裁定**（本报告 §2.3）。
4. **未做**：人工通读物料层（本单为窄范围复核，`marketing/` 未变，故沿用 PRO-20 的物料层通读结论）；商标 / 域名初筛；对外部署。
5. **未用豁免掩盖真实违规**：§6 的「逐行复算」只在**只读副本**内进行，共享分支未被写入；且本报告**明确禁止**以「扩豁免 / 放宽 `quoted_context_markers` / 收窄扫描目标」消解任何项（§2.8 已核无此类动作）。
6. 本报告在共享分支 `PRO-1-pro-1` **就地覆盖**，`git add` 仅本文件；**不写入本轮的自身提交哈希**（延续 PRO-19 §12 的哈希口径）；本轮提交回执见 PRO-26 评论。

---

*报告结束 · Editor（人工复审）· PRO-26 增量复审（窄范围）· 复审基线 `c12aa59` · 判定：**通过**（0 项阻断级；N-1 / N-1b / O-9 / O-10①②③ / §14 与一致性 / 计数与放宽 逐条已闭合；另附 O-11 低观察项，不阻断）*
